---
phase: 11-aeo-refinement
plan: 07
type: execute
wave: 3
depends_on: []
files_modified:
  - src/pages/near-williams.astro
  - src/components/Footer.astro
  - .lighthouserc.json
autonomous: true
requirements: [AEO-08]
requirements_addressed: [AEO-08]
must_haves:
  truths:
    - "src/pages/near-williams.astro exists, mirrors near-grand-canyon.astro structure exactly"
    - "Page builds to dist/near-williams/index.html with canonical link, no build errors"
    - "Page is auto-discovered by @astrojs/sitemap and appears in dist/sitemap-0.xml"
    - ".lighthouserc.json includes /near-williams/ in the url list"
    - "Footer Explore section includes a link to /near-williams/"
    - "Page emits inline FAQPage JSON-LD with 3 Williams-relevant questions and a SpeakableSpecification block"
    - "Page uses the same speakable-heading / speakable-lead / speakable-hours classes as near-grand-canyon.astro"
  artifacts:
    - path: "src/pages/near-williams.astro"
      provides: "GEO landing page for Williams + Kaibab Estates West audience"
      contains: "About 18 Miles East of Williams"
    - path: "src/components/Footer.astro"
      provides: "Updated Explore list including Near Williams"
      contains: "/near-williams/"
    - path: ".lighthouserc.json"
      provides: "Lighthouse CI URL list including new GEO page"
      contains: "/near-williams/"
  key_links:
    - from: "astro.config.mjs sitemap()"
      to: "dist/sitemap-0.xml"
      via: "Auto-discovery of src/pages/near-williams.astro"
      pattern: "near-williams"
    - from: "src/components/Footer.astro Explore list"
      to: "src/pages/near-williams.astro"
      via: "Internal anchor link"
      pattern: "href=\"/near-williams/\""
---

<objective>
Create `src/pages/near-williams.astro` as a GEO landing page targeting Williams, AZ tourists and Kaibab Estates West residents. Mirror `src/pages/near-grand-canyon.astro` structurally and stylistically — same surface tokens, same H2 sections, same `speakable-heading` / `speakable-lead` / `speakable-hours` class hooks, same inline FAQ + SpeakableSpecification injection pattern. Substitute city/distance/title strings per Research §"near-williams.astro Substitution Table". Wire it up: add to `.lighthouserc.json`, add to `Footer.astro` Explore list. Astro sitemap auto-discovers it.

Purpose: AEO-08. Phase 11 success criterion #5 calls for a dedicated `/near-williams/` page covering Williams (~18 mi) and Kaibab Estates West (~5 mi). The existing `near-grand-canyon.astro` is the canonical AEO/GEO template — copying it ensures structural consistency across the GEO page set and avoids re-deriving the answer-first pattern.

Output: New page at `src/pages/near-williams.astro`; updated `Footer.astro`; updated `.lighthouserc.json`. After `npm run build`, `dist/near-williams/index.html` exists, `dist/sitemap-0.xml` lists `/near-williams/`, and Lighthouse CI runs against the new URL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-aeo-refinement/11-CONTEXT.md
@.planning/phases/11-aeo-refinement/11-RESEARCH.md
@src/pages/near-grand-canyon.astro
@src/components/Footer.astro
@.lighthouserc.json
@astro.config.mjs

<interfaces>
<!-- Source-of-truth template: src/pages/near-grand-canyon.astro (222 lines, verified) -->
<!-- Same Layout component, same Tailwind/M3 token usage, same inline JSON-LD blocks. -->

Page-specific class hooks (verified across project — plain CSS hooks, no Tailwind output):
- `.speakable-heading` — H1 of the page (anchor for cssSelector)
- `.speakable-lead` — first lead paragraph (anchor for cssSelector)
- `.speakable-hours` — Hours section paragraph (anchor for cssSelector)

Astro sitemap integration (astro.config.mjs:19): `sitemap()` auto-scans src/pages/ — no manual sitemap edit needed. Verify post-build.

BreadcrumbSchema (src/layouts/Layout.astro:125): auto-generates `Home > Near Williams` from URL via slugToLabel. No edit needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/pages/near-williams.astro from near-grand-canyon.astro template (AEO-08)</name>
  <files>src/pages/near-williams.astro</files>
  <read_first>
    - src/pages/near-grand-canyon.astro (full file — 222 lines — line-by-line template)
    - .planning/phases/11-aeo-refinement/11-RESEARCH.md (§"near-williams.astro Substitution Table" — exhaustive line-by-line diff)
    - .planning/phases/11-aeo-refinement/11-CONTEXT.md (§"`near-williams.astro` GEO Page" — locked structure)
  </read_first>
  <action>
Copy `src/pages/near-grand-canyon.astro` to `src/pages/near-williams.astro` and apply the substitutions below. Preserve every Tailwind class, every M3 surface token (`bg-surface-container-lowest`, `bg-surface-container`, `text-on-surface`, `text-on-surface-variant`, `text-primary-container`), every typography utility (`text-display-md`, `text-heading-lg`, `text-heading-md`, `text-body-lg`), every spacing class. Structure is identical — only content strings change.

**Substitutions (verbatim from Research §"near-williams.astro Substitution Table"):**

1. **Layout `title`**: `"Indian Restaurant Near Williams, AZ — Spice Grill & Bar (~18 Miles East via I-40)"`

2. **Layout `description`**: `"Spice Grill & Bar is about 18 miles east of Williams, AZ on I-40 Exit 146 — roughly 18 minutes. Authentic Punjabi Indian cuisine on historic Route 66."`

3. **Layout `ogTitle`**: `"Indian Restaurant Near Williams, AZ — Spice Grill & Bar"`

4. **Layout `ogDescription`**: `"About 18 miles east of Williams, AZ. Authentic Punjabi cuisine at I-40 Exit 146 in Ash Fork, AZ."`

5. **H1** (the `<h1 class="speakable-heading text-[1.875rem] md:text-display-md text-on-surface mb-6">…</h1>`): replace inner content with:
   ```astro
   Spice Grill &amp; Bar —
   <span class="text-primary-container">About 18 Miles East of Williams, AZ</span>
   ```

6. **Speakable lead** (the `<p class="speakable-lead text-body-lg text-on-surface-variant mb-6">…</p>`):
   ```
   Spice Grill &amp; Bar is located in Ash Fork, Arizona, about 18 miles east of Williams via I-40 East — roughly 18 minutes to Exit 146. It is the closest Indian restaurant to Williams and Kaibab Estates West on the I-40 corridor.
   ```

7. **Standalone exit sentence** (the second `<p>` after the lead): UNCHANGED — keep `Spice Grill &amp; Bar is located at I-40 Exit 146 in Ash Fork, Arizona.`

8. **Why Stop Here body** (the `<p>` inside `<div class="bg-surface-container p-8 rounded-2xl">`):
   ```
   Spice Grill &amp; Bar is the closest authentic Indian restaurant to Williams, the gateway to the Grand Canyon Railway, and to Kaibab Estates West, a residential community ~5 miles east of Ash Fork. Whether you're catching the train to the Canyon or live nearby, our tandoor cooking and freshly baked naan are about 18 minutes east on I-40 Exit 146.
   ```

9. **Distance from Nearby Cities section** — replace the 8-paragraph block with these 5 paragraphs (in this order, each as a `<p class="text-body-lg text-on-surface-variant">…</p>`):

   - `Spice Grill &amp; Bar is about 18 miles from Williams, AZ — about 18 minutes east on I-40. Take Exit 146 in Ash Fork.`
   - `Spice Grill &amp; Bar is about 5 miles from Kaibab Estates West — about 6 minutes west on I-40. Take Exit 146 in Ash Fork.`
   - `Spice Grill &amp; Bar is about 78 miles from the Grand Canyon South Rim — about 1 hour 20 minutes via AZ-64 South and I-40. Take Exit 146 in Ash Fork.`
   - `Spice Grill &amp; Bar is about 51 miles from Flagstaff, AZ — about 46 minutes west on I-40. Take Exit 146 in Ash Fork.`
   - `Spice Grill &amp; Bar is about 25 miles from Seligman, AZ — about 24 minutes west on I-40. Take Exit 146 in Ash Fork.`

   DROP the Phoenix, Las Vegas, Los Angeles, and Kingman paragraphs. They are less relevant to a Williams audience and add length without proximity value.

10. **What to Order section**: UNCHANGED — keep the Fish Pakora / Butter Chicken / Shahi Paneer / Chicken Biryani / Garlic Naan grid exactly as is.

11. **Hours section** (`<p class="speakable-hours">…</p>`): copy verbatim from source (it already shows Monday open):
    ```
    Spice Grill &amp; Bar is open Monday through Thursday from 8 AM to 9 PM, and Friday through Sunday from 8 AM to 10 PM.
    ```
    This sentence on the source file is already correct — no change needed beyond the copy.

12. **Plan Your Visit section** (3 cross-link buttons): UNCHANGED structurally. Keep all three links (`/directions/`, `/`, `/faq/`).

13. **Inline FAQ JSON-LD `mainEntity`** — replace the 3 Grand Canyon questions with these 3 Williams-relevant ones (each `acceptedAnswer.text` ≤45 words):

    ```json
    [
      {
        "@type": "Question",
        "name": "How far is Spice Grill & Bar from Williams, AZ?",
        "acceptedAnswer": {
          "@type": "Answer",
          "text": "Spice Grill & Bar is about 18 miles east of Williams, AZ on I-40 — roughly 18 minutes. Take Exit 146 in Ash Fork. We are the closest Indian restaurant to Williams along the I-40 corridor."
        }
      },
      {
        "@type": "Question",
        "name": "Is there an Indian restaurant near Kaibab Estates West?",
        "acceptedAnswer": {
          "@type": "Answer",
          "text": "Yes. Spice Grill & Bar is about 5 miles west of Kaibab Estates West on I-40 — roughly 6 minutes via Exit 146 in Ash Fork. We are the closest sit-down Indian restaurant to the community."
        }
      },
      {
        "@type": "Question",
        "name": "What is the best Indian restaurant near Williams, AZ on I-40?",
        "acceptedAnswer": {
          "@type": "Answer",
          "text": "Spice Grill & Bar — at I-40 Exit 146 in Ash Fork — is the closest authentic Punjabi restaurant to Williams. About 18 miles east, 18 minutes via I-40. Tandoor cooking, fresh naan, full bar."
        }
      }
    ]
    ```

14. **SpeakableSpecification `name`**: `"Indian Restaurant Near Williams, AZ"`. The `cssSelector` array UNCHANGED — keep `[".speakable-heading", ".speakable-lead", ".speakable-hours"]`.

**Critical constraints:**

1. **NO new glass consumers.** Page uses M3 tokens (`bg-surface-container-lowest`, `bg-surface-container`) only. Do NOT add `glass`, `glass-card`, `backdrop-blur`.
2. **NO Tailwind utility classes in cssSelector.** The `.speakable-*` class hooks are stable plain CSS classes; do not substitute utility classes.
3. **NO stale hours.** The Hours section sentence already shows Monday OPEN in the source — copy it verbatim. Do NOT introduce any "Closed Mondays" or "Mon: Closed" string.
4. Two-space indentation, single-quote strings in frontmatter, double-quote strings in HTML attributes (matches source).
5. `<main class="pt-32 pb-24 px-6 bg-surface-container-lowest min-h-screen">` (identical to source).

The page does NOT need a separate `<canonical>` link — `Layout.astro:68` auto-generates canonical from `Astro.url.pathname`.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test -f dist/near-williams/index.html &amp;&amp; grep -c '<link rel="canonical"' dist/near-williams/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (no Astro errors)
    - `test -f dist/near-williams/index.html` exits 0
    - `grep -c '<link rel="canonical"' dist/near-williams/index.html` returns 1
    - `grep -c 'href="https://spicegrillbar66.com/near-williams/"' dist/near-williams/index.html` returns ≥1 (canonical resolves to correct URL)
    - `grep -c 'near-williams' dist/sitemap-0.xml` returns 1 (auto-discovered by @astrojs/sitemap)
    - `grep -c '"@type":"FAQPage"' dist/near-williams/index.html` returns 1 (inline FAQ block)
    - `grep -c '"@type":"SpeakableSpecification"' dist/near-williams/index.html` returns 1
    - `grep -c "speakable-heading" dist/near-williams/index.html` returns ≥1
    - `grep -c "About 18 Miles East of Williams" dist/near-williams/index.html` returns 1
    - `grep -c "Kaibab Estates West" dist/near-williams/index.html` returns ≥2
    - `grep -cE "Closed Mondays|Mon: Closed" dist/near-williams/index.html` returns 0
    - `grep -cE "backdrop-blur|glass-card" src/pages/near-williams.astro` returns 0
    - `grep -cE "Phoenix|Las Vegas|Los Angeles" src/pages/near-williams.astro` returns 0 (those cities dropped per Substitution Table)
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>near-williams.astro builds; canonical correct; in sitemap; mirrors near-grand-canyon.astro; no glass; no dropped cities; no stale hours.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add /near-williams/ to .lighthouserc.json and Footer.astro Explore list</name>
  <files>.lighthouserc.json, src/components/Footer.astro</files>
  <read_first>
    - .lighthouserc.json (current url list at line 5)
    - src/components/Footer.astro (Explore section at lines 67–92)
    - .planning/phases/11-aeo-refinement/11-RESEARCH.md (§"near-williams.astro Substitution Table > Side effects" — locked changes)
  </read_first>
  <action>
**Edit 1 — `.lighthouserc.json` line 5.**

OLD:
```json
      "url": ["/", "/near-grand-canyon/", "/directions/", "/faq/"],
```

NEW:
```json
      "url": ["/", "/near-grand-canyon/", "/near-williams/", "/directions/", "/faq/"],
```

Insert `/near-williams/` between `/near-grand-canyon/` and `/directions/` (groups GEO pages together). Trailing slash matches existing pattern. Do NOT change `numberOfRuns`, `assert.assertions`, or any other config.

**Edit 2 — `src/components/Footer.astro` Explore list.**

In the `<ul>` under the `Explore` heading (lines 67–92), insert a new `<li>` for Near Williams BETWEEN the existing Near Grand Canyon `<li>` (lines 69–76) and the Directions `<li>` (lines 77–84). Use 8-space indentation (matches surrounding `<li>` blocks). Place this exact block AFTER the Near Grand Canyon `</li>` (after line 76):

```astro
        <li>
          <a
            href="/near-williams/"
            class="hover:text-on-surface transition-colors block focus-visible:outline-hidden focus-visible:ring-2 focus-visible:ring-outline rounded-sm"
          >
            Near Williams
          </a>
        </li>
```

Anchor text `Near Williams` mirrors existing `Near Grand Canyon` short-form. The `class` attribute string is copied verbatim from the existing `<a>` tag — same hover, focus, and rounded-sm utilities.

Do NOT touch the social-icon `<li>` blocks (Instagram, Facebook) at the bottom of the list. Do NOT touch the `<p>Explore</p>` heading or the surrounding `md:grid-cols-4` layout — the grid absorbs an additional list item without overflow.
  </action>
  <verify>
    <automated>grep -c '"/near-williams/"' .lighthouserc.json &amp;&amp; grep -c 'href="/near-williams/"' src/components/Footer.astro &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"/near-williams/"' .lighthouserc.json` returns 1
    - `grep -c 'href="/near-williams/"' src/components/Footer.astro` returns 1
    - `grep -c "Near Williams" src/components/Footer.astro` returns 1
    - `grep -c "Near Grand Canyon" src/components/Footer.astro` returns 1 (regression check — existing entry preserved)
    - `grep -c "Directions" src/components/Footer.astro` returns ≥1 (regression — Directions still present)
    - `grep -c "FAQ" src/components/Footer.astro` returns ≥1 (regression — FAQ still present)
    - `npm run build` exits 0 (Footer renders cleanly on every page)
    - `grep -c 'href="/near-williams/"' dist/index.html` returns ≥1 (Footer link rendered to home page)
    - `grep -c 'href="/near-williams/"' dist/faq/index.html` returns ≥1 (Footer link rendered on /faq/)
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>.lighthouserc.json url list contains /near-williams/; Footer Explore list shows Near Williams between Near Grand Canyon and Directions; built HTML on all pages includes the Footer link.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0
- `test -f dist/near-williams/index.html` exits 0
- `grep -c near-williams dist/sitemap-0.xml` returns 1
- `grep -c '/near-williams/' .lighthouserc.json` returns 1
- `grep -c 'href="/near-williams/"' src/components/Footer.astro` returns 1
- `npm run test:lhci` runs against /near-williams/ and meets thresholds (LCP < 4000ms, TBT < 600ms, CLS < 0.105, A11y ≥ 90, BP ≥ 80, SEO ≥ 90)
</verification>

<success_criteria>
AEO-08 satisfied. /near-williams/ exists as a fully-indexed GEO page mirroring near-grand-canyon.astro structure, discoverable via sitemap and Footer, and covered by Lighthouse CI.
</success_criteria>

<output>
After completion, create `.planning/phases/11-aeo-refinement/11-07-SUMMARY.md` recording: (a) line counts of the new page vs. near-grand-canyon.astro source (should be ~similar — Phoenix/Vegas/LA paragraphs dropped, others substituted), (b) the dist/sitemap-0.xml entry, (c) Lighthouse scores from `npm run test:lhci` for the new URL.
</output>
