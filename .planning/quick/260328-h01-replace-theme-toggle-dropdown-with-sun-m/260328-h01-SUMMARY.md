---
phase: quick
plan: 260328-h01
subsystem: ui
tags: [theme-toggle, react, ux, e2e]
dependency_graph:
  requires: []
  provides: [simple-icon-theme-toggle]
  affects: [src/components/mode-toggle.tsx, e2e/animations.spec.ts]
tech_stack:
  added: []
  patterns: [onClick-toggle, aria-label, css-dark-class]
key_files:
  created:
    - e2e/animations.spec.ts
  modified:
    - src/components/mode-toggle.tsx
    - package.json
decisions:
  - Removed DropdownMenu entirely; binary light/dark toggle needs only a single button
  - Used aria-label="Toggle theme" directly on Button instead of sr-only span
  - Added @playwright/test to devDependencies (previously missing from worktree package.json)
metrics:
  duration: ~6 min
  completed: 2026-03-28
  tasks_completed: 2
  files_changed: 3
---

# Quick 260328-h01: Replace Theme Toggle Dropdown with Sun/Moon Icon Button — Summary

**One-liner:** Replaced DropdownMenu-based theme toggle with a single Button onClick that toggles the `dark` classList, showing Sun in dark mode and Moon in light mode.

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | Replace dropdown with icon toggle button | aaf5618 | src/components/mode-toggle.tsx |
| 2 | Update e2e animation test for new toggle behavior | 0aaed38 | e2e/animations.spec.ts, package.json |

## What Was Built

**mode-toggle.tsx** is now a minimal, focused component:
- Single `Button` with `variant="ghost" size="icon"` and `onClick` handler
- `isDark` state tracked via `useState`; initialized from `document.documentElement.classList.contains('dark')` in `useEffect`
- Click toggles: adds/removes `dark` class, persists to `localStorage`
- Sun icon visible when dark mode active; Moon icon visible in light mode (CSS transitions preserved)
- `aria-label="Toggle theme"` replaces the old `sr-only` span approach

**e2e/animations.spec.ts** updated:
- Removed `DropdownMenu animation` describe block (referenced `data-radix-menu-content`, now invalid)
- Added `Theme toggle` describe block that validates: initial state is light, first click adds `dark` class, second click removes it
- Sheet and MobileActionButtons test blocks preserved unchanged

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added @playwright/test to package.json devDependencies**
- **Found during:** Task 2
- **Issue:** Worktree's `package.json` lacked `@playwright/test` and `playwright` devDependencies; ESLint `import/no-extraneous-dependencies` blocked commit of `e2e/animations.spec.ts`
- **Fix:** Added `@playwright/test: ^1.49.0`, `playwright: ^1.49.0` to devDependencies and `test:e2e` script to package.json
- **Files modified:** package.json
- **Commit:** 0aaed38

## Verification

- No `DropdownMenu` references in mode-toggle.tsx (count: 0)
- `onClick` handler present in mode-toggle.tsx (count: 2)
- `aria-label="Toggle theme"` present (count: 1)
- No `data-radix-menu-content` in e2e test (count: 0)
- `toggle theme` present in e2e test (count: 1)
- ESLint pre-commit hook passed on both commits

## Known Stubs

None — component is fully wired. Theme state persists to localStorage and reflects actual DOM state.

## Self-Check: PASSED
