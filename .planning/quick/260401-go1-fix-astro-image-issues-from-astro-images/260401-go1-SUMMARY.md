---
phase: quick-260401-go1
plan: 01
subsystem: ui
tags: [astro, image-optimization, avif, webp, picture, cls, lcp]

requires: []
provides:
  - Hero image with Picture component generating AVIF+WebP srcset across 8 FULL widths (640-2560)
  - OurStorySection image with Picture component generating AVIF+WebP srcset across 5 HALF widths (320-1600)
  - GoogleMap preview img with explicit width/height/decoding preventing CLS
affects: [lighthouse-performance, lcp, cls]

tech-stack:
  added: []
  patterns:
    - "Use Picture (not Image) from astro:assets for all content images requiring AVIF+WebP srcset"
    - "FULL widths pattern [640,750,828,1080,1200,1920,2048,2560] for full-viewport LCP images"
    - "HALF widths pattern [320,640,960,1280,1600] for half-viewport content images"
    - "quality=60 with modern formats produces smaller files than quality=80 JPEG/WebP-only"

key-files:
  created: []
  modified:
    - src/components/Hero.astro
    - src/components/OurStorySection.astro
    - src/components/GoogleMap.tsx

key-decisions:
  - "Picture over Image for Hero and OurStorySection to generate AVIF+WebP format variants"
  - "quality=60 sufficient with AVIF compression — saves bandwidth vs legacy quality=80"
  - "No loading prop on OurStorySection — default lazy is correct for below-fold"
  - "No loading=lazy on GoogleMap img — IntersectionObserver already handles deferred loading"

requirements-completed: [IMG-01, IMG-02, IMG-03]

duration: 8min
completed: 2026-04-01
---

# Quick Task 260401-go1: Fix Astro Image Issues Summary

**Hero and OurStorySection upgraded to Picture with AVIF+WebP srcset at quality 60; GoogleMap preview img given explicit dimensions to eliminate CLS**

## Performance

- **Duration:** 8 min
- **Started:** 2026-04-01T12:00:00Z
- **Completed:** 2026-04-01T12:08:02Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Hero.astro: `Image` replaced with `Picture`, AVIF+WebP, 8 FULL widths, quality 60, sizes 100vw — proper LCP optimization
- OurStorySection.astro: `Image` replaced with `Picture`, AVIF+WebP, 5 HALF widths, quality 60, decoding async — eliminates oversized image serving
- GoogleMap.tsx: Explicit `width={3230}` and `height={2176}` on preview `<img>` plus `decoding="async"` — eliminates CLS

## Task Commits

Each task was committed atomically:

1. **Task 1: Upgrade Hero.astro and OurStorySection.astro to Picture component** - `b4c951d` (perf)
2. **Task 2: Add width/height/decoding to GoogleMap.tsx preview img** - `6713f13` (perf)

## Files Created/Modified

- `src/components/Hero.astro` — Changed import to Picture, replaced Image tag with Picture using 8 FULL widths, AVIF+WebP, quality 60, sizes 100vw
- `src/components/OurStorySection.astro` — Changed import to Picture, replaced Image tag with Picture using 5 HALF widths, AVIF+WebP, quality 60, decoding async
- `src/components/GoogleMap.tsx` — Added width/height/decoding attributes to preview img tag

## Decisions Made

- Used `Picture` (not `Image`) to get format variants (AVIF, WebP) generated by Astro's image pipeline
- Dropped quality from 80 to 60 — AVIF compression at 60 equals or exceeds WebP at 80 in perceived quality while reducing file size
- No `loading` prop on OurStorySection's Picture — Astro defaults to lazy for below-fold images, which is correct
- No `loading="lazy"` on GoogleMap img — the IntersectionObserver wrapper already handles deferred loading; adding lazy would be redundant

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- First commit attempt failed commitlint validation (subject-case and body-max-line-length). Fixed the commit message to use sentence-case subject and shortened body lines to under 100 characters. No code changes were needed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- All three image issues from the astro-images audit are resolved
- Build succeeds with 4 pages built; AVIF and WebP variants generated for Hero and OurStorySection
- CLS fix for GoogleMap preview is in place; Lighthouse CLS score should improve on next run

---
*Phase: quick-260401-go1*
*Completed: 2026-04-01*
