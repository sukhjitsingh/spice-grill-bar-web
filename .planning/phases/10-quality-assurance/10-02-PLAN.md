---
phase: 10-quality-assurance
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/styles/globals.css
autonomous: true
requirements: [QA-01, QA-02]

must_haves:
  truths:
    - "npm run test:lhci passes on all 4 pages in light mode"
    - "npm run test:lhci:dark passes on all 4 pages in dark mode"
    - "npm run test:axe passes with zero contrast violations in light mode"
    - "npm run test:axe:dark passes with zero contrast violations in dark mode"
  artifacts:
    - path: "src/styles/globals.css"
      provides: "M3 token values that pass WCAG AA contrast in both modes"
      contains: "--on-surface-variant"
  key_links:
    - from: "src/styles/globals.css :root"
      to: "All light mode components"
      via: "CSS custom properties"
      pattern: "--on-surface-variant"
    - from: "src/styles/globals.css .dark"
      to: "All dark mode components"
      via: "CSS custom properties"
      pattern: "--on-surface-variant"
---

<objective>
Run Lighthouse CI and axe-core contrast audits on all 4 pages in both light and dark mode. Fix every failure at the token level in globals.css. No per-component overrides (per D-02).

Purpose: QA-01 requires Lighthouse thresholds met, QA-02 requires WCAG AA contrast in both modes. This plan runs the audits, interprets failures, and adjusts M3 token hex values in globals.css until all gates pass.
Output: Green Lighthouse CI (light + dark), green axe contrast audit (light + dark), any token adjustments committed in globals.css
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-quality-assurance/10-CONTEXT.md
@.planning/phases/10-quality-assurance/10-RESEARCH.md
@.planning/phases/10-quality-assurance/10-01-SUMMARY.md
</context>

<interfaces>
<!-- Key tokens in globals.css that may need adjustment -->
<!-- From src/styles/globals.css :root block (light mode): -->
<!-- --on-surface: hex -->
<!-- --on-surface-variant: hex -->
<!-- --surface-container-low: hex -->
<!-- --surface-container: hex -->
<!-- --surface-dim: hex -->
<!-- --primary-container: hex -->
<!-- --on-primary-container: hex -->
<!-- Same tokens exist in .dark block -->
<!-- ALL fixes go in these two blocks only (D-02) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run Lighthouse CI + axe-core audits (light and dark) and fix all failures</name>
  <files>src/styles/globals.css</files>
  <read_first>
    - src/styles/globals.css (current M3 token hex values in :root and .dark blocks)
    - .lighthouserc.json (light mode thresholds)
    - .lighthouserc.dark.json (dark mode thresholds)
    - package.json (verify test commands exist from Plan 01)
    - docs/DESIGN.md (Radiant Sommelier color spec — understand aesthetic intent before changing tokens)
  </read_first>
  <action>
**Step 1 — Run light mode audits:**
```bash
npm run build && npm run test:lhci
```
Record any Lighthouse assertion failures (LCP, TBT, CLS, accessibility, best-practices, SEO).

**Step 2 — Run light mode contrast audit:**
Start dev server if not running, then:
```bash
npm run test:axe
```
(This runs `axe http://localhost:4321` against all configured pages.)
Record any color-contrast violations.

**Step 3 — Run dark mode Lighthouse audit:**
```bash
npm run test:lhci:dark
```
Record any failures.

**Step 4 — Run dark mode contrast audit:**
```bash
npm run test:axe:dark
```
Record any color-contrast violations.

**Step 5 — Fix ALL failures:**

For **contrast violations** (per D-02 and D-07):
- Identify the failing token pair (e.g., `--on-surface-variant` on `--surface-container-low`)
- Adjust the `--on-*` token hex value in the appropriate block (`:root` for light, `.dark` for dark)
- For light mode: darken the text token (lower lightness) until ratio >= 4.5:1
- For dark mode: lighten the text token (higher lightness) until ratio >= 4.5:1
- Use a contrast calculator to verify: foreground hex vs background hex >= 4.5:1
- ONLY modify globals.css `:root` and `.dark` blocks — no per-component overrides
- Keep changes minimal — shift hex values by small increments, maintaining warm tint
- Common failing pairs from research: `--on-surface-variant` on `--surface-container`/`--surface-container-low`

For **Lighthouse performance failures:**
- LCP > 4000ms: Check for unoptimized hero images, render-blocking resources
- TBT > 600ms: Check for heavy JS bundles, long tasks
- CLS > 0.1: Check for missing width/height on images, font FOIT
- These are unlikely (site was passing before Phase 9) but fix at source if found

For **Lighthouse accessibility < 0.9:**
- Usually overlaps with axe contrast findings
- May also include missing alt text, ARIA labels, landmark issues
- Fix at component level if structural; at token level if color-related

**Step 6 — Re-run ALL 4 audit commands to confirm green:**
```bash
npm run build && npm run test:lhci
npm run test:lhci:dark
npm run test:axe
npm run test:axe:dark
```

Iterate Steps 5-6 until all 4 commands pass with zero failures.
  </action>
  <verify>
    <automated>npm run build && npm run test:lhci && npm run test:lhci:dark</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:lhci` exits 0 (all 4 pages pass LCP < 4000, TBT < 600, CLS < 0.1, A11y >= 0.9, BP >= 0.8, SEO >= 0.9)
    - `npm run test:lhci:dark` exits 0 (same thresholds, dark mode)
    - `npm run test:axe` reports zero violations on all 4 pages (light mode)
    - `npm run test:axe:dark` exits 0 with "All 4 pages pass dark mode contrast audit"
    - If globals.css was modified: changes are ONLY in `:root { }` or `.dark { }` blocks (no changes outside these blocks)
    - If globals.css was modified: no new CSS custom properties added (only existing values adjusted)
    - `npm run qa` still passes (build + quality + lhci light)
  </acceptance_criteria>
  <done>All 4 Lighthouse CI runs green (light + dark), all 4 axe contrast audits green (light + dark), any token fixes committed to globals.css at the :root/.dark level only</done>
</task>

</tasks>

<verification>
```bash
# Full verification sequence
npm run build && npm run test:lhci
npm run test:lhci:dark
# (with dev server running)
npm run test:axe
npm run test:axe:dark
# Existing QA gate
npm run qa
```
All commands exit 0.
</verification>

<success_criteria>
Lighthouse CI passes all thresholds on all 4 pages in both light and dark mode. Axe-core reports zero contrast violations in both modes. The qa pre-push hook still passes. Any token adjustments are confined to globals.css :root and .dark blocks.
</success_criteria>

<output>
After completion, create `.planning/phases/10-quality-assurance/10-02-SUMMARY.md`
</output>
