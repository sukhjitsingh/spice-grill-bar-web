---
phase: 09-visual-redesign
plan: 04
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/components/MenuSection.tsx
  - src/components/OrderSection.astro
  - src/components/LocationSection.astro
  - src/components/Header.tsx
autonomous: true
requirements:
  - VISUAL-02
  - VISUAL-03
  - VISUAL-06

must_haves:
  truths:
    - "MenuSection uses bg-surface-container-low, no borders on tab strip or item cards, glass removed from sidebar"
    - "OrderSection uses bg-surface-dim + cta-gradient, no Unsplash background image"
    - "LocationSection uses bg-surface-container, map container uses tonal lift not glass-card"
    - "Header logo uses text-label-sm, nav links use text-body-md, SheetContent border-l removed"
    - "Glass (backdrop-blur) only exists on Header (.glass), Sheet (.glass-card), Dropdown (.glass-card), OrderSection (.glass), and OurStory photo caption (.glass)"
  artifacts:
    - path: "src/components/MenuSection.tsx"
      provides: "Reskinned menu with tonal cards"
      contains: "bg-surface-container-low"
    - path: "src/components/OrderSection.astro"
      provides: "Reskinned order CTA"
      contains: "cta-gradient"
    - path: "src/components/LocationSection.astro"
      provides: "Reskinned location section"
      contains: "bg-surface-container"
    - path: "src/components/Header.tsx"
      provides: "Updated header typography"
      contains: "text-label-sm"
  key_links:
    - from: "src/components/MenuSection.tsx"
      to: "globals.css"
      via: "text-heading-lg, text-heading-md, text-body-md utilities"
      pattern: "text-heading-md"
    - from: "src/components/OrderSection.astro"
      to: "globals.css"
      via: "text-display-md, text-body-lg, cta-gradient"
      pattern: "cta-gradient"
---

<objective>
Reskin MenuSection, OrderSection, LocationSection, and Header with Radiant Sommelier tokens.

Purpose: Complete the remaining home page components and header. MenuSection is the largest React component needing border removal and glass budget enforcement. Header typography needs updating per UI-SPEC.
Output: All 4 components using correct surface tokens, typography, and glass budget.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-visual-redesign/09-CONTEXT.md
@.planning/phases/09-visual-redesign/09-UI-SPEC.md
@.planning/phases/09-visual-redesign/09-RESEARCH.md
@src/components/MenuSection.tsx
@src/components/OrderSection.astro
@src/components/LocationSection.astro
@src/components/Header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin MenuSection.tsx (border removal, glass budget, typography)</name>
  <files>src/components/MenuSection.tsx</files>
  <read_first>
    - src/components/MenuSection.tsx (current state — border-t, glass sidebar, border on mobile nav, raw typography)
    - .planning/phases/09-visual-redesign/09-UI-SPEC.md (Menu contract — lines 249-257)
    - .planning/phases/09-visual-redesign/09-RESEARCH.md (Menu delta — lines 274-286)
  </read_first>
  <action>
Apply the following changes per UI-SPEC MenuSection contract and research audit:

**Section outer:** Replace `bg-surface-container-lowest border-t border-outline-variant` (or similar) with `bg-surface-container-low`. Remove `border-t`.

**Sticky header wrapper:** Change `bg-surface-container-lowest` to `bg-surface-container-low`.

**Section H2:** Replace raw typography classes with `text-heading-lg text-on-surface`.

**Mobile nav wrapper:** Replace `border border-outline-variant bg-surface-container/50 backdrop-blur-sm` with `bg-surface-container rounded-xl`. Remove border, remove backdrop-blur (outside glass budget).

**Mobile nav button active state:** Replace `bg-surface shadow-xs border-outline-variant` (or similar) with `bg-surface-container-high text-on-surface`. Remove border. Inactive state: `bg-transparent text-on-surface-variant`.

**Desktop sidebar nav:** Replace `.glass rounded-xl` with `bg-surface-container-high rounded-xl`. Remove `.glass` — the sidebar is an ambient card, not a UI chrome overlay (per research Open Question 3 and VISUAL-02 glass budget).

**Active sidebar button:** Keep `bg-primary-container text-on-primary-container` (accent context 1 — correct).

**Category heading:** Replace `text-2xl font-medium text-primary-container border-b border-outline-variant pb-4` with `text-heading-md text-on-surface`. Remove orange from category heading (NOT an approved accent context). Remove `border-b`.

**Item name:** Replace `text-xl font-medium font-display` with `text-heading-md text-on-surface`.

**Item price:** Replace `text-lg font-medium text-on-surface-variant` with `text-heading-md text-primary-container` (per UI-SPEC: price IS accent context 1, co-located with order action).

**Item description:** Apply `text-body-md text-on-surface-variant`.

**Spicy/dietary tags:** Apply `text-label-sm` with `bg-surface-container-highest text-on-surface-variant` (if tags exist).

**Remove ALL `border-outline-variant`**, `border-t`, `shadow-xs`, `shadow-lg`, `backdrop-blur-sm` from this file.
  </action>
  <verify>
    <automated>grep -q 'bg-surface-container-low' src/components/MenuSection.tsx && grep -q 'text-heading-lg' src/components/MenuSection.tsx && ! grep -q 'border-outline-variant' src/components/MenuSection.tsx && ! grep 'class.*glass' src/components/MenuSection.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - MenuSection.tsx outer section has `bg-surface-container-low`
    - Section H2 uses `text-heading-lg text-on-surface`
    - No `border-outline-variant` anywhere in file
    - No `border-t` used for section separation
    - No `.glass` class on desktop sidebar (replaced with `bg-surface-container-high`)
    - No `backdrop-blur-sm` on mobile nav wrapper
    - Category headings use `text-on-surface` (NOT `text-primary-container`)
    - Item prices use `text-primary-container`
    - `npm run build` passes
  </acceptance_criteria>
  <done>MenuSection uses surface-container-low, tonal nav/cards, no borders, glass removed from sidebar</done>
</task>

<task type="auto">
  <name>Task 2: Reskin OrderSection, LocationSection, and Header</name>
  <files>src/components/OrderSection.astro, src/components/LocationSection.astro, src/components/Header.tsx</files>
  <read_first>
    - src/components/OrderSection.astro (current state — bg-surface-container-low, Unsplash bg image)
    - src/components/LocationSection.astro (current state — bg-surface-container-low, glass-card on map)
    - src/components/Header.tsx (current state — logo typography, nav link typography, SheetContent border-l)
    - .planning/phases/09-visual-redesign/09-UI-SPEC.md (Order contract lines 259-266, Location lines 269-273, Header lines 275-280)
    - .planning/phases/09-visual-redesign/09-RESEARCH.md (Order delta lines 289-295, Location lines 299-303, Header lines 307-312)
  </read_first>
  <action>
**OrderSection.astro:**
- Background: Replace `bg-surface-container-low` with `bg-surface-dim`. Add `.cta-gradient` class alongside bg-surface-dim (per D-10)
- REMOVE: Any Unsplash background image URL (`bg-[url('https://images.unsplash.com/...')]`). The `.cta-gradient` overlay is the intended background per D-10. Removing external URL also eliminates a network request for LCP safety.
- Glass card: KEEP `.glass` on the inner card container — this IS a budgeted glass surface (CTA focal point per UI-SPEC)
- H2: Replace raw typography with `text-[1.875rem] md:text-display-md text-on-surface` (responsive: 30px mobile, 40px desktop)
- Lead paragraph: Apply `text-body-lg text-on-surface-variant`. Keep "Pickup" and "Curbside" spans with `text-primary-container` (already present per accent context 4)
- CTA button: Keep "Start Order" with `bg-primary-container text-on-primary-container` (accent context 1)
- Fine print: Apply `text-label-sm text-on-surface-variant`

**LocationSection.astro:**
- Background: Replace `bg-surface-container-low` with `bg-surface-container` (per D-10)
- H2: Replace raw typography with `text-heading-lg text-on-surface`
- Lead paragraph: Apply `text-body-md text-on-surface-variant`. Keep "Biker and Family Friendly!" in `text-primary-container` if present (accent context 4)
- Map container: Replace `glass-card p-2 rounded-2xl` with `bg-surface-container-high rounded-2xl overflow-hidden`. Remove glass-card — map container is not on glass budget
- Remove any `border-outline-variant`

**Header.tsx:**
- Logo text: Replace current `text-lg tracking-tighter font-display font-semibold text-on-surface` with `text-label-sm uppercase tracking-tighter font-semibold text-on-surface`. Change from text-lg to text-label-sm, add uppercase.
- Nav links: Replace `text-xs font-bold font-display` with `text-body-md text-on-surface-variant`. Remove `font-display` and `font-bold` from nav links — they use body-md (Inter), not display font.
- Active nav state: Keep `text-primary-container` (accent context 3)
- Desktop separator: Keep `border-l border-outline-variant` — this is a functional separator in header chrome, not structural layout (per research)
- SheetContent (mobile nav): Replace `bg-surface border-l border-outline-variant` with `bg-surface-container-low`. Add or verify `.glass-card` class is present (Sheet IS on glass budget). Remove `border-l` — glass utility inner glow replaces it.
- Mode toggle: Keep existing — `text-on-surface-variant` icon button
  </action>
  <verify>
    <automated>grep -q 'cta-gradient' src/components/OrderSection.astro && grep -q 'bg-surface-dim' src/components/OrderSection.astro && ! grep -q 'unsplash' src/components/OrderSection.astro && grep -q 'bg-surface-container[^-]' src/components/LocationSection.astro && ! grep -q 'glass-card' src/components/LocationSection.astro && grep -q 'text-label-sm' src/components/Header.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - OrderSection.astro has `bg-surface-dim` and `cta-gradient`
    - OrderSection.astro does NOT contain any Unsplash URL
    - OrderSection.astro H2 uses `text-display-md`
    - OrderSection.astro retains `.glass` on the CTA card container
    - LocationSection.astro outer section has `bg-surface-container` (not `bg-surface-container-low`)
    - LocationSection.astro map container uses `bg-surface-container-high` (not `glass-card`)
    - LocationSection.astro H2 uses `text-heading-lg`
    - Header.tsx logo uses `text-label-sm uppercase`
    - Header.tsx nav links use `text-body-md` (not `text-xs font-bold font-display`)
    - Header.tsx SheetContent has no `border-l` class
    - `npm run build` passes
  </acceptance_criteria>
  <done>OrderSection, LocationSection, and Header all reskinned with correct tokens, typography, and glass budget</done>
</task>

</tasks>

<verification>
- `grep -rn 'unsplash' src/components/OrderSection.astro` returns no matches
- `grep -rn 'glass-card' src/components/LocationSection.astro` returns no matches
- `grep -rn 'glass' src/components/MenuSection.tsx` returns no matches (glass removed from sidebar)
- All home sections use correct surface alternation per D-10
- `npm run build` passes
</verification>

<success_criteria>
- MenuSection: bg-surface-container-low, no borders, no glass on sidebar
- OrderSection: bg-surface-dim + cta-gradient, no Unsplash image
- LocationSection: bg-surface-container, tonal map container
- Header: text-label-sm logo, text-body-md nav, no SheetContent border
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-visual-redesign/09-04-SUMMARY.md`
</output>
