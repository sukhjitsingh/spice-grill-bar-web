---
phase: 11-aeo-refinement
plan: 03
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - public/llms.txt
  - public/llms-full.txt
autonomous: true
requirements: [AEO-04]
requirements_addressed: [AEO-04]
must_haves:
  truths:
    - "public/llms.txt and public/llms-full.txt show Monday OPEN (8:00 AM – 9:00 PM) under Operating Hours"
    - "The bundled FAQ block in llms.txt no longer says 'Closed Mondays'"
    - "Both files contain new H2 sections: ## Payment Methods, ## Reservation Policy, ## Delivery & Takeout, ## Amenities, ## Dietary Options"
    - "grep -iE 'mon[a-z]*: closed|monday: closed|closed mondays' returns 0 hits across both files"
  artifacts:
    - path: "public/llms.txt"
      provides: "Leaner AI-crawler doc with corrected hours and 5 new sections"
      contains: "## Payment Methods"
    - path: "public/llms-full.txt"
      provides: "Full AI-crawler doc with corrected hours and 5 new sections"
      contains: "## Payment Methods"
  key_links:
    - from: ".planning/phases/11-aeo-refinement/11-OWNER-CONFIRMATION.md"
      to: "public/llms.txt § Payment Methods + Reservation Policy + Amenities"
      via: "Executor reads owner-confirmed values before writing prose"
      pattern: "Status: confirmed"
---

<objective>
Eliminate the Monday hours drift in `public/llms.txt` (lines 13 + 37) and `public/llms-full.txt` (line 18), then add 5 new H2 sections to both files: Payment Methods, Reservation Policy, Delivery & Takeout, Amenities, Dietary Options. The Payment Methods, Reservation Policy, and Amenities sections must reflect owner-confirmed values from `11-OWNER-CONFIRMATION.md` (Plan 11-01) — not invented placeholders.

Purpose: AEO-04. AI crawlers fetch these files when discovering the site (`<link rel="help" href="/llms.txt">` is in `Layout.astro:70`). Today both files claim Monday closed — exactly the wrong signal. Adding payment/reservation/amenity/delivery/dietary sections gives ChatGPT/Gemini/Perplexity/Claude direct prose answers for the highest-volume voice queries.
Output: Updated `public/llms.txt` and `public/llms-full.txt` whose Operating Hours blocks list Monday open and whose new H2 sections appear after the existing Frequently Asked Questions / Pages blocks (placement detail in actions below).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-aeo-refinement/11-CONTEXT.md
@.planning/phases/11-aeo-refinement/11-RESEARCH.md
@.planning/phases/11-aeo-refinement/11-OWNER-CONFIRMATION.md
@public/llms.txt
@public/llms-full.txt
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify owner confirmation status</name>
  <files>.planning/phases/11-aeo-refinement/11-OWNER-CONFIRMATION.md</files>
  <read_first>
    - .planning/phases/11-aeo-refinement/11-OWNER-CONFIRMATION.md (must show `Status: confirmed`)
  </read_first>
  <action>
Run this exact command and confirm it returns 1:

```bash
grep -c "^Status: confirmed" .planning/phases/11-aeo-refinement/11-OWNER-CONFIRMATION.md
```

If it returns 0, STOP. The owner has not filled the checklist yet. Surface this back to the user with: "Plan 11-03 blocked: 11-OWNER-CONFIRMATION.md has Status: awaiting-owner. Please fill the checklist and rerun." Do NOT invent payment methods or amenity values.

If it returns 1, proceed. Read the filled checklist and extract:
- Payment methods list (the `[x]` ticked items under §1)
- Reservation policy choice (the `[x]` ticked item under §2)
- Amenity list (the `[x]` ticked items under §3)

These extracted values feed Tasks 2 and 3.
  </action>
  <verify>
    <automated>grep -c "^Status: confirmed" .planning/phases/11-aeo-refinement/11-OWNER-CONFIRMATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^Status: confirmed" .planning/phases/11-aeo-refinement/11-OWNER-CONFIRMATION.md` returns 1
    - Executor records the extracted payment methods, reservation policy, and amenity list in their working notes for use by Tasks 2 and 3
  </acceptance_criteria>
  <done>OWNER-CONFIRMATION.md verified `confirmed`; values extracted for Tasks 2 and 3.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix Monday hours + add 5 sections in public/llms.txt (AEO-04)</name>
  <files>public/llms.txt</files>
  <read_first>
    - public/llms.txt (full file — current line 13 says "Mon: Closed", line 37 FAQ block says "Closed Mondays")
    - .planning/phases/11-aeo-refinement/11-OWNER-CONFIRMATION.md (Status: confirmed; values extracted in Task 1)
    - .planning/phases/11-aeo-refinement/11-RESEARCH.md (§"Code Examples > llms.txt Monday fix + new sections")
    - .planning/phases/11-aeo-refinement/11-CONTEXT.md (§"llms.txt / llms-full.txt Section Plan" — 5 H2 section names locked)
  </read_first>
  <action>
Make exactly TWO edits to `public/llms.txt`:

**EDIT A — Fix Monday hours in two places:**

1. Replace lines 13–16 (the Operating Hours bullets):

OLD:
```
- **Mon**: Closed
- **Tue - Thu**: 8:00 AM - 9:00 PM
- **Fri - Sun**: 8:00 AM - 10:00 PM
```

NEW:
```
- **Mon - Thu**: 8:00 AM - 9:00 PM
- **Fri - Sun**: 8:00 AM - 10:00 PM
```

2. Replace line 37 (the FAQ-block answer):

OLD:
```
A: Tuesday-Thursday 8:00 AM - 9:00 PM, Friday-Sunday 8:00 AM - 10:00 PM. Closed Mondays.
```

NEW:
```
A: Monday-Thursday 8:00 AM - 9:00 PM, Friday-Sunday 8:00 AM - 10:00 PM.
```

**EDIT B — Add 5 new H2 sections AFTER the existing `## Frequently Asked Questions` block (which ends around line 43, before `## Links`).** Insert this block, in this exact order, with section headers verbatim — `aeo-audit.mjs` (Plan 11-08) will grep for these literal strings:

```
## Payment Methods
Spice Grill & Bar accepts {PAYMENT_LIST_FROM_OWNER_CONFIRMATION}. We do not require a minimum purchase for card transactions.

## Reservation Policy
{RESERVATION_PROSE_FROM_OWNER_CONFIRMATION}. Phone us at (928) 277-1292 to confirm availability for large parties.

## Delivery & Takeout
Takeout, pickup, and curbside service available. Order online via Toast at https://order.toasttab.com/online/spice-grill-bar-33-lewis-ave or call (928) 277-1292. We do not offer in-house delivery; third-party apps are not currently integrated.

## Amenities
{AMENITY_BULLETS_FROM_OWNER_CONFIRMATION}

## Dietary Options
Vegetarian and vegan options available, including Shahi Paneer, Dal Tadka, Aloo Gobhi, and Chana Masala. Spice level is customizable from mild to extra hot — let your server know your preference. Gluten-free accommodations on request; ask your server about specific dishes.
```

Substitution rules for the `{...}` placeholders:

- `{PAYMENT_LIST_FROM_OWNER_CONFIRMATION}` — comma-separated, owner-confirmed methods, in this canonical order if ticked: cash, Visa, Mastercard, American Express, Discover, debit cards, Apple Pay, Google Pay. If owner ticked "Other (specify)" with text, append it.
  Example output: `Spice Grill & Bar accepts cash, Visa, Mastercard, American Express, Discover, debit cards, Apple Pay, and Google Pay.`

- `{RESERVATION_PROSE_FROM_OWNER_CONFIRMATION}` — pick the prose form matching the ticked option:
  - Walk-in only ticked: `Walk-ins welcome — we do not take reservations`
  - Phone reservations ticked: `Walk-ins welcome. Phone reservations accepted at (928) 277-1292`
  - Online reservations ticked: `Walk-ins welcome. Reservations available at <URL>`

- `{AMENITY_BULLETS_FROM_OWNER_CONFIRMATION}` — Markdown bullet list, one ticked amenity per line. Use these exact bullet phrasings when the owner has ticked the matching checkbox:
  - `- On-site car and motorcycle parking` (matches "Free on-site parking")
  - `- RV and truck parking across the road at the truck stop` (matches "RV / truck parking nearby")
  - `- Wheelchair accessible entrance`
  - `- Indoor seating`
  - `- Outdoor seating`
  - `- Family-friendly with seating for groups of all sizes`
  - `- Free Wi-Fi for customers`
  - `- Full bar (beer, wine, cocktails)`
  - For any "Other (specify)" text, append: `- {custom amenity text}`

Do NOT add amenity bullets the owner did not tick. Do NOT add a `(Owner-confirmation pending — placeholder)` note (this was a draft pattern in research; we have actual confirmation now).

Leave the `## Links` section and everything else in `llms.txt` UNCHANGED.
  </action>
  <verify>
    <automated>grep -iE "mon[a-z]*: closed|closed mondays" public/llms.txt | wc -l | tr -d " "</automated>
  </verify>
  <acceptance_criteria>
    - `grep -iE "mon[a-z]*: closed|closed mondays" public/llms.txt` returns 0 hits (exit code 1)
    - `grep -c "Mon - Thu" public/llms.txt` returns 1
    - `grep -c "Monday-Thursday 8:00 AM - 9:00 PM" public/llms.txt` returns 1
    - `grep -c "^## Payment Methods$" public/llms.txt` returns 1
    - `grep -c "^## Reservation Policy$" public/llms.txt` returns 1
    - `grep -c "^## Delivery & Takeout$" public/llms.txt` returns 1
    - `grep -c "^## Amenities$" public/llms.txt` returns 1
    - `grep -c "^## Dietary Options$" public/llms.txt` returns 1
    - `grep -c "## Links" public/llms.txt` returns 1 (regression check — Links section still present, must come after the 5 new sections)
    - Section ordering: `awk '/^## /{print NR, $0}' public/llms.txt` shows Payment Methods → Reservation Policy → Delivery & Takeout → Amenities → Dietary Options → Links (the 5 new sections in this order, before Links)
    - File contains no `{PAYMENT_LIST_FROM_OWNER_CONFIRMATION}`, `{RESERVATION_PROSE_FROM_OWNER_CONFIRMATION}`, or `{AMENITY_BULLETS_FROM_OWNER_CONFIRMATION}` placeholders (executor substituted them)
  </acceptance_criteria>
  <done>public/llms.txt has corrected hours in both Operating Hours and FAQ blocks; 5 new H2 sections present with owner-confirmed content.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix Monday hours + add 5 sections in public/llms-full.txt (AEO-04)</name>
  <files>public/llms-full.txt</files>
  <read_first>
    - public/llms-full.txt (full file — current line 18 says "Monday: Closed")
    - .planning/phases/11-aeo-refinement/11-OWNER-CONFIRMATION.md (same Status: confirmed values from Task 1)
    - .planning/phases/11-aeo-refinement/11-CONTEXT.md (same 5 H2 section names locked)
    - public/llms.txt (after Task 2 — use as the canonical phrasing reference; both files should agree on payment/reservation/amenity/delivery/dietary content)
  </read_first>
  <action>
Make exactly TWO edits to `public/llms-full.txt`:

**EDIT A — Fix Monday hours at line 18:**

OLD (lines 17–20):
```
## Operating Hours
- **Monday**: Closed
- **Tuesday - Thursday**: 8:00 AM - 9:00 PM
- **Friday - Sunday**: 8:00 AM - 10:00 PM
```

NEW:
```
## Operating Hours
- **Monday - Thursday**: 8:00 AM - 9:00 PM
- **Friday - Sunday**: 8:00 AM - 10:00 PM
```

**EDIT B — Add 5 new H2 sections AFTER the existing `## Pages` block (around line 35, before `## Full Menu`).** Use the SAME prose as `public/llms.txt` from Task 2 (do not introduce new wording — these two files must agree). Section headers must be verbatim — `aeo-audit.mjs` Plan 11-08 will grep for these literal strings (it reads `llms.txt`, but consistency between the two files is a P1 requirement).

Insert this block, in this exact order:

```
## Payment Methods
Spice Grill & Bar accepts {PAYMENT_LIST_FROM_OWNER_CONFIRMATION}. We do not require a minimum purchase for card transactions.

## Reservation Policy
{RESERVATION_PROSE_FROM_OWNER_CONFIRMATION}. Phone us at (928) 277-1292 to confirm availability for large parties.

## Delivery & Takeout
Takeout, pickup, and curbside service available. Order online via Toast at https://order.toasttab.com/online/spice-grill-bar-33-lewis-ave or call (928) 277-1292. We do not offer in-house delivery; third-party apps are not currently integrated.

## Amenities
{AMENITY_BULLETS_FROM_OWNER_CONFIRMATION}

## Dietary Options
Vegetarian and vegan options available, including Shahi Paneer, Dal Tadka, Aloo Gobhi, and Chana Masala. Spice level is customizable from mild to extra hot — let your server know your preference. Gluten-free accommodations on request; ask your server about specific dishes.
```

Substitution rules — IDENTICAL to Task 2 (use the SAME extracted values from OWNER-CONFIRMATION.md so both files match exactly).

Leave `## Full Menu` and every section after it (Appetizers, Veg Dishes, Non-Veg Dishes, Rice, Egg Dishes, Breads, Sides, Desserts) UNCHANGED.
  </action>
  <verify>
    <automated>grep -iE "monday: closed|closed mondays" public/llms-full.txt | wc -l | tr -d " "</automated>
  </verify>
  <acceptance_criteria>
    - `grep -iE "monday: closed|closed mondays" public/llms-full.txt` returns 0 hits (exit code 1)
    - `grep -c "Monday - Thursday" public/llms-full.txt` returns 1
    - `grep -c "^## Payment Methods$" public/llms-full.txt` returns 1
    - `grep -c "^## Reservation Policy$" public/llms-full.txt` returns 1
    - `grep -c "^## Delivery & Takeout$" public/llms-full.txt` returns 1
    - `grep -c "^## Amenities$" public/llms-full.txt` returns 1
    - `grep -c "^## Dietary Options$" public/llms-full.txt` returns 1
    - `grep -c "## Full Menu" public/llms-full.txt` returns 1 (regression check — Full Menu still present, must come AFTER the 5 new sections)
    - Section ordering: `awk '/^## /{print NR, $0}' public/llms-full.txt` shows Payment Methods → Reservation Policy → Delivery & Takeout → Amenities → Dietary Options → Full Menu
    - Cross-file consistency: `diff <(awk '/^## Payment Methods/,/^## Dietary Options/' public/llms.txt) <(awk '/^## Payment Methods/,/^## Dietary Options/' public/llms-full.txt)` returns 0 differences (the 5-section block is identical between the two files, OR differs only in the Reservation Policy line if owner-confirmed phrasing varies — but Payment, Delivery, Amenities, Dietary blocks must be identical)
    - File contains no `{...FROM_OWNER_CONFIRMATION}` placeholders
  </acceptance_criteria>
  <done>public/llms-full.txt has corrected Monday hours; 5 new H2 sections present with owner-confirmed content matching public/llms.txt.</done>
</task>

</tasks>

<verification>
- `grep -iE "mon[a-z]*: closed|monday: closed|closed mondays" public/llms.txt public/llms-full.txt` returns 0 hits
- Both files contain all 5 new section headers in the locked order
- Cross-file content for Payment/Delivery/Amenities/Dietary blocks is identical
- `npm run test:aeo` exits 0 (existing gates do not regress; new gates land in Plan 11-08)
</verification>

<success_criteria>
AI crawlers fetching `/llms.txt` or `/llms-full.txt` see Monday open and find authoritative prose for payment, reservation, delivery/takeout, amenity, and dietary queries. Plan 11-08 (audit-script gates) can grep for the 5 H2 headers and pass.
</success_criteria>

<output>
After completion, create `.planning/phases/11-aeo-refinement/11-03-SUMMARY.md` recording the exact substituted owner-confirmed values for payment / reservation / amenities (so future audits can verify consistency without re-reading OWNER-CONFIRMATION.md), and the line-count delta on each file.
</output>
