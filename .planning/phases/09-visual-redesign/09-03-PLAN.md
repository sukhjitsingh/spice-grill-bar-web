---
phase: 09-visual-redesign
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/components/OurStorySection.astro
  - src/components/ReviewsSection.astro
  - src/components/Footer.astro
autonomous: true
requirements:
  - VISUAL-03
  - VISUAL-06

must_haves:
  truths:
    - "OurStory section uses bg-surface-container-low and editorial typography utilities"
    - "ReviewsSection uses bg-surface-dim, no borders on review cards, no decorative blobs"
    - "Footer uses bg-surface-container-low with tonal copyright bar, no border-t anywhere"
    - "No border-outline-variant exists in any of these 3 components"
  artifacts:
    - path: "src/components/OurStorySection.astro"
      provides: "Reskinned story section"
      contains: "bg-surface-container-low"
    - path: "src/components/ReviewsSection.astro"
      provides: "Reskinned reviews section"
      contains: "bg-surface-dim"
    - path: "src/components/Footer.astro"
      provides: "Reskinned footer with tonal separation"
      contains: "bg-surface-container-low"
  key_links:
    - from: "src/components/OurStorySection.astro"
      to: "globals.css"
      via: "text-display-md, text-heading-lg, text-body-md, text-label-sm utilities"
      pattern: "text-display-md"
    - from: "src/components/ReviewsSection.astro"
      to: "globals.css"
      via: "text-heading-lg, text-body-lg utilities"
      pattern: "text-heading-lg"
---

<objective>
Reskin OurStorySection, ReviewsSection, and Footer with Radiant Sommelier tokens and typography.

Purpose: These 3 static Astro components need surface hierarchy updates, border removal, and editorial typography applied. They share no file dependencies and can be done together efficiently.
Output: All 3 components using correct surface tokens, typography utilities, and no structural borders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-visual-redesign/09-CONTEXT.md
@.planning/phases/09-visual-redesign/09-UI-SPEC.md
@.planning/phases/09-visual-redesign/09-RESEARCH.md
@src/components/OurStorySection.astro
@src/components/ReviewsSection.astro
@src/components/Footer.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin OurStorySection.astro</name>
  <files>src/components/OurStorySection.astro</files>
  <read_first>
    - src/components/OurStorySection.astro (current state — glass-card usage, bg-surface, raw typography)
    - .planning/phases/09-visual-redesign/09-UI-SPEC.md (OurStory contract — lines 228-237)
    - .planning/phases/09-visual-redesign/09-RESEARCH.md (OurStory delta — lines 248-256)
  </read_first>
  <action>
Apply the following changes per UI-SPEC OurStory contract and research audit:

**Section background:** Change outer section from `bg-surface` (or whatever current) to `bg-surface-container-low`. Add section padding `py-24 md:py-32` if not already present.

**H2 heading:** Replace raw Tailwind size classes (e.g., `text-4xl md:text-5xl font-bold`) with `text-[1.875rem] md:text-display-md text-on-surface`. The responsive override uses 1.875rem (30px) on mobile, 2.5rem on md+ via the display-md utility.

**Body paragraphs:** Apply `text-body-md text-on-surface-variant` to all paragraph elements.

**Stat cards** ("25+ Signature Spices", "900°F Clay Oven", etc.):
- Remove `glass-card` class — replace with `bg-surface-container p-6 rounded-2xl`
- No border, no shadow, no backdrop-blur
- Stat number: Replace `text-3xl font-bold` (or similar) with `text-heading-lg text-primary-container` (accent context 4 — stat number highlight)
- Stat caption: Apply `text-label-sm text-on-surface-variant uppercase tracking-widest`

**Mission/Vision/Values cards:**
- Remove `glass-card` class — replace with `bg-surface-container rounded-2xl`
- No border, no shadow
- KEEP existing `hover:-translate-y-1` on cards (per UI-SPEC interaction contract)
- Card H3: Apply `text-heading-md` — keep `text-primary-container` (accent context 4 — card heading highlight)
- Card body: Apply `text-body-md text-on-surface-variant`
- Card gap: ensure `gap-4` (16px) between cards

**OurStory photo caption glass overlay:** RETAIN `.glass` class on the caption div that floats over the image — this is a floating overlay (per research Open Question 2).

**Remove any remaining `border-outline-variant`** from any element in this file.
  </action>
  <verify>
    <automated>grep -q 'bg-surface-container-low' src/components/OurStorySection.astro && grep -q 'text-display-md' src/components/OurStorySection.astro && ! grep -q 'glass-card' src/components/OurStorySection.astro && ! grep -q 'border-outline-variant' src/components/OurStorySection.astro && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - OurStorySection.astro outer section has `bg-surface-container-low`
    - H2 uses `text-display-md` (with mobile responsive override)
    - Stat cards use `bg-surface-container` (NOT `glass-card`)
    - Stat numbers use `text-heading-lg text-primary-container`
    - Stat captions use `text-label-sm text-on-surface-variant`
    - Mission/Vision/Values cards use `bg-surface-container` (NOT `glass-card`)
    - No `glass-card` class appears in the file
    - No `border-outline-variant` appears in the file
    - Photo caption overlay retains `.glass` class
    - `npm run build` passes
  </acceptance_criteria>
  <done>OurStorySection uses surface-container-low background, editorial typography, tonal cards with no borders</done>
</task>

<task type="auto">
  <name>Task 2: Reskin ReviewsSection.astro</name>
  <files>src/components/ReviewsSection.astro</files>
  <read_first>
    - src/components/ReviewsSection.astro (current state — border on review cards, decorative blobs, marquee fades)
    - .planning/phases/09-visual-redesign/09-UI-SPEC.md (Reviews contract — lines 239-247)
    - .planning/phases/09-visual-redesign/09-RESEARCH.md (Reviews delta — lines 259-271)
  </read_first>
  <action>
Apply the following changes per UI-SPEC ReviewsSection contract and research audit:

**Section background:** Add `bg-surface-dim` to the outer section element (currently has no background, inherits body). Per D-10 surface alternation.

**Section H2:** Replace raw `text-4xl md:text-5xl font-bold` with `text-heading-lg text-on-surface`. Keep `"Guests Say"` span with `text-primary-container` (accent context 4 — section accent detail).

**Lead paragraph:** Apply `text-body-lg text-on-surface-variant`.

**Review cards:** Replace the current complex card styling:
- REMOVE: `bg-surface-container/80 border border-outline-variant shadow-lg hover:border-primary-container/50 hover:shadow-xl` (or whatever current border/shadow combo exists)
- REPLACE WITH: `bg-surface-container rounded-2xl` (no border, no shadow, no hover-border)

**Decorative blob backgrounds:** REMOVE any elements with classes like `bg-primary-container/10 rounded-full blur-3xl` — these are decorative blobs that violate DESIGN.md "Do Not" rules.

**Marquee gradient fade masks:** Change `from-background` to `from-surface-dim` to match the section's `bg-surface-dim` background. This is critical for correct light mode rendering (in light mode `--surface-dim: #f3d3cb` differs from `--background: #fff8f6`).

**Avatar gradient circles:** Simplify `from-primary-container to-secondary-container` gradient border glow to flat `bg-surface-container-high` — no glow blur.

**Star icons:** Keep existing `fill-primary-container text-primary-container` (accent context 2 — correct).

**Source badges (Google/Yelp):** Keep platform colors — informational, not brand accent.

**Remove all `border-outline-variant`** and `shadow-lg` / `shadow-xs` from any element.
  </action>
  <verify>
    <automated>grep -q 'bg-surface-dim' src/components/ReviewsSection.astro && grep -q 'text-heading-lg' src/components/ReviewsSection.astro && ! grep -q 'border-outline-variant' src/components/ReviewsSection.astro && ! grep -q 'shadow-lg' src/components/ReviewsSection.astro && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - ReviewsSection.astro outer section has `bg-surface-dim`
    - H2 uses `text-heading-lg text-on-surface`
    - "Guests Say" span retains `text-primary-container`
    - Review cards use `bg-surface-container` with no `border`, no `shadow-lg`, no `shadow-xs`
    - No `border-outline-variant` appears in the file
    - No decorative blob divs remain (no `blur-3xl` on decorative elements)
    - Marquee fade uses `from-surface-dim` (not `from-background`)
    - Star icons use `fill-primary-container text-primary-container`
    - `npm run build` passes
  </acceptance_criteria>
  <done>ReviewsSection uses surface-dim background, editorial typography, borderless cards, correct marquee fades</done>
</task>

<task type="auto">
  <name>Task 3: Reskin Footer.astro with tonal separation</name>
  <files>src/components/Footer.astro</files>
  <read_first>
    - src/components/Footer.astro (current state — border-t, border-outline-variant, copyright bar)
    - .planning/phases/09-visual-redesign/09-UI-SPEC.md (Footer contract — lines 283-288)
    - .planning/phases/09-visual-redesign/09-RESEARCH.md (Footer delta — lines 316-321)
  </read_first>
  <action>
Apply the following changes per UI-SPEC Footer contract and research audit:

**Footer outer element:** Replace `bg-surface py-16 border-t border-outline-variant` with `bg-surface-container-low py-16`. Remove `border-t` — background tonal shift from previous section replaces the border.

**Section headers:** Apply `text-label-sm text-on-surface uppercase tracking-wider` to footer column headings.

**Links:** Apply `text-body-md text-on-surface-variant` with `hover:text-on-surface transition-colors duration-300`.

**Social list item border:** Remove `border-t border-outline-variant` from the Instagram/Facebook social list item (currently has `mt-3 pt-3 border-t border-outline-variant`). Keep `mt-3 pt-3` for spacing.

**Copyright bar:** The current copyright section has `mt-16 pt-8 pb-20 md:pb-8 border-t border-outline-variant`. Replace with a separate `<div>` wrapper: `bg-surface-container py-4` — creating tonal shift from the `surface-container-low` parent. Remove `border-t`.

**Copyright text:** Apply `text-label-sm text-on-surface-variant`.

**Remove ALL `border-outline-variant`** from every element in this file.
**Remove ALL `border-t`** from every element (both footer outer and copyright bar).
  </action>
  <verify>
    <automated>grep -q 'bg-surface-container-low' src/components/Footer.astro && ! grep -q 'border-t' src/components/Footer.astro && ! grep -q 'border-outline-variant' src/components/Footer.astro && grep -q 'text-label-sm' src/components/Footer.astro && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Footer.astro outer element has `bg-surface-container-low` (not `bg-surface`)
    - No `border-t` appears in the file
    - No `border-outline-variant` appears in the file
    - Copyright bar uses `bg-surface-container` (tonal shift from parent)
    - Section headers use `text-label-sm`
    - Links use `text-body-md text-on-surface-variant`
    - `npm run build` passes
  </acceptance_criteria>
  <done>Footer uses surface-container-low with tonal copyright bar, no borders anywhere, editorial typography</done>
</task>

</tasks>

<verification>
- `grep -rn 'border-outline-variant' src/components/OurStorySection.astro src/components/ReviewsSection.astro src/components/Footer.astro` returns no matches
- `grep -rn 'glass-card' src/components/OurStorySection.astro` returns no matches
- All 3 files use editorial typography utilities (text-display-md, text-heading-lg, text-body-md, text-label-sm)
- `npm run build` passes
</verification>

<success_criteria>
- OurStory: bg-surface-container-low, tonal cards, display-md heading
- Reviews: bg-surface-dim, borderless cards, from-surface-dim marquee fades
- Footer: bg-surface-container-low, tonal copyright bar, no borders
- No border-outline-variant in any of these 3 files
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-visual-redesign/09-03-SUMMARY.md`
</output>
