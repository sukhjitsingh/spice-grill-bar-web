---
phase: quick
plan: 260401-fv4
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/faq.astro
  - src/layouts/Layout.astro
  - src/pages/near-grand-canyon.astro
  - src/pages/directions.astro
autonomous: true
must_haves:
  truths:
    - "FAQ page has its own OG title, description, and URL in meta tags"
    - "FAQSchema JSON-LD only appears in HTML output for /faq/ page"
    - "GEO pages use Layout-computed canonicalURL for og:url, not hardcoded strings"
  artifacts:
    - path: "src/pages/faq.astro"
      provides: "FAQ-specific OG props passed to Layout"
      contains: "ogTitle"
    - path: "src/layouts/Layout.astro"
      provides: "Conditional FAQSchema rendering"
      contains: "currentPath.startsWith"
    - path: "src/pages/near-grand-canyon.astro"
      provides: "GEO page without hardcoded ogUrl"
    - path: "src/pages/directions.astro"
      provides: "GEO page without hardcoded ogUrl"
  key_links:
    - from: "src/pages/faq.astro"
      to: "src/layouts/Layout.astro"
      via: "ogTitle, ogDescription props"
      pattern: "ogTitle=|ogDescription="
---

<objective>
Fix three SEO issues: add FAQ-specific OG meta tags, scope FAQSchema to /faq/ only, and remove hardcoded ogUrl from GEO pages.

Purpose: Correct OG metadata so social shares show page-specific titles/descriptions, prevent Google from flagging FAQ schema on non-FAQ pages, and use dynamic canonical URL computation consistently.
Output: Four modified files with correct SEO metadata behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/pages/faq.astro
@src/layouts/Layout.astro
@src/pages/near-grand-canyon.astro
@src/pages/directions.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FAQ OG props and scope FAQSchema to /faq/ only</name>
  <files>src/pages/faq.astro, src/layouts/Layout.astro</files>
  <action>
1. In `src/pages/faq.astro`, add `ogTitle` and `ogDescription` props to the Layout component call:
   - `ogTitle="FAQ | Spice Grill & Bar — Common Questions Answered"`
   - `ogDescription="Find answers to common questions about Spice Grill & Bar — hours, location, menu, vegetarian options, and more."`
   - Do NOT add ogUrl — Layout already computes canonicalURL dynamically from `Astro.url.pathname` + `Astro.site` and uses it as the og:url fallback (line 74).

2. In `src/layouts/Layout.astro`, wrap the `<FAQSchema />` call (line 121) in a conditional:
   - Change `<FAQSchema />` to `{currentPath.startsWith('/faq') && <FAQSchema />}`
   - `currentPath` is already defined on line 24 as `Astro.url.pathname`.
   - This ensures FAQSchema JSON-LD only renders on /faq/ and not on homepage, directions, or near-grand-canyon pages.
  </action>
  <verify>
    <automated>cd /Users/moni/Workspace/SpiceGrillBarWeb/spice-grill-bar-web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>faq.astro passes ogTitle and ogDescription to Layout. FAQSchema only renders on /faq/ path. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Remove hardcoded ogUrl from GEO pages</name>
  <files>src/pages/near-grand-canyon.astro, src/pages/directions.astro</files>
  <action>
1. In `src/pages/near-grand-canyon.astro` (line 10), remove the entire `ogUrl="https://spicegrillbar66.com/near-grand-canyon/"` prop from the Layout call. Keep ogTitle and ogDescription props intact.

2. In `src/pages/directions.astro` (line 14), remove the entire `ogUrl="https://spicegrillbar66.com/directions/"` prop from the Layout call. Keep ogTitle and ogDescription props intact.

Layout already computes `canonicalURL` on line 25 from `Astro.url.pathname` + `Astro.site` and uses it as the og:url fallback on line 74 (`ogUrl ?? canonicalURL`). Removing the hardcoded values lets this dynamic computation take effect, which is safer if the domain ever changes.
  </action>
  <verify>
    <automated>cd /Users/moni/Workspace/SpiceGrillBarWeb/spice-grill-bar-web && npm run build 2>&1 | tail -5 && grep -c 'og:url' dist/near-grand-canyon/index.html && grep -c 'og:url' dist/directions/index.html</automated>
  </verify>
  <done>Neither GEO page has a hardcoded ogUrl prop. Build output still contains og:url meta tags (computed dynamically by Layout). Build succeeds with no errors.</done>
</task>

</tasks>

<verification>
After both tasks, verify the built output:

1. `grep 'og:title' dist/faq/index.html` should show "FAQ | Spice Grill & Bar — Common Questions Answered"
2. `grep 'og:description' dist/faq/index.html` should show the FAQ-specific description
3. `grep -c 'FAQPage' dist/index.html` should return 0 (no FAQ schema on homepage)
4. `grep -c 'FAQPage' dist/faq/index.html` should return 1+ (FAQ schema present on FAQ page)
5. `grep 'og:url' dist/near-grand-canyon/index.html` should NOT contain a hardcoded URL string as a prop value
6. `npm run build` completes without errors
</verification>

<success_criteria>
- FAQ page renders with page-specific OG title and description in meta tags
- FAQSchema JSON-LD appears only on /faq/ page, not on homepage or GEO pages
- GEO pages use Layout-computed canonical URL for og:url
- Full build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/quick/260401-fv4-fix-seo-issues-faq-og-tags-faqschema-sco/260401-fv4-SUMMARY.md`
</output>
