---
phase: quick
plan: 260328-h01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/mode-toggle.tsx
  - e2e/animations.spec.ts
autonomous: true
requirements: [QUICK-260328-h01]

must_haves:
  truths:
    - "Clicking the toggle in light mode switches to dark mode"
    - "Clicking the toggle in dark mode switches to light mode"
    - "Sun icon visible in dark mode, Moon icon visible in light mode"
    - "No dropdown menu appears on click"
  artifacts:
    - path: "src/components/mode-toggle.tsx"
      provides: "Simple icon toggle button (no dropdown)"
      contains: "onClick"
  key_links:
    - from: "src/components/mode-toggle.tsx"
      to: "document.documentElement.classList"
      via: "onClick handler toggles dark class"
      pattern: "classList.*dark"
---

<objective>
Replace the theme toggle dropdown menu with a simple sun/moon icon toggle button.

Purpose: Simplify the theme toggle UX — a binary light/dark choice does not need a dropdown menu. One click toggles between states.
Output: Updated mode-toggle.tsx as a single Button with onClick, updated e2e test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/mode-toggle.tsx
@e2e/animations.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace dropdown with icon toggle button</name>
  <files>src/components/mode-toggle.tsx</files>
  <action>
Rewrite ModeToggle to be a simple toggle button (no DropdownMenu):

1. Remove all dropdown-menu imports (DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger).
2. Keep imports: Moon, Sun from lucide-react, React, Button from ui/button.
3. Track current theme in state. Initialize from `document.documentElement.classList.contains('dark')` in useEffect (same as current).
4. Replace the DropdownMenu JSX with a single Button:
   - `variant="ghost"` `size="icon"` (same as current trigger)
   - `onClick` handler that toggles theme: if currently dark, switch to light; if light, switch to dark. Reuse the existing `setTheme` logic (classList add/remove, localStorage.setItem).
   - Show Sun icon when in dark mode (clicking switches to light). Show Moon icon when in light mode (clicking switches to dark).
   - Keep the CSS transition classes on icons: Sun gets `h-[1.2rem] w-[1.2rem]` with rotate/scale transitions for dark mode. Moon gets `absolute h-[1.2rem] w-[1.2rem]` with rotate/scale transitions. Same animation pattern as current.
   - `aria-label="Toggle theme"` on the Button (replaces the sr-only span approach for better accessibility).
5. The component should still be a named export: `export function ModeToggle()`.
  </action>
  <verify>
    <automated>cd /Users/moni/Workspace/SpiceGrillBarWeb/spice-grill-bar-web && npm run build && grep -c "DropdownMenu" src/components/mode-toggle.tsx | grep "^0$" && grep -c "onClick" src/components/mode-toggle.tsx | grep -v "^0$" && echo "PASS"</automated>
  </verify>
  <done>mode-toggle.tsx renders a single Button with onClick toggle. No DropdownMenu imports or usage. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Update e2e animation test for new toggle behavior</name>
  <files>e2e/animations.spec.ts</files>
  <action>
The "DropdownMenu animation" test block in e2e/animations.spec.ts (lines 28-46) tests the old dropdown behavior by clicking the toggle and asserting `[data-radix-menu-content]` appears. This will fail since there is no dropdown anymore.

Replace the "DropdownMenu animation" test.describe block with a "Theme toggle" test that:
1. Navigates to '/'.
2. Finds the toggle button via `page.getByRole('button', { name: /toggle theme/i })`.
3. Checks initial state (document.documentElement should not have class 'dark' since light is default).
4. Clicks the button.
5. Asserts `document.documentElement.classList.contains('dark')` is true via `page.evaluate`.
6. Clicks again.
7. Asserts dark class is removed.

This validates the toggle works without any dropdown/menu assertions. Keep all other test blocks (Sheet animation, MobileActionButtons animation) unchanged.
  </action>
  <verify>
    <automated>cd /Users/moni/Workspace/SpiceGrillBarWeb/spice-grill-bar-web && grep -c "data-radix-menu-content" e2e/animations.spec.ts | grep "^0$" && grep -c "toggle theme" e2e/animations.spec.ts | grep -v "^0$" && echo "PASS"</automated>
  </verify>
  <done>e2e test no longer references DropdownMenu/radix-menu-content. New test validates theme toggling via classList. All other animation tests unchanged.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- `npm run lint` passes
- `npm run typecheck` passes
- No "DropdownMenu" string in mode-toggle.tsx
- mode-toggle.tsx contains onClick handler and aria-label
- e2e test references toggle theme, not dropdown menu
</verification>

<success_criteria>
- Theme toggle is a single icon button: Sun in dark mode, Moon in light mode
- Clicking toggles between light and dark — no dropdown appears
- Build, lint, and typecheck all pass
- e2e test updated to match new behavior
</success_criteria>

<output>
After completion, create `.planning/quick/260328-h01-replace-theme-toggle-dropdown-with-sun-m/260328-h01-SUMMARY.md`
</output>
