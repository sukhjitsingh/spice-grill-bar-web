---
phase: quick-260401-go1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Hero.astro
  - src/components/OurStorySection.astro
  - src/components/GoogleMap.tsx
autonomous: true
requirements: [IMG-01, IMG-02, IMG-03]

must_haves:
  truths:
    - "Hero background generates AVIF and WebP srcset with 8 widths from 640 to 2560"
    - "OurStorySection image generates AVIF and WebP srcset with HALF-pattern widths"
    - "GoogleMap preview img has explicit width/height attributes preventing CLS"
  artifacts:
    - path: "src/components/Hero.astro"
      provides: "LCP image with Picture component, AVIF+WebP, quality 60"
      contains: "Picture"
    - path: "src/components/OurStorySection.astro"
      provides: "Below-fold image with Picture component, HALF pattern, decoding async"
      contains: "Picture"
    - path: "src/components/GoogleMap.tsx"
      provides: "Map preview img with width/height/decoding attributes"
      contains: "decoding"
  key_links:
    - from: "src/components/Hero.astro"
      to: "astro:assets"
      via: "Picture import"
      pattern: "import.*Picture.*astro:assets"
    - from: "src/components/OurStorySection.astro"
      to: "astro:assets"
      via: "Picture import"
      pattern: "import.*Picture.*astro:assets"
---

<objective>
Fix all Astro image optimization issues identified by the astro-images audit across Hero.astro, OurStorySection.astro, and GoogleMap.tsx.

Purpose: Improve LCP performance (Hero), reduce bandwidth waste (OurStorySection serving 4032px to all devices), and eliminate CLS from the map preview image.
Output: Three updated component files with proper image optimization props.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/Hero.astro
@src/components/OurStorySection.astro
@src/components/GoogleMap.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Hero.astro and OurStorySection.astro to Picture component</name>
  <files>src/components/Hero.astro, src/components/OurStorySection.astro</files>
  <action>
**Hero.astro (LCP image — CRITICAL):**
1. Change import from `import { Image } from 'astro:assets'` to `import { Picture } from 'astro:assets'`
2. Replace the `<Image>` tag with `<Picture>` using these exact props:
   - `src={heroBg}`
   - `alt="Authentic Punjabi cuisine restaurant interior"`
   - `class="object-cover opacity-40 scale-105 w-full h-full"`
   - `itemprop="image"`
   - `widths={[640, 750, 828, 1080, 1200, 1920, 2048, 2560]}`
   - `sizes="100vw"`
   - `formats={['avif', 'webp']}`
   - `quality={60}`
   - `loading="eager"`
   - `fetchpriority="high"`
3. Do NOT change any other element, class, or structure in the file.

**OurStorySection.astro (below-fold, HALF pattern):**
1. Change import from `import { Image } from 'astro:assets'` to `import { Picture } from 'astro:assets'`
2. Replace the `<Image>` tag with `<Picture>` using these exact props:
   - `src={garlicNaanImg}`
   - `alt="Tandoori Preparation"`
   - `class="w-full aspect-[4/5] object-cover rounded-2xl lg:grayscale lg:group-hover:grayscale-0 transition-all duration-700 ease-out transform group-hover:scale-105"`
   - `widths={[320, 640, 960, 1280, 1600]}`
   - `sizes="(min-width: 1024px) 50vw, 100vw"`
   - `formats={['avif', 'webp']}`
   - `quality={60}`
   - `decoding="async"`
3. Do NOT add `loading` prop (defaults to lazy, which is correct for below-fold).
4. Do NOT change any other element, class, or structure in the file.
  </action>
  <verify>
    <automated>cd /Users/moni/Workspace/SpiceGrillBarWeb/spice-grill-bar-web && grep -c "Picture" src/components/Hero.astro src/components/OurStorySection.astro && grep -c "formats" src/components/Hero.astro src/components/OurStorySection.astro && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Both files import Picture (not Image) from astro:assets. Hero uses FULL widths pattern with quality 60 and eager loading. OurStorySection uses HALF widths pattern with decoding async. Build succeeds without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add width/height/decoding to GoogleMap.tsx preview img</name>
  <files>src/components/GoogleMap.tsx</files>
  <action>
In GoogleMap.tsx, locate the `<img>` tag (line ~52) and add three attributes:
1. `width={3230}`
2. `height={2176}`
3. `decoding="async"`

The img tag should become:
```tsx
<img
  src="/location-preview.webp"
  alt="Map Preview"
  width={3230}
  height={2176}
  decoding="async"
  className="absolute inset-0 h-full w-full object-cover transition-opacity duration-500 group-hover:opacity-75"
/>
```

Do NOT add `loading="lazy"` — the IntersectionObserver already handles lazy loading.
Do NOT change any other code in the file.
  </action>
  <verify>
    <automated>cd /Users/moni/Workspace/SpiceGrillBarWeb/spice-grill-bar-web && grep -E "width=|height=|decoding=" src/components/GoogleMap.tsx && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>GoogleMap.tsx img tag has width={3230}, height={2176}, and decoding="async" attributes. Build succeeds. No other code changed.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `grep "Picture" src/components/Hero.astro` shows Picture usage
3. `grep "Picture" src/components/OurStorySection.astro` shows Picture usage
4. `grep "width={3230}" src/components/GoogleMap.tsx` confirms CLS fix
5. `npm run qa` passes all quality checks
</verification>

<success_criteria>
- Hero.astro uses Picture with AVIF+WebP, 8 FULL-pattern widths, quality 60, sizes 100vw
- OurStorySection.astro uses Picture with AVIF+WebP, 5 HALF-pattern widths, proper sizes attr, decoding async
- GoogleMap.tsx img has explicit width/height/decoding preventing CLS
- Full build succeeds, no regressions in Lighthouse CI
</success_criteria>

<output>
After completion, create `.planning/quick/260401-go1-fix-astro-image-issues-from-astro-images/260401-go1-SUMMARY.md`
</output>
