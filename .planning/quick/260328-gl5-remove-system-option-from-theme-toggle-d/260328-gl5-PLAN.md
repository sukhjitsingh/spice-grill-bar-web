---
phase: quick
plan: 260328-gl5
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/mode-toggle.tsx
  - public/scripts/theme.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "Theme toggle dropdown shows only Light and Dark options"
    - "No System option visible in theme dropdown"
    - "Selecting Light applies light theme, selecting Dark applies dark theme"
    - "Theme preference persists in localStorage across page reloads"
  artifacts:
    - path: "src/components/mode-toggle.tsx"
      provides: "Theme toggle with light/dark only"
    - path: "public/scripts/theme.js"
      provides: "Theme initialization from localStorage only"
  key_links:
    - from: "src/components/mode-toggle.tsx"
      to: "localStorage"
      via: "setTheme stores 'light' or 'dark'"
      pattern: "localStorage\\.setItem.*theme"
---

<objective>
Remove the "System" option from the theme toggle dropdown, keeping only Light and Dark.

Purpose: Simplify the theme toggle UX — two clear choices instead of three.
Output: Updated mode-toggle.tsx and theme.js with no system-preference detection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/mode-toggle.tsx
@public/scripts/theme.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove system option from mode-toggle and theme init</name>
  <files>src/components/mode-toggle.tsx, public/scripts/theme.js</files>
  <action>
In src/components/mode-toggle.tsx:
1. Remove the DropdownMenuItem that calls setTheme('system') (line 47).
2. Remove 'system' from the setTheme parameter union type — change to just 'light' | 'dark'.
3. Remove the system branch inside setTheme: delete the `newTheme === 'system'` condition from isDark calculation (line 22-23) and the `if (newTheme === 'system')` localStorage.removeItem block (lines 27-28).
4. Simplify setTheme: `const isDark = newTheme === 'dark'; document.documentElement.classList[isDark ? 'add' : 'remove']('dark'); localStorage.setItem('theme', newTheme);`
5. Remove 'system' from the useState type — change to `'theme-light' | 'dark'`.

In public/scripts/theme.js:
6. Remove the system/prefers-color-scheme fallback. The init script should read localStorage only: if theme === 'dark', add dark class; otherwise remove it. If no localStorage value exists, default to light (no dark class). Update to:
```js
const setMode = () => {
  const theme = localStorage.getItem('theme');
  document.documentElement.classList.toggle('dark', theme === 'dark');
};
setMode();
document.addEventListener('astro:after-swap', setMode);
```
  </action>
  <verify>
    <automated>cd /Users/moni/Workspace/SpiceGrillBarWeb/spice-grill-bar-web && npm run build && grep -c "system" src/components/mode-toggle.tsx | grep "^0$" && grep -c "prefers-color-scheme" public/scripts/theme.js | grep "^0$" && echo "PASS"</automated>
  </verify>
  <done>Theme toggle has exactly two options (Light, Dark). No "System" option. No prefers-color-scheme detection anywhere in theme code. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- No occurrence of "system" in mode-toggle.tsx (as theme option)
- No occurrence of "prefers-color-scheme" in public/scripts/theme.js
- `npm run lint` passes
</verification>

<success_criteria>
Theme toggle dropdown renders only "Light" and "Dark" menu items. Theme persists via localStorage with light as the default when no preference is stored.
</success_criteria>

<output>
After completion, create `.planning/quick/260328-gl5-remove-system-option-from-theme-toggle-d/260328-gl5-SUMMARY.md`
</output>
