---
phase: 08-token-system
plan: 04
type: execute
wave: 3
depends_on:
  - 08-02
  - 08-03
files_modified:
  - src/components/Header.tsx
  - src/components/Hero.astro
  - src/components/MenuSection.tsx
  - src/components/MobileActionButtons.astro
  - src/pages/near-grand-canyon.astro
  - src/pages/directions.astro
  - src/pages/faq.astro
autonomous: true
requirements:
  - TOKEN-03
  - TOKEN-04

must_haves:
  truths:
    - "All headings and display text use font-display (Manrope) not font-serif (Playfair Display)"
    - "Zero instances of font-serif remain in any src/ file"
    - "Zero instances of zinc-, neutral-, orange- hardcoded Tailwind classes remain in src/ (excluding globals.css)"
    - "Zero instances of brand-orange, brand-green, brand-gold remain in src/"
    - "npm run build passes cleanly"
    - "npm run qa passes (full quality gate including Lighthouse)"
  artifacts:
    - path: "src/components/Header.tsx"
      provides: "Header with font-display instead of font-serif"
      contains: "font-display"
    - path: "src/components/Hero.astro"
      provides: "Hero with font-display headline"
      contains: "font-display"
  key_links:
    - from: "font-display utility class"
      to: "src/styles/globals.css @theme inline"
      via: "--font-display: Manrope Variable"
      pattern: "font-display"
---

<objective>
Replace all font-serif references with font-display across the codebase and run a final zero-remnant verification sweep to confirm the entire M3 token migration is complete.

Purpose: Complete TOKEN-04 (font families registered and used correctly) and ensure TOKEN-03 has zero gaps. Plan 02 handles brand-*/shadcn token replacement but does not replace font-serif with font-display in its files. This plan closes that gap and validates the entire phase.

Output: All font-serif replaced with font-display. Full codebase verified clean of legacy tokens. Build and QA pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-token-system/08-CONTEXT.md
@.planning/phases/08-token-system/08-02-SUMMARY.md
@.planning/phases/08-token-system/08-03-SUMMARY.md

<interfaces>
<!-- Font family tokens from Plan 01 @theme inline in globals.css -->
--font-sans: "Inter Variable", ui-sans-serif, system-ui, sans-serif
--font-display: "Manrope Variable", ui-sans-serif, system-ui, sans-serif

<!-- Tailwind utility classes: -->
font-sans -> Inter Variable (body text)
font-display -> Manrope Variable (headings, display text)

<!-- NOTE: font-serif is NO LONGER registered in @theme inline after Plan 01 -->
<!-- Playfair Display was replaced by Manrope per D-25 -->
<!-- All font-serif usages will break (no matching font) and must become font-display -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace font-serif with font-display across all files</name>
  <files>src/components/Header.tsx, src/components/Hero.astro, src/components/MenuSection.tsx, src/components/MobileActionButtons.astro, src/pages/near-grand-canyon.astro, src/pages/directions.astro, src/pages/faq.astro</files>
  <read_first>
    - src/components/Header.tsx (lines 44, 49, 66, 76, 111: font-serif on logo, nav container, nav items, CTA button, mobile nav links)
    - src/components/Hero.astro (line 43: font-serif on main headline)
    - src/components/MenuSection.tsx (lines 105, 130, 160: font-serif on category tabs and item names)
    - src/components/MobileActionButtons.astro (line 16, 29: font-serif on action buttons)
    - src/pages/near-grand-canyon.astro (lines 16, 36, 53, 94, 105, 116, 129, 142, 155, 167, 178: font-serif on all headings and item names)
    - src/pages/directions.astro (lines 20, 103, 125, 147, 169, 191, 213, 235, 260: font-serif on all headings)
    - src/pages/faq.astro (lines 13, 22: font-serif on page title and question headings)
    - src/styles/globals.css (verify --font-display is registered in @theme inline from Plan 01)
  </read_first>
  <action>
Per D-25 and TOKEN-04: Playfair Display is replaced by Manrope. The `font-serif` utility no longer maps to anything (Plan 01 removed `--font-serif` from @theme inline). All `font-serif` classes must become `font-display` (which maps to Manrope Variable).

Perform a simple find-and-replace in each file: `font-serif` -> `font-display`.

**Header.tsx** (5 occurrences):
- Line 44: `font-serif` -> `font-display` (logo text)
- Line 49: `font-serif` -> `font-display` (desktop nav container)
- Line 66: `font-serif` -> `font-display` (desktop nav DropdownMenuTrigger)
- Line 76: `font-serif` -> `font-display` (Order Online CTA button)
- Line 111: `font-serif` -> `font-display` (mobile nav links)

**Hero.astro** (1 occurrence):
- Line 43: `font-serif` -> `font-display` (main hero headline h1)

**MenuSection.tsx** (3 occurrences):
- Line 105: `font-serif` -> `font-display` (desktop category tab)
- Line 130: `font-serif` -> `font-display` (mobile category tab)
- Line 160: `font-serif` -> `font-display` (menu item name h4)

**MobileActionButtons.astro** (2 occurrences):
- Line 16: `font-serif` -> `font-display` (Phone button)
- Line 29: `font-serif` -> `font-display` (Order button)

**near-grand-canyon.astro** (11 occurrences):
- Lines 16, 36, 53, 94: `font-serif` -> `font-display` (h1 and h2 headings)
- Lines 105, 116, 129, 142, 155: `font-serif` -> `font-display` (h3 item names)
- Lines 167, 178: `font-serif` -> `font-display` (h2 headings)

**directions.astro** (9 occurrences):
- Lines 20, 103, 125, 147, 169, 191, 213, 235, 260: `font-serif` -> `font-display` (h1, h2 headings)

**faq.astro** (2 occurrences):
- Line 13: `font-serif` -> `font-display` (page title h1)
- Line 22: `font-serif` -> `font-display` (question heading h3)

After replacement, run: `grep -r "font-serif" src/ --include="*.tsx" --include="*.astro" --include="*.ts"` — must return zero results.
  </action>
  <verify>
    <automated>cd /Users/moni/Workspace/SpiceGrillBarWeb/spice-grill-bar-web && npm run build 2>&1 | tail -5 && echo "---FONT CHECK---" && grep -rc "font-serif" src/ --include="*.tsx" --include="*.astro" --include="*.ts" 2>/dev/null | grep -v ":0$" || echo "ZERO font-serif remaining"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "font-serif" src/ --include="*.tsx" --include="*.astro" --include="*.ts"` returns zero matches
    - `Header.tsx` contains `font-display` (at least 5 occurrences)
    - `Hero.astro` contains `font-display`
    - `MenuSection.tsx` contains `font-display` (at least 3 occurrences)
    - `near-grand-canyon.astro` contains `font-display` (at least 11 occurrences)
    - `directions.astro` contains `font-display` (at least 9 occurrences)
    - `faq.astro` contains `font-display` (at least 2 occurrences)
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>All font-serif replaced with font-display. Manrope renders for all headings and display text. Zero font-serif references remain in src/. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Final zero-remnant verification sweep and QA</name>
  <files>none (verification only)</files>
  <read_first>
    - src/styles/globals.css (confirm M3 tokens present, no shadcn remnants)
  </read_first>
  <action>
Run comprehensive verification commands to confirm the entire Phase 8 token migration is complete. This is a read-only verification task — no files should be modified unless a defect is found.

**Step 1: Zero-remnant grep sweep**
Run each of these commands and confirm zero matches (excluding globals.css for token definitions):

```bash
# Shadcn token names in components
grep -r "text-foreground\|text-muted-foreground\|bg-popover\|text-popover-foreground\|bg-accent\|text-accent-foreground\|bg-muted\|ring-ring\|ring-offset-background\|bg-card\|text-card-foreground" src/ --include="*.tsx" --include="*.astro" --include="*.ts" | grep -v globals.css

# Brand tokens
grep -r "brand-orange\|brand-green\|brand-gold" src/ --include="*.tsx" --include="*.astro" --include="*.ts"

# Old font
grep -r "font-serif" src/ --include="*.tsx" --include="*.astro" --include="*.ts"

# Hardcoded neutral palette (should be zero in components)
grep -r "neutral-50\|neutral-100\|neutral-200\|neutral-300\|neutral-800\|neutral-900\|neutral-950" src/ --include="*.tsx" --include="*.astro" --include="*.ts"

# Chart tokens
grep -r "chart-" src/ --include="*.tsx" --include="*.astro" --include="*.ts" --include="*.css"
```

If any of these return matches, fix them using the M3 token mappings from Plan 02/03 context.

**Step 2: Build verification**
```bash
npm run build
```
Must complete with zero errors.

**Step 3: Full QA**
```bash
npm run qa
```
Must pass lint, knip, typecheck, AEO audit, build, and Lighthouse CI. This confirms no performance regressions from the token migration.

If QA fails on any check, diagnose and fix. Common issues:
- knip may flag unused imports if components were refactored
- typecheck may flag issues if type annotations reference old patterns
- Lighthouse should be unaffected since this is CSS-only change (no new JS)
  </action>
  <verify>
    <automated>cd /Users/moni/Workspace/SpiceGrillBarWeb/spice-grill-bar-web && npm run build 2>&1 | tail -3 && echo "---SWEEP---" && (grep -r "brand-orange\|brand-green\|brand-gold\|font-serif\|text-foreground\b\|text-muted-foreground\|bg-popover\|bg-accent\|ring-ring\|neutral-50\|neutral-900\|neutral-950" src/ --include="*.tsx" --include="*.astro" --include="*.ts" | grep -v globals.css | head -5) && echo "REMNANT CHECK" || echo "ZERO REMNANTS"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - `grep -r "brand-orange\|brand-green\|brand-gold" src/` returns zero matches
    - `grep -r "font-serif" src/ --include="*.tsx" --include="*.astro" --include="*.ts"` returns zero matches
    - `grep -r "text-foreground" src/ --include="*.tsx" --include="*.astro" | grep -v globals.css` returns zero matches
    - `grep -r "neutral-50\|neutral-900\|neutral-950" src/ --include="*.tsx" --include="*.astro"` returns zero matches
    - `grep -r "chart-" src/ --include="*.tsx" --include="*.astro" --include="*.css"` returns zero matches
    - `npm run qa` exits with code 0 (or if Lighthouse CI is unavailable in this environment, `npm run test:quality` exits with code 0)
  </acceptance_criteria>
  <done>Full codebase verified clean of all legacy tokens (shadcn, brand-*, font-serif, neutral-*, chart-*). Build and QA pass. Phase 8 token migration is complete.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `npm run qa` passes (full quality gate)
- Zero instances of font-serif, brand-*, shadcn token names, neutral-*, chart-* in src/ (excluding globals.css)
- All headings render in Manrope (font-display), all body text in Inter (font-sans)
</verification>

<success_criteria>
- TOKEN-04 fully satisfied: font-display (Manrope) used everywhere headings previously used font-serif (Playfair)
- TOKEN-03 verified complete: zero legacy token remnants across entire codebase
- Full QA passes with no regressions
- Phase 8 token system migration confirmed complete
</success_criteria>

<output>
After completion, create `.planning/phases/08-token-system/08-04-SUMMARY.md`
</output>
