---
phase: 14-speakable-coverage
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/faq.astro
  - scripts/aeo-audit.mjs
autonomous: true
requirements:
  - AEO-12

must_haves:
  truths:
    - "A voice assistant visiting /faq/ receives a short, spoken-friendly intro paragraph as the Speakable target"
    - "The FAQ container div has id='faq-list' so other consumers (AEO-13 phase, page anchors) can reference it"
    - "The SpeakableSpecification JSON-LD block is present in the built /faq/ HTML output"
    - "npm run test:aeo passes (new gate checks for SpeakableSpecification in dist/faq/index.html)"
    - "npm run test:aeo gracefully skips the new gate with console.warn when dist/faq/index.html does not exist"
  artifacts:
    - path: "src/pages/faq.astro"
      provides: "Intro paragraph with speakable-faq-intro class + id on FAQ container + WebPage Speakable schema block"
      contains: "speakable-faq-intro"
    - path: "scripts/aeo-audit.mjs"
      provides: "CI gate verifying SpeakableSpecification in dist/faq/index.html"
      contains: "distFaqPath"
  key_links:
    - from: "src/pages/faq.astro (SpeakableSpecification cssSelector)"
      to: "src/pages/faq.astro (<p class='speakable-faq-intro'>)"
      via: "cssSelector: ['.speakable-faq-intro'] — Google's crawler resolves this at index time"
      pattern: "speakable-faq-intro"
    - from: "scripts/aeo-audit.mjs (new gate)"
      to: "dist/faq/index.html"
      via: "fs.readFileSync + String.includes('SpeakableSpecification')"
      pattern: "distFaqPath"
---

<objective>
Add Speakable coverage to the /faq/ page so voice assistants can extract an authoritative spoken snippet, and add a CI gate that enforces the Speakable schema is present in the built output.

Purpose: AEO-12 — voice assistants need a short, targeted passage on /faq/ to read aloud. Without a SpeakableSpecification, Google may extract arbitrary text that is too long or not location-anchored.

Output:
- `src/pages/faq.astro` — intro paragraph (class: speakable-faq-intro), id="faq-list" on outer div, raw JSON WebPage + SpeakableSpecification schema block after </main>
- `scripts/aeo-audit.mjs` — new FAQ Speakable gate section appended before the closing summary
</objective>

<execution_context>
@/Users/moni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-speakable-coverage/14-CONTEXT.md
@.planning/phases/14-speakable-coverage/14-RESEARCH.md

<interfaces>
<!-- Extracted from existing source files — executor reads these directly. No codebase exploration needed. -->

From src/pages/faq.astro (full current file — 30 lines):
  Line 1-4: frontmatter (imports faqData + Layout)
  Line 6-11: Layout component open with title="FAQ | Spice Grill & Bar"
  Line 12-16: <main> → <div class="max-w-4xl mx-auto"> → <h1> with "Frequently Asked Questions"
  Line 18: <div class="space-y-4">  ← THIS receives id="faq-list"; intro <p> goes BEFORE this line
  Line 19-27: faqData.map() rendering Q&A cards
  Line 28-30: closing </div> </main> </Layout>

From src/pages/directions.astro (existing Speakable block — lines 288-299):
  Raw JSON pattern:
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "WebPage",
        "name": "Directions to Spice Grill & Bar",
        "speakable": {
          "@type": "SpeakableSpecification",
          "cssSelector": [".speakable-heading", ".speakable-lead", ".speakable-exit"]
        }
      }
    </script>

From scripts/aeo-audit.mjs (existing gate pattern — lines 97-126):
  Gate reads dist/index.html once, runs @id gate and FAQPage count gate.
  Skip pattern: if (!fs.existsSync(path)) { console.warn('⚠ ...gate: not found — skipping...'); }
  Success: console.log('✓ ...')
  Failure: console.error('✗ ...'); errors++;
  The new FAQ Speakable gate appends AFTER line 126 (after the existing if/else block closes),
  BEFORE the summary console.log block starting at line 128.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add intro paragraph, id annotation, and Speakable schema to faq.astro</name>
  <files>src/pages/faq.astro</files>
  <read_first>
    - src/pages/faq.astro — read in full before making any edits; confirm current line 18 is exactly `<div class="space-y-4">` with no id attribute
    - src/pages/directions.astro — read lines 288-299 for the exact raw JSON Speakable block format to copy
    - .planning/phases/14-speakable-coverage/14-CONTEXT.md — sections: D-01 (verbatim intro text), D-02, D-03, D-04
    - .planning/phases/14-speakable-coverage/14-RESEARCH.md — sections: "FAQ Intro Paragraph", "FAQ Speakable Block", Pitfall 1 (HTML entity encoding)
  </read_first>
  <action>
    Make three surgical changes to src/pages/faq.astro:

    CHANGE 1 — Add id="faq-list" to the outer FAQ container div.
    The line `<div class="space-y-4">` (currently line 18) becomes:
    `<div id="faq-list" class="space-y-4">`

    CHANGE 2 — Insert intro paragraph between the closing `</h1>` tag and the now-annotated `<div id="faq-list">`.
    The paragraph uses class "speakable-faq-intro text-body-lg text-on-surface-variant mb-8" per D-03.
    The intro text is verbatim per D-01 — use &amp; for the ampersand in "Spice Grill &amp; Bar" because this is raw HTML (not an Astro expression), consistent with directions.astro convention:
      Spice Grill &amp; Bar is an authentic Indian restaurant at I-40 Exit 146 in Ash Fork, Arizona on historic Route 66. This page answers common questions about our hours, menu, vegetarian and vegan options, takeout, and location. Find the answer you need below.

    CHANGE 3 — Append Speakable schema block after </main> and before </Layout>.
    Use raw JSON (not set:html pattern) to match the existing directions.astro Speakable block exactly.
    Schema object fields: @context, @type ("WebPage"), name ("FAQ | Spice Grill & Bar"), speakable (@type: "SpeakableSpecification", cssSelector: [".speakable-faq-intro"]).
    Placement: the schema block goes between line 29 (</main>) and line 30 (</Layout>) in the current file.
    Add HTML comment before the script tag: <!-- Speakable schema markup — targets intro paragraph for voice extraction -->

    Constraints:
    - Do NOT use id="faq-intro" on the paragraph (D-03 forbids it; use class only)
    - Do NOT use set:html={JSON.stringify(...)} for the Speakable block (raw JSON per RESEARCH.md)
    - Do NOT place the schema block inside <main> (D-04 requires after </main>)
    - Do NOT place the schema block after </Layout> (Astro discards content outside Layout)
  </action>
  <verify>
    <automated>cd /Users/moni/Workspace/SpiceGrillBarWeb/spice-grill-bar-web && npm run build 2>&1 | tail -5 && grep -c "speakable-faq-intro" dist/faq/index.html && grep -c "SpeakableSpecification" dist/faq/index.html && grep -c 'id="faq-list"' dist/faq/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "speakable-faq-intro" dist/faq/index.html` returns 2 (once on the <p> class, once in the cssSelector JSON)
    - `grep -c "SpeakableSpecification" dist/faq/index.html` returns 1
    - `grep -c 'id="faq-list"' dist/faq/index.html` returns 1
    - `grep "speakable-faq-intro" src/pages/faq.astro` matches exactly the <p> class attribute (no id="faq-intro" present anywhere in the file)
    - The intro paragraph text in the built HTML contains "I-40 Exit 146 in Ash Fork" and "vegetarian and vegan options"
    - The built HTML does NOT contain `&amp;amp;` (double-encoding) — the ampersand renders as a single `&amp;` in source
    - `npm run build` exits 0 with no Astro build errors or HTML warnings
  </acceptance_criteria>
  <done>faq.astro builds cleanly with intro paragraph, id="faq-list" on container, and SpeakableSpecification in built HTML output</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add FAQ Speakable CI gate to aeo-audit.mjs</name>
  <files>scripts/aeo-audit.mjs</files>
  <read_first>
    - scripts/aeo-audit.mjs — read in full; note the @id + FAQPage gate block (lines 97-126) as the pattern to follow; identify exact line where the new gate inserts (after line 126, before the summary console.log at line 128)
    - .planning/phases/14-speakable-coverage/14-CONTEXT.md — sections: D-07, D-08
    - .planning/phases/14-speakable-coverage/14-RESEARCH.md — sections: "AEO Audit Gate Pattern", Pitfall 2
  </read_first>
  <action>
    Insert a new FAQ Speakable gate block into scripts/aeo-audit.mjs.

    Location: after the closing brace of the existing @id + FAQPage gate block (currently line 126), and before the summary console.log block (currently line 128 "console.log('\n---...")").

    The new gate (per D-07 and D-08):
    1. Declares const distFaqPath = path.join(ROOT_DIR, 'dist/faq/index.html')
    2. Checks fs.existsSync(distFaqPath) — if false, calls console.warn with message: '⚠ FAQ Speakable gate: dist/faq/index.html not found — skipping (run npm run build first for full audit)' and does NOT increment errors
    3. If file exists, reads it with fs.readFileSync(distFaqPath, 'utf-8')
    4. If the content does NOT include 'SpeakableSpecification': calls console.error with '✗ FAQ Speakable gate: SpeakableSpecification not found in dist/faq/index.html'; increments errors++
    5. If the content includes 'SpeakableSpecification': calls console.log with '✓ FAQ Speakable gate: SpeakableSpecification found in dist/faq/index.html'

    Add a single-line comment before the block: // 5. FAQ Speakable gate — verifies SpeakableSpecification present in dist/faq/index.html (AEO-12)

    Constraints per D-08:
    - This gate reads ONLY dist/faq/index.html — do NOT add any gate for dist/directions/index.html
    - The skip branch (file not found) must NOT call errors++ — it only console.warns
    - The gate number comment should be "5." to continue the existing numbered gate sequence (gates 1-4 already exist)
  </action>
  <verify>
    <automated>cd /Users/moni/Workspace/SpiceGrillBarWeb/spice-grill-bar-web && npm run test:aeo 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:aeo` exits 0 (all gates pass, including the new FAQ Speakable gate which either finds dist/faq/index.html from Task 1 build or gracefully warns and skips)
    - If dist/faq/index.html exists (Task 1 build was run): output includes "✓ FAQ Speakable gate: SpeakableSpecification found in dist/faq/index.html"
    - If dist/faq/index.html does NOT exist: output includes "⚠ FAQ Speakable gate: dist/faq/index.html not found — skipping" and exit code is still 0
    - `grep -c "distFaqPath" scripts/aeo-audit.mjs` returns 2 (declaration + existsSync check)
    - `grep "dist/directions" scripts/aeo-audit.mjs` returns nothing (D-08: no directions gate added)
    - Full sequence: `npm run build && npm run test:aeo` exits 0 with "✓ FAQ Speakable gate" in output
  </acceptance_criteria>
  <done>AEO audit CI gate passes for FAQ Speakable; gate gracefully skips when dist is absent; npm run test:aeo green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build output → CI gate | aeo-audit.mjs reads static dist HTML files; no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure | SpeakableSpecification JSON-LD | accept | Schema is intentionally public-facing structured data for crawlers; no PII or sensitive data |
| T-14-02 | Tampering | aeo-audit.mjs CI gate | accept | Script reads only local dist/ files; no network calls; runs in trusted CI environment |
</threat_model>

<verification>
After both tasks complete:

1. `npm run build && npm run test:aeo` — must exit 0 with "✓ FAQ Speakable gate" in output
2. `grep "speakable-faq-intro" dist/faq/index.html` — must return 2 matches
3. `grep 'id="faq-list"' dist/faq/index.html` — must return 1 match
4. `grep "SpeakableSpecification" dist/faq/index.html` — must return 1 match
5. Intro paragraph contains exact text "I-40 Exit 146 in Ash Fork, Arizona on historic Route 66"
6. `npm run lint` — exits 0 (no linting regressions)
</verification>

<success_criteria>
- faq.astro intro paragraph with speakable-faq-intro class is present and contains the D-01 verbatim text
- id="faq-list" is on the outer FAQ container div
- SpeakableSpecification JSON-LD block is in the built /faq/ HTML, targeting .speakable-faq-intro
- aeo-audit.mjs FAQ Speakable gate (gate #5) passes on a fresh build
- All existing aeo-audit.mjs gates continue to pass
- npm run build exits 0 with no warnings
</success_criteria>

<output>
After completion, create `.planning/phases/14-speakable-coverage/14-01-SUMMARY.md`
</output>
