---
phase: 07-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified: []
autonomous: false
requirements:
  - INFRA-04
  - INFRA-06

must_haves:
  truths:
    - "Dark mode toggle produces visible color changes on Header, Sheet, and body backgrounds"
    - "Sheet panel slides in from right with animation, not instant appear"
    - "DropdownMenu fades and zooms in, not instant appear"
    - "MobileActionButtons slide up from bottom on mobile viewport"
  artifacts: []
  key_links:
    - from: "mode-toggle.tsx"
      to: "html.classList dark"
      via: "document.documentElement.classList toggle"
      pattern: "classList.*dark"
---

<objective>
Visually verify that dark mode toggling and component animations work correctly after the TailwindCSS v4 migration. These behaviors cannot be confirmed by automated tests alone.

Purpose: Dark mode (INFRA-04) and animations (INFRA-06) require human visual confirmation that the rendered output behaves correctly, not just that the code compiles.
Output: Confirmed functional dark mode and animations, or identified fixes needed.
</objective>

<execution_context>
@/Users/moni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-infrastructure/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server for visual inspection</name>
  <files></files>
  <read_first>
    src/styles/globals.css
    src/components/ui/sheet.tsx
    src/components/ui/dropdown-menu.tsx
    src/components/MobileActionButtons.astro
    src/components/mode-toggle.tsx
  </read_first>
  <action>
Start the development server:

```bash
npm run dev
```

Verify it starts without errors on http://localhost:4321.

Then prepare the checkpoint information for the user by confirming:
1. The dev server URL is accessible
2. No console errors on startup related to Tailwind or CSS
  </action>
  <verify>
```bash
# Dev server should be running — verify it responds
curl -s -o /dev/null -w "%{http_code}" http://localhost:4321/
# Expected: 200
```
  </verify>
  <acceptance_criteria>
- Dev server starts without errors
- `curl http://localhost:4321/` returns HTTP 200
  </acceptance_criteria>
  <done>Dev server running and serving pages correctly on TailwindCSS v4.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify dark mode and animations visually</name>
  <files></files>
  <action>
Present the following verification checklist to the user. This is a human-verify checkpoint — Claude cannot verify visual/interactive behavior autonomously.

The user must open http://localhost:4321 in their browser and confirm the items listed in how-to-verify below.

If the user reports issues, diagnose and fix:
- Dark mode not toggling: check @custom-variant syntax in globals.css, check mode-toggle.tsx classList logic
- Animations not firing: check @import "tw-animate-css" is present in globals.css, check animation class names match tw-animate-css expectations
- Fonts not loading: check @fontsource-variable imports in Layout.astro, check --font-sans value in @theme inline
  </action>
  <verify>User confirms visual checkpoint by typing "approved".</verify>
  <done>User has visually confirmed dark mode toggle, component animations, and font loading all work correctly after the TailwindCSS v4 migration.</done>
  <what-built>
TailwindCSS v4 migration with dark mode via @custom-variant and animations via tw-animate-css. The build passes and all automated tests pass. This checkpoint confirms the visual/interactive behavior is correct.
  </what-built>
  <how-to-verify>
**Dark Mode Verification (INFRA-04):**

1. Open http://localhost:4321 in your browser
2. Open DevTools -> Elements panel
3. Select the `<html>` element
4. Add class `dark` to the html element
5. VERIFY: Body background changes from light (near-white) to dark (near-black)
6. VERIFY: Header background tint changes appropriately
7. VERIFY: Text colors invert (dark text on light -> light text on dark)
8. Remove the `dark` class — page returns to light mode
9. Click the theme toggle button in the Header — it should toggle dark/light mode
10. Check that the `/faq/` page also toggles correctly

**Animation Verification (INFRA-06):**

11. Resize browser to mobile width (< 768px)
12. VERIFY: MobileActionButtons bar at bottom should have slid up from below on page load (reload to re-trigger)
13. Click the hamburger menu icon in the Header
14. VERIFY: Sheet panel slides in from the right with a smooth transition (NOT instant appear)
15. Close the Sheet — VERIFY it slides back out (NOT instant disappear)
16. On desktop, click the theme toggle dropdown (if it uses DropdownMenu)
17. VERIFY: Menu fades/zooms in smoothly (NOT instant appear)
18. Click away — VERIFY: Menu fades/zooms out smoothly

**Font Verification:**

19. Open DevTools -> Elements -> select any body text
20. Check Computed styles -> font-family — should include "Inter Variable"
21. This confirms the variable font is loading (old "Open Sans" may also appear as fallback)
  </how-to-verify>
  <resume-signal>Type "approved" if dark mode, animations, and fonts all work correctly. If any issue is found, describe what is broken (e.g., "dark mode does not toggle", "Sheet appears instantly without animation").</resume-signal>
</task>

</tasks>

<verification>
1. User confirms dark mode toggle works (light/dark backgrounds change)
2. User confirms Sheet slides in/out with animation
3. User confirms DropdownMenu fades/zooms in/out
4. User confirms MobileActionButtons slides up on mobile
5. User confirms fonts are loading
</verification>

<success_criteria>
- Dark mode: adding class="dark" to html produces visible dark backgrounds on Header, Sheet, and body
- Animations: Sheet, DropdownMenu, and MobileActionButtons all animate (not instant appear/disappear)
- Fonts: Inter Variable and Manrope Variable load in the browser
- User approves visual checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/07-infrastructure/07-02-SUMMARY.md`
</output>
