---
phase: 13-faqpage-schema-compliance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/aeo-audit.mjs
autonomous: true
requirements: [AEO-10]
must_haves:
  truths:
    - "npm run test:aeo fails when dist/index.html has a Question entry count other than 8"
    - "npm run test:aeo passes the FAQPage gate when dist/index.html has exactly 8 Question entries"
    - "The FAQPage gate skips gracefully with a console.warn when dist/index.html is absent (no false failure)"
  artifacts:
    - path: "scripts/aeo-audit.mjs"
      provides: "Post-build gate asserting exactly 8 Question entries in dist/index.html"
      contains: "FAQPage gate"
  key_links:
    - from: "scripts/aeo-audit.mjs"
      to: "dist/index.html"
      via: "fs.readFileSync + regex match on \"@type\":\"Question\""
      pattern: "match\\(/\"@type\":\"Question\"/g\\)"
---

<objective>
Add a permanent automated regression gate to `scripts/aeo-audit.mjs` that verifies the built home page (`dist/index.html`) contains exactly 8 `Question` entries. This locks in the AEO-10 fix from Plan 13-01 so any future change that re-broadens the FAQ schema (or drops a homeFaq index) fails CI.

Purpose: Without this gate, the schema/DOM match achieved in Plan 13-01 could silently regress. The gate is the only automated test covering AEO-10.
Output: 1 modified file — `scripts/aeo-audit.mjs` — shipped as its own `chore:` commit (per D-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-faqpage-schema-compliance/13-CONTEXT.md
@.planning/phases/13-faqpage-schema-compliance/13-RESEARCH.md
@.planning/phases/13-faqpage-schema-compliance/13-PATTERNS.md

<interfaces>
<!-- Existing in scripts/aeo-audit.mjs — reuse, do not redeclare -->
const distIndexPath = path.join(ROOT_DIR, 'dist/index.html');  // line 98, in scope to end of file
let errors;                                                    // module-level counter; gate increments on failure
// Phase 12 @id gate occupies lines 97-114: existsSync guard -> readFileSync -> string check -> errors++ / console.log
// Final summary block at lines 116-123 reads `errors` and process.exit(1) if > 0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FAQPage Question-count gate to aeo-audit.mjs</name>
  <files>scripts/aeo-audit.mjs</files>
  <read_first>
    - scripts/aeo-audit.mjs (full file — specifically the Phase 12 @id gate at lines 97-114 being extended, `distIndexPath` declaration at line 98, the `errors` counter, and the final summary block at lines 116-123)
    - .planning/phases/13-faqpage-schema-compliance/13-PATTERNS.md (Pattern Map — "scripts/aeo-audit.mjs — FAQPage Question count gate" section, including the preferred merged single-block approach)
    - .planning/phases/13-faqpage-schema-compliance/13-RESEARCH.md (Pattern 2 and the AEO Audit Gate code example)
  </read_first>
  <action>
    Merge a FAQPage Question-count gate into the existing Phase 12 `@id` gate block (lines 97-114) so `dist/index.html` is read only once — the preferred single-block approach from 13-PATTERNS.md (D-05).

    In the `else` branch (where `distHtml` is already read via `fs.readFileSync`), after the existing `@id` missing-fragments check, add: `const questionMatches = distHtml.match(/"@type":"Question"/g) || [];` then `if (questionMatches.length !== 8)` → `console.error` with a message reporting the actual count and expected 8, and `errors++`; `else` → `console.log('✓ FAQPage gate: dist/index.html contains exactly 8 Question entries')`.

    In the `if (!fs.existsSync(distIndexPath))` skip branch, add a second `console.warn` line for the FAQPage gate so the skip is visible in output (mirrors the existing `@id` gate warn). The gate must NOT increment `errors` when `dist/index.html` is absent — skip gracefully (same pattern as the Phase 12 @id gate; prevents a false pass-or-fail when `test:aeo` runs before `build`).

    Do not touch the final summary block (lines 116-123) — it already reads `errors` and exits non-zero. Reuse the existing `distIndexPath` constant — do not redeclare it. Commit as a standalone `chore:` commit.
  </action>
  <verify>
    <automated>npm run build && npm run test:aeo 2>&1 | grep -q 'FAQPage gate: dist/index.html contains exactly 8 Question entries' && echo "PASS: gate present and green"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/aeo-audit.mjs` contains `distHtml.match(/"@type":"Question"/g)`
    - `scripts/aeo-audit.mjs` contains a `questionMatches.length !== 8` comparison that does `errors++` on failure
    - `scripts/aeo-audit.mjs` contains a `console.warn` for the FAQPage gate inside the `!fs.existsSync(distIndexPath)` branch
    - `scripts/aeo-audit.mjs` does NOT add a second `const distIndexPath` declaration (reuses line 98)
    - After `npm run build`, `npm run test:aeo` prints `✓ FAQPage gate: dist/index.html contains exactly 8 Question entries` and exits 0
    - Behavior: running `npm run test:aeo` with no `dist/` directory present prints the FAQPage skip `console.warn` and does NOT fail solely because of this gate
  </acceptance_criteria>
  <done>scripts/aeo-audit.mjs has a FAQPage gate that fails CI on any Question count != 8 in dist/index.html and skips gracefully when the build output is absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | No trust boundary is crossed. `aeo-audit.mjs` is a developer-run / CI build-tooling script that reads a build artifact (`dist/index.html`) generated from trusted source. No user input, no network, no auth, no runtime — the script runs only in the local/CI build environment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Tampering | `dist/index.html` (read by the gate) | accept | `dist/` is a generated build artifact, not a network-exposed input. A tampered `dist/` only affects a local audit run; the canonical check runs in CI on a fresh build. No mitigation warranted. |
| T-13-05 | Denial of Service | `aeo-audit.mjs` regex over `dist/index.html` | accept | Single bounded `String.match` over one local file; no user-controlled size or runtime path. Negligible. |

No Spoofing, Repudiation, Information Disclosure, or Elevation-of-Privilege vectors apply — the script performs read-only validation of a local artifact with no identity, secrets, or privileged operations. This phase involves only static build-time tooling changes; no high-severity threats identified.
</threat_model>

<verification>
- `npm run build` succeeds, then `npm run test:aeo` prints the FAQPage gate pass line and exits 0
- `npm run test:aeo` run without a prior build prints the FAQPage skip warning and does not fail on this gate alone
- `scripts/aeo-audit.mjs` reads `dist/index.html` only once (merged block — no duplicate `readFileSync` for the FAQPage gate)
</verification>

<success_criteria>
- A FAQPage gate exists in `scripts/aeo-audit.mjs` that asserts exactly 8 `"@type":"Question"` matches in `dist/index.html`
- The gate increments `errors` (failing `npm run qa`) on any count != 8
- The gate skips with a `console.warn` (no false failure) when `dist/index.html` is absent
- Change ships as a standalone `chore:` commit, separate from the Plan 13-01 schema commit(s)
</success_criteria>

<output>
After completion, create `.planning/phases/13-faqpage-schema-compliance/13-02-SUMMARY.md`
</output>
