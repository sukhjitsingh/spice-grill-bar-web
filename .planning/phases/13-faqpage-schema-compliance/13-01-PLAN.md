---
phase: 13-faqpage-schema-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/Layout.astro
  - src/pages/index.astro
  - src/components/schema/WebSiteSchema.astro
  - src/components/schema/RestaurantSchema.astro
autonomous: true
requirements: [AEO-10]
must_haves:
  truths:
    - "Home page (/) emits a FAQPage JSON-LD block with exactly 8 Question entries matching the 8 visible DOM questions"
    - "Home page (/) no longer emits the global 34-question FAQSchema block"
    - "/faq/ page still emits the full 34-question FAQSchema block"
    - "geo.position meta on every page reads 35.222908;-112.4781558"
    - "RestaurantSchema GeoCoordinates reads latitude 35.222908, longitude -112.4781558"
    - "WebSiteSchema publisher object carries @id https://spicegrillbar66.com/#organization"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Inline 8-question FAQPage JSON-LD derived from homeFaq array"
      contains: "faqPageSchema"
    - path: "src/layouts/Layout.astro"
      provides: "FAQSchema gate scoped to /faq/ only + updated geo.position meta"
      contains: "currentPath.startsWith('/faq')"
    - path: "src/components/schema/WebSiteSchema.astro"
      provides: "publisher object wired to Organization entity via @id"
      contains: "https://spicegrillbar66.com/#organization"
    - path: "src/components/schema/RestaurantSchema.astro"
      provides: "CID-verified GeoCoordinates"
      contains: "35.222908"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/data/faq.json"
      via: "homeFaq array (homeFaqIndices) mapped into faqPageSchema.mainEntity"
      pattern: "homeFaq.map"
    - from: "src/layouts/Layout.astro"
      to: "src/components/schema/FAQSchema.astro"
      via: "currentPath.startsWith('/faq') conditional render"
      pattern: "startsWith\\('/faq'\\)"
---

<objective>
Correct the Google FAQPage policy violation on the home page: the build currently emits a 34-question FAQPage schema on `/` (via the global `FAQSchema` component) while only 8 questions are visible in the DOM. Narrow the `FAQSchema` gate to `/faq/` only and inject an 8-question inline `FAQPage` JSON-LD block on the home page. Bundle two zero-risk follow-ups in the same atomic change: update geo coordinates to CID-verified values (CR-01) and wire the WebSite node to the Organization entity (CR-02).

Purpose: Eliminates an active Google rich-results policy violation (schema must match visible DOM) and completes the Knowledge Graph entity chain Restaurant → Organization ← WebSite.
Output: 4 modified source files, shipped as a single atomic commit so `/` is never without a FAQ schema.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-faqpage-schema-compliance/13-CONTEXT.md
@.planning/phases/13-faqpage-schema-compliance/13-RESEARCH.md
@.planning/phases/13-faqpage-schema-compliance/13-PATTERNS.md
@.planning/phases/12-schema-entity-disambiguation/12-01-SUMMARY.md

<interfaces>
<!-- Already present in src/pages/index.astro frontmatter (lines 21-22) — use directly, no new imports -->
homeFaqIndices = [14, 2, 3, 13, 10, 1, 15, 21]   // 0-indexed into src/data/faq.json (34 entries)
homeFaq = homeFaqIndices.map((i) => faqData[i]).filter((entry) => entry)
// each homeFaq entry shape: { question: string, answer: string }

<!-- Emission pattern used by all 6 schema components (FAQSchema.astro:20 et al.) -->
<script is:inline type="application/ld+json" set:html={JSON.stringify(schemaObj)} />

<!-- @id fragment URL convention (RestaurantSchema.astro:21) -->
'@id': 'https://spicegrillbar66.com/#restaurant'   // existing
'@id': 'https://spicegrillbar66.com/#organization' // to add to WebSiteSchema publisher
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic FAQPage fix — narrow FAQSchema gate + inject 8-question inline FAQPage schema</name>
  <files>src/layouts/Layout.astro, src/pages/index.astro</files>
  <read_first>
    - src/layouts/Layout.astro (current geo.position meta at line 104, FAQSchema gate at line 122)
    - src/pages/index.astro (homeFaq array at lines 21-22, existing static SpeakableSpecification script at lines 57-67 — placement reference)
    - src/components/schema/FAQSchema.astro (the FAQPage schema shape and `is:inline` + `set:html` emission pattern being replicated)
    - .planning/phases/13-faqpage-schema-compliance/13-PATTERNS.md (Pattern Map — "index.astro inline FAQPage JSON-LD injection" and "Layout.astro FAQSchema gate narrowing" sections)
  </read_first>
  <action>
    Two coordinated edits that MUST land together (per D-01, D-02):

    In `src/layouts/Layout.astro` line 122, change the FAQSchema render condition from `{(currentPath === '/' || currentPath.startsWith('/faq')) && <FAQSchema />}` to `{currentPath.startsWith('/faq') && <FAQSchema />}` — remove only the `currentPath === '/' ||` clause. Also update line 104 geo.position meta `content` attribute from `35.2241;-112.4829` to `35.222908;-112.4781558` (semicolon separator — required for the geo.position meta format).

    In `src/pages/index.astro` frontmatter, add a raw JS object `faqPageSchema` (no schema-dts import per D-01) with `'@context': 'https://schema.org'`, `'@type': 'FAQPage'`, and `mainEntity` built by mapping `homeFaq` — each entry becomes `{ '@type': 'Question', name: q.question, acceptedAnswer: { '@type': 'Answer', text: q.answer } }`. Then emit it as `<script is:inline type="application/ld+json" set:html={JSON.stringify(faqPageSchema)} />` placed immediately after the closing `</script>` of the existing SpeakableSpecification block (after line 67, inside the Layout). Do NOT use a bare `<script type="application/ld+json">` — Astro will not interpolate the expression (Pitfall 3). Do NOT hardcode the 8 FAQ strings — derive from `homeFaq` so faq.json stays the source of truth.

    Stage BOTH files and commit them together with prefix `fix:` — no intermediate commit may touch only one file (atomicity window, D-02).
  </action>
  <verify>
    <automated>npm run build && grep -c '"@type":"Question"' dist/index.html | grep -qx 8 && echo "PASS: 8 questions on home" && grep -c '"@type":"Question"' dist/faq/index.html | grep -qx 34 && echo "PASS: 34 questions on /faq/"</automated>
  </verify>
  <acceptance_criteria>
    - `src/layouts/Layout.astro` line 122 region contains `currentPath.startsWith('/faq') && <FAQSchema />` and does NOT contain `currentPath === '/'` adjacent to `<FAQSchema`
    - `src/layouts/Layout.astro` contains `content="35.222908;-112.4781558"` for the `geo.position` meta and no longer contains `35.2241;-112.4829`
    - `src/pages/index.astro` frontmatter contains `faqPageSchema` with `'@type': 'FAQPage'` and `homeFaq.map(`
    - `src/pages/index.astro` contains `<script is:inline type="application/ld+json" set:html={JSON.stringify(faqPageSchema)} />`
    - After `npm run build`: `grep -c '"@type":"Question"' dist/index.html` outputs exactly `8`
    - After `npm run build`: `grep -c '"@type":"Question"' dist/faq/index.html` outputs exactly `34`
    - `git show --stat HEAD` for the commit lists both `src/layouts/Layout.astro` and `src/pages/index.astro`
  </acceptance_criteria>
  <done>Home page emits an 8-question FAQPage schema matching the visible DOM; /faq/ still emits the 34-question FAQSchema; both file changes shipped in one commit.</done>
</task>

<task type="auto">
  <name>Task 2: Wire entity graph + correct coordinates — WebSiteSchema @id and RestaurantSchema GeoCoordinates</name>
  <files>src/components/schema/WebSiteSchema.astro, src/components/schema/RestaurantSchema.astro</files>
  <read_first>
    - src/components/schema/WebSiteSchema.astro (publisher object at lines 12-16)
    - src/components/schema/RestaurantSchema.astro (geo / GeoCoordinates block at lines 78-82; existing `@id` fragment convention at line 21)
    - .planning/phases/12-schema-entity-disambiguation/12-01-SUMMARY.md (Phase 12 @id fragment decisions — `#organization` established on OrganizationSchema)
    - .planning/phases/13-faqpage-schema-compliance/13-PATTERNS.md (Pattern Map — WebSiteSchema and RestaurantSchema sections)
  </read_first>
  <action>
    Two independent value edits (D-03, D-04):

    In `src/components/schema/WebSiteSchema.astro`, add the line `'@id': 'https://spicegrillbar66.com/#organization',` to the existing `publisher` object, immediately after `'@type': 'Organization',` and before `name:` (lines 12-16). This wires the WebSite node to the Organization entity established in Phase 12. No other change to this file.

    In `src/components/schema/RestaurantSchema.astro`, in the `geo` GeoCoordinates block (lines 78-82), replace `latitude: 35.22291449138381,` with `latitude: 35.222908,` and `longitude: -112.47815397255074,` with `longitude: -112.4781558,`. These are the CID-verified coordinates (Google Maps cid=5034425112937519671). Use separate `latitude` / `longitude` properties — no separator character (distinct from the semicolon format used in the Layout.astro meta tag). Surrounding GeoCoordinates structure unchanged.

    These two files may be committed in the same commit as Task 1 or as a separate `fix:` commit — both are acceptable per D-06. They have no atomicity constraint.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build && grep -q '"@id":"https://spicegrillbar66.com/#organization"' dist/index.html && grep -q '"latitude":35.222908' dist/index.html && grep -q '"longitude":-112.4781558' dist/index.html && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/schema/WebSiteSchema.astro` `publisher` object contains `'@id': 'https://spicegrillbar66.com/#organization'`
    - `src/components/schema/RestaurantSchema.astro` `geo` block contains `latitude: 35.222908` and `longitude: -112.4781558`
    - `src/components/schema/RestaurantSchema.astro` no longer contains `35.22291449138381` or `-112.47815397255074`
    - `npm run typecheck` exits 0
    - After `npm run build`: `dist/index.html` contains `"@id":"https://spicegrillbar66.com/#organization"` within the WebSite JSON-LD block
    - After `npm run build`: `dist/index.html` contains `"latitude":35.222908` and `"longitude":-112.4781558`
  </acceptance_criteria>
  <done>WebSite node references the Organization entity via @id; RestaurantSchema carries CID-verified coordinates; typecheck and build pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | No trust boundary is crossed. All inputs are trusted static JSON (`src/data/faq.json`) and hardcoded source values, consumed at build time. No user input, no authentication, no session state, no network calls, no runtime — the site is fully static (Apache hosting). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | `src/data/faq.json` (build-time input to `faqPageSchema`) | accept | Source-controlled file; only modified via PR review or the Gemini reviews GitHub Action (which never touches `faq.json`). RESEARCH.md A1 confirms 8 indices stay valid. No runtime tampering surface. |
| T-13-02 | Information Disclosure | Inline JSON-LD in `dist/index.html` | accept | Schema content (FAQ Q&A, geo coordinates, public profile URLs) is intentionally public SEO/AEO data. Nothing sensitive is exposed. |
| T-13-03 | Denial of Service | Astro build / `JSON.stringify(faqPageSchema)` | accept | Build-time only, fixed 8-entry array, bounded output. No runtime path, no user-triggerable code. |

No Spoofing, Repudiation, or Elevation-of-Privilege vectors apply — there is no auth, no identity, no privileged operation in this phase. This phase involves only static build-time JSON-LD changes; no high-severity threats identified.
</threat_model>

<verification>
- `npm run build` succeeds with no errors
- `grep -c '"@type":"Question"' dist/index.html` outputs `8`
- `grep -c '"@type":"Question"' dist/faq/index.html` outputs `34`
- `dist/index.html` contains `"@id":"https://spicegrillbar66.com/#organization"`, `"latitude":35.222908`, `"longitude":-112.4781558`, and `content="35.222908;-112.4781558"`
- `npm run typecheck` exits 0
- The atomic-fix commit (Task 1) lists both `Layout.astro` and `index.astro` in `git show --stat`
</verification>

<success_criteria>
- Home page `/` emits exactly one FAQPage JSON-LD block with exactly 8 Question entries, matching the 8 visible DOM questions — no global 34-question block
- `/faq/` page still emits the full 34-question FAQSchema block
- geo.position meta and RestaurantSchema GeoCoordinates both reflect CID-verified `35.222908 / -112.4781558`
- WebSiteSchema publisher carries `@id: https://spicegrillbar66.com/#organization`
- FAQPage gate change ships atomically — `/` is never without a FAQ schema in any intermediate commit
</success_criteria>

<output>
After completion, create `.planning/phases/13-faqpage-schema-compliance/13-01-SUMMARY.md`
</output>
