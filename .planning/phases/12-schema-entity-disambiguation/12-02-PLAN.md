---
phase: 12-schema-entity-disambiguation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/Layout.astro
  - scripts/aeo-audit.mjs
autonomous: true
requirements: [AEO-16]

must_haves:
  truths:
    - "AI crawlers can discover /llms.txt via a rel=alternate text/plain link in <head> (D-05: replace rel=help in-place; llms-full.txt link added immediately after)"
    - "AI crawlers can discover /llms-full.txt via a second rel=alternate text/plain link in <head>"
    - "The AEO audit fails the build when rendered JSON-LD is missing the #restaurant or #organization @id fragments (D-03: gate reads dist/index.html after build)"
    - "The AEO audit runs standalone (no prior build) without failing on the missing dist directory (D-04: graceful skip via fs.existsSync guard)"
  artifacts:
    - path: "src/layouts/Layout.astro"
      provides: "AI crawler discovery links for llms.txt and llms-full.txt"
      contains: "rel=\"alternate\" type=\"text/plain\""
    - path: "scripts/aeo-audit.mjs"
      provides: "@id fragment gate on dist/index.html"
      contains: "spicegrillbar66.com/#restaurant"
  key_links:
    - from: "scripts/aeo-audit.mjs"
      to: "dist/index.html"
      via: "fs.readFileSync + literal string search for @id fragments"
      pattern: "dist/index\\.html"
---

<objective>
Wire AI crawler discovery of the plain-text site content and add a CI gate that enforces the Phase 12 schema `@id` fragments survive into build output.

Purpose: `rel="help"` is the wrong relation for plain-text content mirrors — AI crawlers expect `rel="alternate" type="text/plain"` to discover `llms.txt`/`llms-full.txt`. Separately, the `@id` disambiguation work (Plan 01) needs a regression gate: the AEO audit must fail if a future change strips the `#restaurant`/`#organization` fragments from rendered JSON-LD.
Output: Updated `Layout.astro` `<head>` (corrected + added discovery links) and `aeo-audit.mjs` (new `@id` fragment gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-schema-entity-disambiguation/12-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- scripts/aeo-audit.mjs current structure (read the file for exact lines): -->
<!--   - `fs` and `path` already imported (lines 1-2) -->
<!--   - ROOT_DIR = path.resolve(__dirname, '..') already defined (line 6) -->
<!--   - `let errors = 0;` shared counter (line 11) -->
<!--   - Existing gates increment `errors++` on failure (do NOT throw) -->
<!--   - File ends with: if (errors > 0) { process.exit(1) } else { process.exit(0) } at lines 97-104 -->
<!--   - Pattern for a gate: build a path with path.join(ROOT_DIR, ...), guard with fs.existsSync, read with fs.readFileSync, check with .includes(...) -->
<!-- Layout.astro <head> current state (read the file): line 69 = manifest link, line 70 = <link rel="help" href="/llms.txt" /> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix llms.txt link relation and add llms-full.txt discovery link</name>
  <files>src/layouts/Layout.astro</files>
  <read_first>
    - src/layouts/Layout.astro (current state — line 69 is `<link rel="manifest" href="/manifest.json" />`, line 70 is `<link rel="help" href="/llms.txt" />`)
    - .planning/phases/12-schema-entity-disambiguation/12-CONTEXT.md (D-05 locks in-place replacement at line 70 — do NOT move to end of <head>)
  </read_first>
  <action>
    Per D-05, replace the line 70 element `<link rel="help" href="/llms.txt" />` in place (its current position, immediately after the manifest link) with `<link rel="alternate" type="text/plain" href="/llms.txt" />`. Immediately after that line, add a second element on its own line: `<link rel="alternate" type="text/plain" href="/llms-full.txt" />`. Do NOT relocate either link to the end of `<head>` and do NOT alter the manifest link or any surrounding meta tags.
  </action>
  <verify>
    <automated>grep -c 'rel="alternate" type="text/plain"' src/layouts/Layout.astro</automated>
  </verify>
  <acceptance_criteria>
    - `Layout.astro` contains `<link rel="alternate" type="text/plain" href="/llms.txt" />`
    - `Layout.astro` contains `<link rel="alternate" type="text/plain" href="/llms-full.txt" />`
    - `Layout.astro` no longer contains `rel="help"` (`grep -c 'rel="help"' src/layouts/Layout.astro` returns 0)
    - Both new links sit immediately after `<link rel="manifest" href="/manifest.json" />` (in-place per D-05, not moved to end of head)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Layout.astro `<head>` exposes both `/llms.txt` and `/llms-full.txt` via `rel="alternate" type="text/plain"` links in their original position; no `rel="help"` remains.</done>
</task>

<task type="auto">
  <name>Task 2: Add @id fragment gate to aeo-audit.mjs</name>
  <files>scripts/aeo-audit.mjs</files>
  <read_first>
    - scripts/aeo-audit.mjs (current state — ~104 lines; `fs`/`path` imported, `ROOT_DIR` defined line 6, `let errors = 0` line 11, existing gates increment `errors++`, final summary block at lines 97-104 with `process.exit`)
    - .planning/phases/12-schema-entity-disambiguation/12-CONTEXT.md (D-03 and D-04 lock the gate behavior; "Specific Ideas" lists the exact literal search strings)
  </read_first>
  <action>
    Add a new gate section to `aeo-audit.mjs` immediately before the final summary block (the `console.log('\n---...')` / `if (errors > 0)` block at the end). Per D-03, the gate computes the dist path as `path.join(ROOT_DIR, 'dist/index.html')`, and per D-04 it must gracefully skip with a `console.warn` (not increment `errors`) when that file does not exist — `npm run test:aeo` can run standalone without a prior build. When the file DOES exist, read it with `fs.readFileSync(..., 'utf-8')` and check for both literal strings (JSON.stringify output — no spaces around the colon): `"@id":"https://spicegrillbar66.com/#restaurant"` and `"@id":"https://spicegrillbar66.com/#organization"`. If either literal string is absent, `console.error` a descriptive message naming the missing fragment(s) and increment the shared `errors` counter (do NOT throw). If both are present, `console.log` a success line. Follow the existing gate style (the llms.txt / robots.txt gates above are the pattern reference).
  </action>
  <verify>
    <automated>npm run build && npm run test:aeo</automated>
  </verify>
  <acceptance_criteria>
    - `aeo-audit.mjs` contains the literal search string `"@id":"https://spicegrillbar66.com/#restaurant"`
    - `aeo-audit.mjs` contains the literal search string `"@id":"https://spicegrillbar66.com/#organization"`
    - `aeo-audit.mjs` references `dist/index.html` and guards the read with `fs.existsSync`
    - Running `node scripts/aeo-audit.mjs` with no `dist/` directory present prints a warning and does NOT increment `errors` for the @id gate (gate is skipped, not failed)
    - After `npm run build` (with Plan 01 changes shipped), `npm run test:aeo` exits 0 — the @id gate finds both fragments in `dist/index.html`
    - The new gate code increments the shared `errors` counter on failure rather than throwing
  </acceptance_criteria>
  <done>aeo-audit.mjs enforces that built JSON-LD in `dist/index.html` carries both the `#restaurant` and `#organization` @id fragments, while skipping gracefully when no build output exists.</done>
</task>

</tasks>

<verification>
- `grep -c 'rel="help"' src/layouts/Layout.astro` returns 0; `grep -c 'rel="alternate" type="text/plain"' src/layouts/Layout.astro` returns 2.
- `node scripts/aeo-audit.mjs` run with no `dist/` directory prints a skip warning for the @id gate and does not fail on it.
- `npm run build && npm run test:aeo` exits 0 once Plan 01's schema `@id` changes are in the source — the gate confirms both fragments in `dist/index.html`.
- `npm run qa` (build → test:quality → test:lhci) passes — Lighthouse SEO score does not degrade.
</verification>

<success_criteria>
- Layout.astro `<head>` contains `<link rel="alternate" type="text/plain" href="/llms.txt" />` (not `rel="help"`).
- Layout.astro `<head>` contains a second `<link rel="alternate" type="text/plain" href="/llms-full.txt" />`.
- aeo-audit.mjs has an `@id` fragment gate reading `dist/index.html` that fails the audit when fragments are missing and skips gracefully when the file is absent.
</success_criteria>

<output>
After completion, create `.planning/phases/12-schema-entity-disambiguation/12-02-SUMMARY.md`
</output>
