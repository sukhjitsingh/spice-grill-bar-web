---
phase: 09-visual-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/globals.css
  - src/layouts/Layout.astro
  - package.json
autonomous: true
requirements:
  - VISUAL-01
  - VISUAL-04
  - VISUAL-05

must_haves:
  truths:
    - "Typography utility classes text-display-lg through text-label-sm are available in CSS"
    - "Old font packages @fontsource/open-sans and @fontsource/playfair-display are uninstalled"
    - "Layout.astro only imports Manrope Variable and Inter Variable fonts"
    - ".glass and .glass-card already use @utility directive syntax (VISUAL-05 pre-satisfied)"
  artifacts:
    - path: "src/styles/globals.css"
      provides: "7 @utility typography classes"
      contains: "@utility text-display-lg"
    - path: "src/layouts/Layout.astro"
      provides: "Clean font imports"
    - path: "package.json"
      provides: "No old font packages"
  key_links:
    - from: "src/styles/globals.css"
      to: "all components"
      via: "@utility classes available globally"
      pattern: "@utility text-display-lg"
---

<objective>
Define the 7-level editorial typography scale as CSS @utility classes and remove old font packages.

Purpose: Every subsequent plan depends on these typography utilities existing. This is the foundational change that enables all component reskinning.
Output: globals.css with 7 @utility typography classes; Layout.astro with clean font imports; old font packages removed from package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-visual-redesign/09-CONTEXT.md
@.planning/phases/09-visual-redesign/09-UI-SPEC.md
@.planning/phases/09-visual-redesign/09-RESEARCH.md
@src/styles/globals.css
@src/layouts/Layout.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 7 editorial typography @utility classes to globals.css</name>
  <files>src/styles/globals.css</files>
  <read_first>
    - src/styles/globals.css (current state — find the existing @utility blocks to place new ones after)
    - .planning/phases/09-visual-redesign/09-UI-SPEC.md (typography scale definitions — lines 59-90)
  </read_first>
  <action>
Add 7 `@utility` blocks to `src/styles/globals.css` AFTER the existing `@utility glass-card` block (around line 261). Per D-01/D-02/D-03 and the UI-SPEC typography table:

```css
/* ============================================================
   Editorial Typography Scale (VISUAL-04 / D-01/D-02/D-03)
   7 levels: display-lg → label-sm
   Components use these instead of raw Tailwind size classes.
   ============================================================ */

@utility text-display-lg {
  font-family: var(--font-display);
  font-size: 3.5rem;
  font-weight: 500;
  line-height: 1.1;
  letter-spacing: -0.02em;
}

@utility text-display-md {
  font-family: var(--font-display);
  font-size: 2.5rem;
  font-weight: 500;
  line-height: 1.15;
  letter-spacing: -0.015em;
}

@utility text-heading-lg {
  font-family: var(--font-display);
  font-size: 1.875rem;
  font-weight: 600;
  line-height: 1.2;
  letter-spacing: -0.01em;
}

@utility text-heading-md {
  font-family: var(--font-display);
  font-size: 1.5rem;
  font-weight: 600;
  line-height: 1.25;
  letter-spacing: -0.005em;
}

@utility text-body-lg {
  font-family: var(--font-sans);
  font-size: 1.125rem;
  font-weight: 400;
  line-height: 1.6;
  letter-spacing: 0em;
}

@utility text-body-md {
  font-family: var(--font-sans);
  font-size: 1rem;
  font-weight: 400;
  line-height: 1.5;
  letter-spacing: 0em;
}

@utility text-label-sm {
  font-family: var(--font-sans);
  font-size: 0.75rem;
  font-weight: 600;
  line-height: 1.4;
  letter-spacing: 0.06em;
}
```

Do NOT modify any existing utilities or token values. Only append new @utility blocks.
  </action>
  <verify>
    <automated>grep -c '@utility text-display-lg\|@utility text-display-md\|@utility text-heading-lg\|@utility text-heading-md\|@utility text-body-lg\|@utility text-body-md\|@utility text-label-sm' src/styles/globals.css | grep -q '7'</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains `@utility text-display-lg` with `font-size: 3.5rem` and `font-weight: 500`
    - globals.css contains `@utility text-display-md` with `font-size: 2.5rem`
    - globals.css contains `@utility text-heading-lg` with `font-size: 1.875rem` and `font-weight: 600`
    - globals.css contains `@utility text-heading-md` with `font-size: 1.5rem`
    - globals.css contains `@utility text-body-lg` with `font-size: 1.125rem`
    - globals.css contains `@utility text-body-md` with `font-size: 1rem`
    - globals.css contains `@utility text-label-sm` with `font-size: 0.75rem` and `letter-spacing: 0.06em`
    - All 7 utilities use `var(--font-display)` or `var(--font-sans)` per the UI-SPEC table
    - Existing @utility blocks (glass, glass-card, hero-gradient, cta-gradient) are unchanged
  </acceptance_criteria>
  <done>7 editorial typography @utility classes exist in globals.css with exact values from UI-SPEC</done>
</task>

<task type="auto">
  <name>Task 2: Remove old font packages and clean Layout.astro imports</name>
  <files>src/layouts/Layout.astro, package.json</files>
  <read_first>
    - src/layouts/Layout.astro (current font imports — lines 1-10)
    - package.json (confirm @fontsource/open-sans and @fontsource/playfair-display are listed)
  </read_first>
  <action>
Per D-04 and VISUAL-01:

1. In `src/layouts/Layout.astro`, DELETE these 6 import lines:
```
import '@fontsource/open-sans/400.css';
import '@fontsource/open-sans/500.css';
import '@fontsource/open-sans/700.css';
import '@fontsource/playfair-display/400.css';
import '@fontsource/playfair-display/500.css';
import '@fontsource/playfair-display/700.css';
```

KEEP these 2 imports:
```
import '@fontsource-variable/manrope/wght.css';
import '@fontsource-variable/inter/wght.css';
```

2. Run `npm uninstall @fontsource/open-sans @fontsource/playfair-display` to remove packages from package.json and node_modules.

3. Verify build succeeds: `npm run build`
  </action>
  <verify>
    <automated>grep -c 'fontsource/open-sans\|fontsource/playfair-display' src/layouts/Layout.astro | grep -q '0' && grep -c 'fontsource-variable/manrope\|fontsource-variable/inter' src/layouts/Layout.astro | grep -q '2' && ! grep -q '@fontsource/open-sans\|@fontsource/playfair-display' package.json && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Layout.astro contains NO lines with `@fontsource/open-sans` or `@fontsource/playfair-display`
    - Layout.astro contains `import '@fontsource-variable/manrope/wght.css'`
    - Layout.astro contains `import '@fontsource-variable/inter/wght.css'`
    - package.json does NOT contain `@fontsource/open-sans` or `@fontsource/playfair-display`
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>Old font packages removed, only Manrope Variable and Inter Variable remain, build passes</done>
</task>

</tasks>

<verification>
- `grep '@utility text-display-lg' src/styles/globals.css` returns a match
- `grep '@fontsource/open-sans' src/layouts/Layout.astro` returns no matches
- `grep '@fontsource/playfair-display' package.json` returns no matches
- `npm run build` completes successfully
</verification>

<success_criteria>
- 7 editorial typography @utility classes defined in globals.css with exact values from UI-SPEC
- @fontsource/open-sans and @fontsource/playfair-display removed from project
- Layout.astro imports only Manrope Variable and Inter Variable
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/09-visual-redesign/09-01-SUMMARY.md`
</output>
