---
phase: 08-token-system
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Hero.astro
  - src/components/ReviewsSection.astro
  - src/pages/near-grand-canyon.astro
  - src/components/MobileActionButtons.astro
autonomous: true
requirements: [TOKEN-01, TOKEN-02, TOKEN-03, TOKEN-04, TOKEN-05]
gap_closure: true

must_haves:
  truths:
    - "Zero zinc- classes remain in Hero.astro, ReviewsSection.astro, and near-grand-canyon.astro"
    - "Zero orange- classes remain in Hero.astro and ReviewsSection.astro (orange used only via primary-container tokens)"
    - "Zero dark: color override classes remain in Hero.astro, ReviewsSection.astro, near-grand-canyon.astro, and MobileActionButtons.astro"
    - "ReviewsSection review cards use surface tokens and no backdrop-blur (glass budget compliance)"
    - "MobileActionButtons glass container uses only token-driven styles with no hardcoded black/white fallbacks"
  artifacts:
    - path: "src/components/Hero.astro"
      provides: "Hero section with M3 token classes"
      contains: "bg-surface"
    - path: "src/components/ReviewsSection.astro"
      provides: "Review cards with M3 token classes"
      contains: "bg-surface-container"
    - path: "src/pages/near-grand-canyon.astro"
      provides: "Dish card with M3 token classes"
      contains: "bg-surface-container border-outline-variant"
    - path: "src/components/MobileActionButtons.astro"
      provides: "Glass container with token-driven styles"
  key_links:
    - from: "src/components/Hero.astro"
      to: "globals.css @theme inline"
      via: "Tailwind utility classes bg-surface, text-on-surface, text-on-surface-variant"
      pattern: "bg-surface|text-on-surface"
    - from: "src/components/ReviewsSection.astro"
      to: "globals.css @theme inline"
      via: "Tailwind utility classes bg-surface-container, text-on-surface, text-primary-container"
      pattern: "bg-surface-container|text-on-surface"
---

<objective>
Close 2 verification gaps blocking TOKEN-03 completion: migrate remaining hardcoded zinc-/orange-/dark: color classes in Hero.astro, ReviewsSection.astro, near-grand-canyon.astro, and MobileActionButtons.astro to M3 token utility classes.

Purpose: TOKEN-03 requires all components to use M3 semantic tokens via the hybrid @theme inline pattern. The verification found 4 files with unmigrated hardcoded color classes that bypass the token system.
Output: All 4 files fully migrated to M3 tokens with zero zinc-, orange-, or dark: color overrides remaining.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-token-system/08-VERIFICATION.md
@.planning/phases/08-token-system/08-CONTEXT.md
@src/styles/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Hero.astro and ReviewsSection.astro to M3 tokens</name>
  <files>src/components/Hero.astro, src/components/ReviewsSection.astro</files>
  <read_first>
    src/components/Hero.astro
    src/components/ReviewsSection.astro
    src/styles/globals.css
    src/components/Footer.astro (reference: already-migrated component pattern)
  </read_first>
  <action>
    **Hero.astro — 4 replacements:**

    1. Line 10 section background: Replace `bg-zinc-50 dark:bg-black` with `bg-surface`
    2. Line 38 EST.2024 badge: Replace `border-zinc-200 dark:border-white/20 bg-white/50 dark:bg-white/5 text-orange-700 dark:text-orange-300` with `border-outline-variant/30 bg-surface-container-high/50 text-primary-container`
    3. Line 43 h1 text: Replace `text-zinc-900 dark:text-white` with `text-on-surface`
    4. Line 49 body paragraph: Replace `text-zinc-600 dark:text-zinc-400` with `text-on-surface-variant`

    Also on line 27 gradient overlay: Replace `from-white via-white/10 to-transparent dark:from-black dark:via-black/40 dark:to-transparent` with `from-surface via-surface/10 to-transparent` (surface token auto-adapts in dark mode, no dark: prefix needed).

    **ReviewsSection.astro — full review card interior migration (lines 57-109):**

    1. Line 57 card container: Replace `bg-white/80 dark:bg-zinc-900/80 backdrop-blur-xl border border-zinc-200 dark:border-white/10 shadow-lg shadow-zinc-200/50 dark:shadow-black/20 hover:border-orange-500/50 dark:hover:border-orange-500/40 hover:shadow-xl hover:shadow-orange-500/10` with `bg-surface-container/80 border border-outline-variant shadow-lg hover:border-primary-container/50 hover:shadow-xl`. Remove `backdrop-blur-xl` entirely (violates glass budget per D-15 — cards use tonal surface treatment, no blur).

    2. Line 59 Quote icon: Replace `text-orange-500/15 dark:text-orange-500/10 group-hover:text-orange-500/25 dark:group-hover:text-orange-500/20` with `text-primary-container/15 group-hover:text-primary-container/25`

    3. Line 63 gradient overlay: Replace `from-orange-500/0 to-yellow-500/0 group-hover:from-orange-500/5 group-hover:to-yellow-500/5` with `from-primary-container/0 to-secondary-container/0 group-hover:from-primary-container/5 group-hover:to-secondary-container/5`

    4. Line 69 avatar border: Replace `border-2 border-white/30 dark:border-white/20` with `border-2 border-outline-variant/30`

    5. Line 74 author name: Replace `text-zinc-900 dark:text-zinc-100` with `text-on-surface`

    6. Line 77 review date: Replace `text-zinc-500 dark:text-zinc-400` with `text-on-surface-variant`

    7. Lines 85-86 source badges — these are intentional brand colors (Google blue, Yelp red) for platform identification. Leave the blue-/red- classes as-is; they are NOT part of the zinc-/orange- migration scope.

    8. Line 96 star rating: Replace `fill-orange-500 text-orange-500` with `fill-primary-container text-primary-container`. Replace `text-zinc-300 dark:text-zinc-600` (empty stars) with `text-outline-variant`

    9. Line 104 review text: Replace `text-zinc-700 dark:text-zinc-200` with `text-on-surface-variant`

    10. Line 109 bottom fade gradient: Replace `from-white/90 dark:from-zinc-900/90` with `from-surface-container/90`
  </action>
  <verify>
    <automated>grep -n "zinc-\|orange-\|dark:bg-\|dark:text-\|dark:border-\|dark:shadow-\|dark:from-\|dark:via-" src/components/Hero.astro src/components/ReviewsSection.astro; echo "EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "zinc-" src/components/Hero.astro` returns 0
    - `grep -c "orange-" src/components/Hero.astro` returns 0
    - `grep -c "dark:bg-\|dark:text-\|dark:border-\|dark:shadow-\|dark:from-\|dark:via-\|dark:hover:" src/components/Hero.astro` returns 0
    - `grep -c "zinc-" src/components/ReviewsSection.astro` returns 0
    - `grep -c "orange-" src/components/ReviewsSection.astro` returns 0
    - `grep -c "dark:bg-\|dark:text-\|dark:border-\|dark:shadow-\|dark:from-" src/components/ReviewsSection.astro` returns 0 (excluding dark:opacity- which is acceptable for image overlay)
    - `grep "backdrop-blur" src/components/ReviewsSection.astro` returns 0 matches (glass budget compliance)
    - `grep "bg-surface" src/components/Hero.astro` returns at least 1 match
    - `grep "text-on-surface" src/components/Hero.astro` returns at least 1 match
    - `grep "bg-surface-container" src/components/ReviewsSection.astro` returns at least 1 match
    - `grep "text-primary-container" src/components/ReviewsSection.astro` returns at least 1 match
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>Hero.astro and ReviewsSection.astro contain zero zinc-, zero orange-, and zero dark: color override classes. All colors reference M3 tokens. Review cards have no backdrop-blur. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate near-grand-canyon.astro dish card and MobileActionButtons.astro glass container</name>
  <files>src/pages/near-grand-canyon.astro, src/components/MobileActionButtons.astro</files>
  <read_first>
    src/pages/near-grand-canyon.astro
    src/components/MobileActionButtons.astro
    src/styles/globals.css
  </read_first>
  <action>
    **near-grand-canyon.astro — 1 replacement:**

    Line 136: Replace `bg-white dark:bg-zinc-900/50 border-zinc-200 dark:border-white/10` with `bg-surface-container border-outline-variant`.

    **MobileActionButtons.astro — 2 replacements:**

    1. Line 9 glass container: Remove `dark:shadow-black/50` (the `shadow-2xl shadow-black/20` is acceptable as a base shadow; the `--shadow-color` token from globals.css handles tinting). Replace `border border-white/20 dark:border-white/10` with `border border-outline-variant/20` (glass utility already provides its own border styling via `--glass-border`, but if the explicit border is needed for this component, use the outline-variant token).

    2. Line 12 background fallback layer: Replace the entire `<div class="absolute inset-0 bg-white/60 dark:bg-black/60 backdrop-blur-xl -z-10"></div>` — this is a redundant blur+background layer behind the `.glass` parent which already provides `backdrop-blur: 32px` and `--glass-bg`. Remove this div entirely since the parent `.glass` class handles both the blur and the background. If removal causes visual regression (unlikely since glass already covers this), replace with `<div class="absolute inset-0 bg-surface/60 -z-10"></div>` (no backdrop-blur, just a tinted surface fallback).
  </action>
  <verify>
    <automated>grep -n "zinc-\|dark:bg-\|dark:shadow-\|dark:border-\|bg-white\|bg-black" src/pages/near-grand-canyon.astro src/components/MobileActionButtons.astro | grep -v "node_modules"; echo "EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "zinc-" src/pages/near-grand-canyon.astro` returns 0
    - `grep -c "dark:bg-\|dark:border-" src/pages/near-grand-canyon.astro` returns 0
    - `grep -c "bg-white\|bg-zinc-900\|dark:bg-" src/pages/near-grand-canyon.astro` returns 0
    - `grep -c "dark:shadow-black\|dark:border-white\|dark:bg-black\|bg-white/60" src/components/MobileActionButtons.astro` returns 0
    - `grep -c "dark:" src/components/MobileActionButtons.astro` returns 0
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>near-grand-canyon.astro dish card uses M3 surface/outline tokens. MobileActionButtons.astro glass container has zero dark: overrides and zero hardcoded black/white fallbacks. Build passes.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full verification sweep:

1. `grep -rn "zinc-\|orange-" src/components/Hero.astro src/components/ReviewsSection.astro src/pages/near-grand-canyon.astro src/components/MobileActionButtons.astro` — must return zero matches
2. `grep -rn "dark:bg-\|dark:text-\|dark:border-\|dark:shadow-\|dark:from-\|dark:via-" src/components/Hero.astro src/components/ReviewsSection.astro src/pages/near-grand-canyon.astro src/components/MobileActionButtons.astro` — must return zero matches (exception: dark:opacity- on Hero image is acceptable)
3. `grep -rn "backdrop-blur" src/components/ReviewsSection.astro` — must return zero matches (glass budget)
4. `npm run build` — must complete without errors
5. `npm run lint` — must pass
</verification>

<success_criteria>
- All 4 files contain zero hardcoded zinc-, orange-, or dark: color override classes
- ReviewsSection review cards use tonal surface treatment (no backdrop-blur)
- MobileActionButtons glass container is fully token-driven
- Site builds successfully with no CSS parse errors
- TOKEN-03 verification gap closed: all components in Plan 02/03 scope now use M3 tokens
</success_criteria>

<output>
After completion, create `.planning/phases/08-token-system/08-05-SUMMARY.md`
</output>
