---
phase: 15-voice-directions-content-polish
plan: 03
type: execute
wave: 2
depends_on:
  - "15-01"
  - "15-02"
files_modified:
  - scripts/aeo-audit.mjs
autonomous: true
requirements:
  - AEO-14
  - AEO-15

must_haves:
  truths:
    - "CI catches HowTo schema regressions on directions page via automated gate"
    - "Full QA suite passes: build, lint, typecheck, AEO audit (all 6 gates), Lighthouse CI"
    - "AEO gate skips gracefully when dist/directions/index.html is absent"
    - "AEO gate increments errors counter and prints failure message when HowTo schema is missing from built output"
  artifacts:
    - path: "scripts/aeo-audit.mjs"
      provides: "Section 6 HowTo gate that checks dist/directions/index.html for '@type':'HowTo'"
      contains: 'dist/directions/index.html'
  key_links:
    - from: "scripts/aeo-audit.mjs section 6"
      to: "dist/directions/index.html"
      via: "fs.existsSync + fs.readFileSync + .includes"
      pattern: '"@type":"HowTo"'
---

<objective>
Add Section 6 to scripts/aeo-audit.mjs — a CI gate that verifies HowTo schema is present in the built directions page output. Then run the full QA suite to confirm all Phase 15 changes (Plans 01 and 02) pass CI end-to-end.

Purpose: Without an automated gate, a future editor could accidentally remove the HowTo block without CI catching it. The gate closes the monitoring gap for AEO-14. Running `npm run qa` at the end of this plan proves both the HowTo schema (AEO-14) and the FAQ description change (AEO-15) ship green.

Output: aeo-audit.mjs with 6 gates (was 5). `npm run qa` exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/15-voice-directions-content-polish/15-CONTEXT.md
@.planning/phases/15-voice-directions-content-polish/15-01-SUMMARY.md
@.planning/phases/15-voice-directions-content-polish/15-02-SUMMARY.md
</context>

<interfaces>
<!-- Current end of aeo-audit.mjs — new section 6 inserts between line 142 and line 144 -->

From scripts/aeo-audit.mjs lines 128-151 (current state):
  // 5. FAQ Speakable gate — verifies SpeakableSpecification present in dist/faq/index.html (AEO-12)
  const distFaqPath = path.join(ROOT_DIR, 'dist/faq/index.html');
  if (!fs.existsSync(distFaqPath)) {
    console.warn(
      '⚠ FAQ Speakable gate: dist/faq/index.html not found — skipping (run npm run build first for full audit)'
    );
  } else {
    const distFaqHtml = fs.readFileSync(distFaqPath, 'utf-8');
    if (!distFaqHtml.includes('SpeakableSpecification')) {
      console.error('✗ FAQ Speakable gate: SpeakableSpecification not found in dist/faq/index.html');
      errors++;
    } else {
      console.log('✓ FAQ Speakable gate: SpeakableSpecification found in dist/faq/index.html');
    }
  }
                                   ← INSERT SECTION 6 HERE (line 143)
  console.log('\n---------------------------------------------------');
  if (errors > 0) {
    console.error(`❌ AEO Audit Failed with ${errors} errors.`);
    process.exit(1);
  } else {
    console.log('✅ AEO Audit Passed! Content is optimized for Voice & AI.');
    process.exit(0);
  }

Existing gate pattern (replicate exactly for section 6):
  const distXxxPath = path.join(ROOT_DIR, 'dist/xxx/index.html');
  if (!fs.existsSync(distXxxPath)) {
    console.warn('⚠ XXX gate: dist/xxx/index.html not found — skipping (run npm run build first for full audit)');
  } else {
    const distXxxHtml = fs.readFileSync(distXxxPath, 'utf-8');
    if (!distXxxHtml.includes('SEARCH_STRING')) {
      console.error('✗ XXX gate: message');
      errors++;
    } else {
      console.log('✓ XXX gate: message');
    }
  }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Insert Section 6 HowTo gate into aeo-audit.mjs</name>
  <read_first>
    scripts/aeo-audit.mjs
  </read_first>
  <files>scripts/aeo-audit.mjs</files>
  <action>
    In scripts/aeo-audit.mjs, insert a new section 6 gate block between the closing brace of section 5 (after line 142, the last `}` of the FAQ Speakable gate) and the final summary block that starts with `console.log('\n---...`.

    The exact block to insert (per D-07):

    Section comment: `// 6. HowTo schema gate — verifies HowTo present in dist/directions/index.html (AEO-14)`
    Variable name: `distDirectionsPath` 
    Path: `path.join(ROOT_DIR, 'dist/directions/index.html')`
    Skip warning (when file absent): `'⚠ HowTo gate: dist/directions/index.html not found — skipping (run npm run build first for full audit)'`
    Read variable: `distDirectionsHtml`
    Search string: `'"@type":"HowTo"'` — no space between colon and value (matches Astro's minified JSON-LD output)
    Failure message: `'✗ HowTo gate: HowTo schema not found in dist/directions/index.html'`
    Success message: `'✓ HowTo gate: HowTo schema found in dist/directions/index.html'`

    Follow the identical code structure as sections 4 and 5:
    - fs.existsSync guard → console.warn (skip path)
    - fs.readFileSync into a new local variable
    - .includes check → errors++ on failure, console.log on success

    Do NOT modify any other section (1 through 5) or the final summary block.
  </action>
  <verify>
    <automated>grep -c 'distDirectionsPath' /Users/moni/Workspace/SpiceGrillBarWeb/spice-grill-bar-web/scripts/aeo-audit.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'distDirectionsPath' scripts/aeo-audit.mjs` returns 2 (const declaration + existsSync call)
    - `grep -c '"@type":"HowTo"' scripts/aeo-audit.mjs` returns 1 (the search string in the .includes check — note: no space)
    - `grep -c 'dist/directions/index.html' scripts/aeo-audit.mjs` returns at least 2 (path declaration + warn message)
    - `grep -c 'AEO-14' scripts/aeo-audit.mjs` returns 1 (section comment references AEO-14)
    - `grep -c 'HowTo gate' scripts/aeo-audit.mjs` returns 3 (skip warn, fail error, success log)
    - The final summary block (`console.log('\n---`) still exists and is unchanged (gate inserted before it, not replacing it)
    - All 5 existing sections (1-5) are unchanged: `grep -c '// 5. FAQ Speakable gate' scripts/aeo-audit.mjs` returns 1
  </acceptance_criteria>
  <done>scripts/aeo-audit.mjs has 6 sections. Section 6 checks dist/directions/index.html for the string `"@type":"HowTo"` using the same existsSync-guard pattern as sections 4 and 5.</done>
</task>

<task type="auto">
  <name>Task 2: Run full QA suite to verify all Phase 15 changes pass CI</name>
  <read_first>
    scripts/aeo-audit.mjs
  </read_first>
  <files></files>
  <action>
    Run the full QA suite from the project root to verify all Phase 15 changes (HowTo schema, FAQ description, AEO gate) pass every CI check end-to-end. The QA suite is defined in package.json as:

    `npm run qa` = build + test:quality + test:lhci
    where test:quality = lint + knip + typecheck + AEO audit

    Execute the commands sequentially, stopping on first failure:
    1. `npm run build` — Astro static build; confirms HowTo schema emits to dist/directions/index.html
    2. `npm run test:aeo` — runs all 6 AEO gates including the new section 6 HowTo gate
    3. `npm run test:quality` — lint + typecheck (catches any JS/TS errors introduced in aeo-audit.mjs)
    4. `npm run test:lhci` — Lighthouse CI against all 5 audited pages (/, /near-grand-canyon/, /near-williams/, /directions/, /faq/)

    If test:lhci is slow, `npm run build && npm run test:aeo && npm run lint && npm run typecheck` is an acceptable partial verification, with full `npm run qa` as the final gate before git commit.

    No file changes — this task is verification only.
  </action>
  <verify>
    <automated>cd /Users/moni/Workspace/SpiceGrillBarWeb/spice-grill-bar-web && npm run build && npm run test:aeo</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (Astro build succeeds with no errors)
    - `npm run test:aeo` exits 0 — all 6 gates pass:
      - Section 1: All FAQ answers ≤50 words, FAQ count ≥34
      - Section 2: llms.txt exists with all required sections
      - Section 3: robots.txt allows GPTBot, ClaudeBot, PerplexityBot, Google-Extended, CCBot
      - Section 4: @id fragments (#restaurant, #organization) in dist/index.html; exactly 8 Question entries
      - Section 5: SpeakableSpecification in dist/faq/index.html
      - Section 6: `"@type":"HowTo"` found in dist/directions/index.html
    - `npm run lint` exits 0 (no ESLint errors in scripts/aeo-audit.mjs)
    - `npm run typecheck` exits 0 (no TypeScript errors)
    - `npm run test:lhci` exits 0 — all Lighthouse thresholds met: LCP < 4000ms, TBT < 600ms, CLS < 0.105, Accessibility ≥ 90, Best Practices ≥ 80, SEO ≥ 90 on all 5 audited pages
    - No Phase 14 regressions: `grep -c 'speakable-city-directions' dist/directions/index.html` returns 3 (class still present in built HTML on Flagstaff, Williams, Las Vegas paragraphs)
  </acceptance_criteria>
  <done>All CI checks pass: build, AEO audit (6 gates), lint, typecheck, and Lighthouse CI. The new Section 6 gate reports "HowTo gate: HowTo schema found in dist/directions/index.html". No regressions from Phase 14.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI script file reads | aeo-audit.mjs reads dist/ files — read-only, no user input, no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering | AEO gate search string `"@type":"HowTo"` | mitigate | Use no-space form (matches Astro minified output) — same pattern as existing `"@type":"Question"` gate which is verified working |
| T-15-05 | Denial of Service | AEO gate when dist/ absent | mitigate | fs.existsSync guard per D-07 — gate skips gracefully with console.warn instead of crashing |
</threat_model>

<verification>
After Task 1, verify the gate is syntactically correct by running the AEO audit against an existing build (or after a fresh build):

```bash
cd /Users/moni/Workspace/SpiceGrillBarWeb/spice-grill-bar-web && node scripts/aeo-audit.mjs
```

If dist/ is stale, run `npm run build` first. The output should end with line:
`✓ HowTo gate: HowTo schema found in dist/directions/index.html`

After Task 2, the full suite output should show no failures and `✅ AEO Audit Passed!`
</verification>

<success_criteria>
- `grep -c '"@type":"HowTo"' scripts/aeo-audit.mjs` returns 1 (the gate search string)
- `npm run build && npm run test:aeo` exits 0 with all 6 gates passing
- `npm run test:lhci` exits 0 — all Lighthouse thresholds met for all 5 pages
- `npm run qa` exits 0 overall
</success_criteria>

<output>
After completion, create `.planning/phases/15-voice-directions-content-polish/15-03-SUMMARY.md`
</output>
