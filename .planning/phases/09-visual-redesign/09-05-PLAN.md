---
phase: 09-visual-redesign
plan: 05
type: execute
wave: 3
depends_on: ["09-01"]
files_modified:
  - src/pages/faq.astro
  - src/pages/near-grand-canyon.astro
  - src/pages/directions.astro
autonomous: true
requirements:
  - VISUAL-07
  - VISUAL-08
  - VISUAL-09

must_haves:
  truths:
    - "FAQ page uses bg-surface-container-low, FAQ items have no borders, uses editorial typography"
    - "Near Grand Canyon page uses bg-surface-container-lowest, all cards have no borders, uses editorial typography"
    - "Directions page uses bg-surface-container-lowest, all city sections have no borders, uses editorial typography"
    - "No border-outline-variant exists in any of these 3 pages"
    - "No shadow-xs or shadow-lg exists on any card in these 3 pages"
  artifacts:
    - path: "src/pages/faq.astro"
      provides: "Reskinned FAQ page"
      contains: "bg-surface-container-low"
    - path: "src/pages/near-grand-canyon.astro"
      provides: "Reskinned Near Grand Canyon page"
      contains: "bg-surface-container-lowest"
    - path: "src/pages/directions.astro"
      provides: "Reskinned Directions page"
      contains: "bg-surface-container-lowest"
  key_links:
    - from: "src/pages/faq.astro"
      to: "globals.css"
      via: "text-display-md, text-heading-md, text-body-md utilities"
      pattern: "text-display-md"
    - from: "src/pages/near-grand-canyon.astro"
      to: "globals.css"
      via: "text-display-md, text-heading-lg, text-body-lg utilities"
      pattern: "text-heading-lg"
    - from: "src/pages/directions.astro"
      to: "globals.css"
      via: "text-display-md, text-heading-lg, text-label-sm utilities"
      pattern: "text-display-md"
---

<objective>
Reskin all 3 secondary pages (FAQ, Near Grand Canyon, Directions) with Radiant Sommelier tokens and typography.

Purpose: Complete border removal and typography updates on all secondary pages, achieving full VISUAL-07/08/09 compliance. These pages share the same pattern: replace bg-surface + border-outline-variant cards with bg-surface-container tonal cards.
Output: All 3 secondary pages using correct surface hierarchy, editorial typography, and no structural borders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-visual-redesign/09-CONTEXT.md
@.planning/phases/09-visual-redesign/09-UI-SPEC.md
@.planning/phases/09-visual-redesign/09-RESEARCH.md
@src/pages/faq.astro
@src/pages/near-grand-canyon.astro
@src/pages/directions.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin FAQ page</name>
  <files>src/pages/faq.astro</files>
  <read_first>
    - src/pages/faq.astro (current state — bg-surface + border on FAQ items, raw typography)
    - .planning/phases/09-visual-redesign/09-UI-SPEC.md (FAQ contract — lines 290-297)
    - .planning/phases/09-visual-redesign/09-RESEARCH.md (FAQ delta — lines 325-332)
  </read_first>
  <action>
Apply the following changes per UI-SPEC FAQ contract and research audit:

**Main element:** Change `bg-surface-container-lowest` (or `bg-surface`) to `bg-surface-container-low min-h-screen`.

**H1:** Replace raw `font-display font-bold text-4xl md:text-5xl` with `text-[1.875rem] md:text-display-md text-center text-on-surface`. Responsive: 30px mobile, 40px desktop.
Keep "Questions" span with `text-primary-container` (accent context 4 — section accent detail per UI-SPEC).

**FAQ item cards:** Replace `bg-surface p-8 rounded-2xl border border-outline-variant shadow-xs` with `bg-surface-container p-8 rounded-2xl`. Remove `border`, remove `shadow-xs`.

**Item spacing:** Change `space-y-8` to `space-y-4` (16px gap per UI-SPEC).

**Item H3 (question):** Replace `font-display font-semibold text-xl` with `text-heading-md text-on-surface`.

**Item answer:** Replace `text-on-surface-variant leading-relaxed font-sans` with `text-body-md text-on-surface-variant`.

**Remove ALL `border-outline-variant`** and `shadow-xs` from this file.
  </action>
  <verify>
    <automated>grep -q 'bg-surface-container-low' src/pages/faq.astro && grep -q 'text-display-md' src/pages/faq.astro && grep -q 'text-heading-md' src/pages/faq.astro && ! grep -q 'border-outline-variant' src/pages/faq.astro && ! grep -q 'shadow-xs' src/pages/faq.astro && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - faq.astro main element has `bg-surface-container-low`
    - H1 uses `text-display-md`
    - "Questions" span retains `text-primary-container`
    - FAQ items use `bg-surface-container p-8 rounded-2xl` (no border, no shadow)
    - Item spacing is `space-y-4` (not `space-y-8`)
    - H3 questions use `text-heading-md text-on-surface`
    - Answers use `text-body-md text-on-surface-variant`
    - No `border-outline-variant` in file
    - No `shadow-xs` in file
    - `npm run build` passes
  </acceptance_criteria>
  <done>FAQ page uses surface-container-low, tonal FAQ items, editorial typography, no borders</done>
</task>

<task type="auto">
  <name>Task 2: Reskin Near Grand Canyon page</name>
  <files>src/pages/near-grand-canyon.astro</files>
  <read_first>
    - src/pages/near-grand-canyon.astro (current state — bg-surface + border on cards)
    - .planning/phases/09-visual-redesign/09-UI-SPEC.md (Near Grand Canyon contract — lines 299-309)
    - .planning/phases/09-visual-redesign/09-RESEARCH.md (Near Grand Canyon delta — lines 336-346)
  </read_first>
  <action>
Apply the following changes per UI-SPEC Near Grand Canyon contract and research audit:

**Main element:** Keep `bg-surface-container-lowest min-h-screen` (per UI-SPEC — already correct if present).

**H1:** Replace raw `font-display font-bold text-4xl md:text-5xl` with `text-[1.875rem] md:text-display-md text-on-surface`. Keep distance span with `text-primary-container` (accent context 4).

**Lead paragraphs:** Replace `text-lg leading-relaxed text-on-surface-variant` with `text-body-lg text-on-surface-variant`.

**"Why Stop Here" block:** Replace `bg-surface p-8 rounded-2xl border border-outline-variant shadow-xs` with `bg-surface-container p-8 rounded-2xl`.

**Section H2s:** Replace raw `font-display font-bold text-2xl md:text-3xl` with `text-heading-lg text-on-surface`.

**Dish cards (What to Order grid):** Replace `bg-surface p-6 rounded-2xl border border-outline-variant shadow-xs` with `bg-surface-container p-6 rounded-2xl`. No border, no shadow.
- Dish name: Replace `font-display font-semibold text-on-surface` with `text-heading-md text-on-surface`
- Price: Keep `text-primary-container font-semibold` (accent context 1)

**Plan Your Visit CTAs:**
- Primary link: Keep `bg-primary-container text-on-primary-container` (accent context 1)
- Secondary links: Replace `border border-outline-variant` with `bg-surface-container text-on-surface hover:bg-surface-container-high`. No border per no-line rule.

**Remove ALL `border-outline-variant`** and `shadow-xs` from this file.
  </action>
  <verify>
    <automated>grep -q 'text-display-md' src/pages/near-grand-canyon.astro && grep -q 'text-heading-lg' src/pages/near-grand-canyon.astro && ! grep -q 'border-outline-variant' src/pages/near-grand-canyon.astro && ! grep -q 'shadow-xs' src/pages/near-grand-canyon.astro && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - near-grand-canyon.astro main has `bg-surface-container-lowest`
    - H1 uses `text-display-md`
    - Section H2s use `text-heading-lg text-on-surface`
    - Lead paragraphs use `text-body-lg`
    - All card/block elements use `bg-surface-container` (not `bg-surface`)
    - No `border-outline-variant` in file
    - No `shadow-xs` in file
    - Secondary CTA links use `bg-surface-container` (not border)
    - Dish prices retain `text-primary-container`
    - `npm run build` passes
  </acceptance_criteria>
  <done>Near Grand Canyon page uses surface-container-lowest, tonal cards, editorial typography, no borders</done>
</task>

<task type="auto">
  <name>Task 3: Reskin Directions page</name>
  <files>src/pages/directions.astro</files>
  <read_first>
    - src/pages/directions.astro (current state — bg-surface + border on 7 city sections + nav)
    - .planning/phases/09-visual-redesign/09-UI-SPEC.md (Directions contract — lines 311-323)
    - .planning/phases/09-visual-redesign/09-RESEARCH.md (Directions delta — lines 349-363)
  </read_first>
  <action>
Apply the following changes per UI-SPEC Directions contract and research audit:

**Main element:** Keep `bg-surface-container-lowest min-h-screen` (per UI-SPEC).

**H1:** Replace raw `font-display font-bold text-4xl md:text-5xl` with `text-[1.875rem] md:text-display-md text-on-surface`.
- Exit span: Replace `text-2xl md:text-3xl text-primary-container` with `text-heading-lg text-primary-container`.

**Lead paragraphs:** Apply `text-body-lg text-on-surface-variant`.

**City nav container:** Replace `bg-surface rounded-2xl border border-outline-variant shadow-xs` with `bg-surface-container rounded-2xl`. No border, no shadow.

**City nav chips:** Keep existing `bg-surface-container-high text-on-surface-variant hover:bg-primary-container hover:text-on-primary-container` — already correct per UI-SPEC. Apply `text-label-sm` if not already present.

**"Jump to:" label:** Apply `text-label-sm text-on-surface-variant uppercase tracking-wide`.

**7 city sections:** Replace ALL `bg-surface p-8 rounded-2xl border border-outline-variant shadow-xs` with `bg-surface-container p-8 rounded-2xl`. No border, no shadow.

**Section H2s (city names):** Replace raw `font-display font-bold text-2xl md:text-3xl` with `text-heading-lg text-on-surface`.

**Directions body text:** Apply `text-body-md text-on-surface-variant`.

**Address blocks:** Apply `text-label-sm text-on-surface-variant`. Phone link hover: Keep `hover:text-primary-container` (already present).

**Map embed containers:** Keep `rounded-2xl overflow-hidden` — no visual changes needed.

**Plan Your Visit section (if present):** Same treatment as near-grand-canyon — secondary links get `bg-surface-container` instead of borders.

**Remove ALL `border-outline-variant`** and `shadow-xs` from this file.
  </action>
  <verify>
    <automated>grep -q 'text-display-md' src/pages/directions.astro && grep -q 'text-heading-lg' src/pages/directions.astro && ! grep -q 'border-outline-variant' src/pages/directions.astro && ! grep -q 'shadow-xs' src/pages/directions.astro && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - directions.astro main has `bg-surface-container-lowest`
    - H1 uses `text-display-md`
    - Exit span uses `text-heading-lg text-primary-container`
    - City nav container uses `bg-surface-container` (no border, no shadow)
    - All 7 city sections use `bg-surface-container` (no border, no shadow)
    - Section H2s use `text-heading-lg text-on-surface`
    - Body text uses `text-body-md text-on-surface-variant`
    - No `border-outline-variant` in file
    - No `shadow-xs` in file
    - `npm run build` passes
  </acceptance_criteria>
  <done>Directions page uses surface-container-lowest, tonal city sections, editorial typography, no borders</done>
</task>

</tasks>

<verification>
- `grep -rn 'border-outline-variant' src/pages/faq.astro src/pages/near-grand-canyon.astro src/pages/directions.astro` returns no matches
- `grep -rn 'shadow-xs' src/pages/faq.astro src/pages/near-grand-canyon.astro src/pages/directions.astro` returns no matches
- All 3 pages use editorial typography utilities
- `npm run build` passes
- `npm run test:quality` passes (lint + knip + typecheck + AEO)
</verification>

<success_criteria>
- FAQ: bg-surface-container-low, tonal items with space-y-4, editorial typography
- Near Grand Canyon: bg-surface-container-lowest, tonal cards, editorial typography
- Directions: bg-surface-container-lowest, tonal city sections, editorial typography
- Zero border-outline-variant across all 3 pages
- Zero shadow-xs/shadow-lg across all 3 pages
- Build and quality checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-visual-redesign/09-05-SUMMARY.md`
</output>
