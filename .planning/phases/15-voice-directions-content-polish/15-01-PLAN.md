---
phase: 15-voice-directions-content-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/directions.astro
autonomous: true
requirements:
  - AEO-14

must_haves:
  truths:
    - "Voice assistants can find verbatim driving instructions from Flagstaff, Williams, and Las Vegas"
    - "HowTo schema in directions.astro contains three HowTo objects with correct totalTime values"
    - "DOM paragraph text and HowToStep.text match verbatim for all three cities"
    - "Phase 14 speakable-city-directions class on the three <p> elements is preserved"
  artifacts:
    - path: "src/pages/directions.astro"
      provides: "HowTo @graph schema block + updated DOM paragraph text for 3 cities"
      contains: '"@type":"HowTo"'
  key_links:
    - from: "directions.astro #flagstaff <p> DOM text"
      to: "HowToStep.text for Flagstaff HowTo object"
      via: "verbatim text copy"
      pattern: 'I-40 Exit 146 \(Ash Fork'
    - from: "directions.astro @graph"
      to: "dist/directions/index.html"
      via: "Astro static build"
      pattern: '"@type":"HowTo"'
---

<objective>
Update directions.astro to add a HowTo JSON-LD @graph schema block covering the three highest-traffic origin cities (Flagstaff, Williams, Las Vegas) and update the DOM paragraph text for all seven cities to use "I-40 Exit 146" (was "Exit 146") for content uniformity.

Purpose: Enables voice assistants to answer "how do I get to Spice Grill & Bar from [city]" with structured, authoritative step text pulled from machine-readable HowTo schema. Closes AEO-14.

Output: directions.astro with updated DOM text in all city <p> elements and a new HowTo @graph <script> block after the Speakable block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-voice-directions-content-polish/15-CONTEXT.md
@.planning/phases/15-voice-directions-content-polish/15-RESEARCH.md
</context>

<interfaces>
<!-- Key existing patterns in directions.astro that must be preserved and extended -->

From src/pages/directions.astro (lines 88-105, Flagstaff section — DO NOT change the class attribute):
  <section id="flagstaff" class="bg-surface-container p-8 rounded-2xl">
    <h2 class="text-heading-lg text-on-surface mb-4">...</h2>
    <p class="speakable-city-directions text-body-lg text-on-surface-variant mb-4">
      From Flagstaff, take I-40 West toward Kingman. Drive 51 miles (about 46 minutes) and
      take <strong>Exit 146</strong> (Ash Fork / Historic Route 66). Turn right onto Lewis Ave —
      Spice Grill &amp; Bar is at 33 Lewis Ave on your right.
    </p>
  </section>

From src/pages/directions.astro (lines 108-124, Williams section):
  <p class="speakable-city-directions text-body-lg text-on-surface-variant mb-4">
    From Williams, take I-40 West. Drive 18 miles (about 18 minutes) and take
    <strong>Exit 146</strong>. Turn right onto Lewis Ave — Spice Grill &amp; Bar is at 33
    Lewis Ave on your right.
  </p>

From src/pages/directions.astro (lines 146-162, Las Vegas section):
  <p class="speakable-city-directions text-body-lg text-on-surface-variant mb-4">
    From Las Vegas, take US-93 South to I-40 East. Drive approximately 200 miles (about 3
    hours) and take <strong>Exit 146</strong> in Ash Fork. Turn left onto Lewis Ave — Spice Grill
    &amp; Bar is at 33 Lewis Ave.
  </p>

From src/pages/directions.astro (lines 288-304, existing Speakable block — HowTo block appended AFTER this):
  <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "WebPage",
      "name": "Directions to Spice Grill & Bar",
      "speakable": {
        "@type": "SpeakableSpecification",
        "cssSelector": [
          ".speakable-heading",
          ".speakable-lead",
          ".speakable-exit",
          ".speakable-city-directions"
        ]
      }
    }
  </script>
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update DOM paragraph text — "Exit 146" → "I-40 Exit 146" in all 7 city sections</name>
  <read_first>
    src/pages/directions.astro
  </read_first>
  <files>src/pages/directions.astro</files>
  <action>
    In src/pages/directions.astro, locate every `<strong>Exit 146</strong>` occurrence inside city `<p>` elements and change it to `<strong>I-40 Exit 146</strong>`. Per D-03 (locked), the three HowTo cities (Flagstaff #line 95, Williams #line 114, Las Vegas #line 152) MUST be updated. Per D-04 (Claude's discretion), also update the remaining four cities (Seligman #line 133, Los Angeles #line 171, Kingman #line 190, Phoenix #line 209) for content uniformity.

    CRITICAL constraints:
    - Change ONLY the text inside `<strong>Exit 146</strong>` → `<strong>I-40 Exit 146</strong>`. Do NOT touch any class attribute on any `<p>` element — the `speakable-city-directions` class on Flagstaff, Williams, and Las Vegas paragraphs was added in Phase 14 and must remain intact.
    - Do NOT change the existing Speakable cssSelector array at lines 296-301.
    - Do NOT change any `<address>` blocks, headings, or other content.

    There are 7 city sections total. Each has exactly one `<strong>Exit 146</strong>` in its direction `<p>`. After edit, all seven should read `<strong>I-40 Exit 146</strong>`.
  </action>
  <verify>
    <automated>grep -c 'I-40 Exit 146' /Users/moni/Workspace/SpiceGrillBarWeb/spice-grill-bar-web/src/pages/directions.astro</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'I-40 Exit 146' src/pages/directions.astro` returns 7 (one per city section)
    - `grep -c '<strong>Exit 146</strong>' src/pages/directions.astro` returns 0 (bare "Exit 146" form fully replaced)
    - `grep -c 'speakable-city-directions' src/pages/directions.astro` returns 3 (Phase 14 classes still present on Flagstaff, Williams, Las Vegas paragraphs)
    - The title attribute in `<Layout title="...">` still reads "I-40 Exit 146" (unchanged; it already had that form)
  </acceptance_criteria>
  <done>All 7 city direction paragraphs use "I-40 Exit 146" inside a `<strong>` element; the speakable-city-directions class remains on exactly 3 paragraphs.</done>
</task>

<task type="auto">
  <name>Task 2: Append HowTo @graph schema block after Speakable block</name>
  <read_first>
    src/pages/directions.astro
  </read_first>
  <files>src/pages/directions.astro</files>
  <action>
    In src/pages/directions.astro, after the closing `</script>` of the Speakable block (currently the last `</script>` before `</Layout>`, around line 304), append a new raw JSON `<script type="application/ld+json">` block containing the HowTo @graph for three cities.

    Per D-01: One HowToStep per city. Per D-02: No supply or tool fields.

    The three HowToStep.text values MUST be verbatim from the updated DOM paragraphs (after Task 1's "I-40 Exit 146" wording). JSON-LD text values use plain `&` — NOT the HTML entity `&amp;` that appears in the Astro source.

    The exact schema structure to insert (use this verbatim — the text values below already reflect the Task 1 DOM updates and use plain `&`):

    HowTo @graph with three objects:
    - Object 1: name="How to get to Spice Grill & Bar from Flagstaff, AZ", totalTime="PT46M", step=[{@type:HowToStep, text:"From Flagstaff, take I-40 West toward Kingman. Drive 51 miles (about 46 minutes) and take I-40 Exit 146 (Ash Fork / Historic Route 66). Turn right onto Lewis Ave — Spice Grill & Bar is at 33 Lewis Ave on your right."}]
    - Object 2: name="How to get to Spice Grill & Bar from Williams, AZ", totalTime="PT18M", step=[{@type:HowToStep, text:"From Williams, take I-40 West. Drive 18 miles (about 18 minutes) and take I-40 Exit 146. Turn right onto Lewis Ave — Spice Grill & Bar is at 33 Lewis Ave on your right."}]
    - Object 3: name="How to get to Spice Grill & Bar from Las Vegas, NV", totalTime="PT3H", step=[{@type:HowToStep, text:"From Las Vegas, take US-93 South to I-40 East. Drive approximately 200 miles (about 3 hours) and take I-40 Exit 146 in Ash Fork. Turn left onto Lewis Ave — Spice Grill & Bar is at 33 Lewis Ave."}]

    Top-level structure: { "@context": "https://schema.org", "@graph": [...] }
    Do NOT put @context inside any individual HowTo object — @context belongs only at the top level.

    Use the same raw JSON `<script type="application/ld+json">` format as the existing FAQPage and Speakable blocks — do NOT use Astro's is:inline or set:html.

    Add a comment before the block: `<!-- HowTo schema markup — voice directions for Flagstaff, Williams, Las Vegas (AEO-14) -->`

    The new block is the last element before `</Layout>` (after the Speakable block).
  </action>
  <verify>
    <automated>grep -c '"@type": "HowTo"' /Users/moni/Workspace/SpiceGrillBarWeb/spice-grill-bar-web/src/pages/directions.astro</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"@type": "HowTo"' src/pages/directions.astro` returns 3 (one per HowTo city object)
    - `grep -c '"@type": "HowToStep"' src/pages/directions.astro` returns 3
    - `grep '"@graph"' src/pages/directions.astro` returns at least 1 match
    - `grep '"@context"' src/pages/directions.astro` — in the new HowTo block, @context appears only once at the top level; no individual HowTo object contains "@context"
    - `grep 'I-40 Exit 146' src/pages/directions.astro` returns matches inside the HowToStep text values (plain `&`, not `&amp;`) — verify with: `grep '"text"' src/pages/directions.astro | grep 'I-40'`
    - `grep '"totalTime": "PT46M"' src/pages/directions.astro` returns 1
    - `grep '"totalTime": "PT18M"' src/pages/directions.astro` returns 1
    - `grep '"totalTime": "PT3H"' src/pages/directions.astro` returns 1
    - The Speakable cssSelector array (lines ~296-301) is unchanged — `grep 'speakable-exit' src/pages/directions.astro` still returns 1 match in the cssSelector array
    - File still ends with `</Layout>` (closing tag not accidentally removed)
  </acceptance_criteria>
  <done>directions.astro contains a HowTo @graph block with three HowTo objects, each with the correct totalTime and a single HowToStep whose text matches the updated DOM paragraph verbatim (using plain & not &amp;).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static build output | Astro compiles .astro source to static HTML — no user input, no runtime trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | JSON-LD text values in raw `<script>` block | accept | Static site — no user input reaches schema text; values are hardcoded at build time |
| T-15-02 | Repudiation | Schema text diverging from DOM paragraphs | mitigate | Task 1 updates DOM first; Task 2 copies text verbatim — single-session atomic edit eliminates divergence window |
</threat_model>

<verification>
After both tasks complete, run:

```bash
cd /Users/moni/Workspace/SpiceGrillBarWeb/spice-grill-bar-web && npm run build && grep -c '"@type":"HowTo"' dist/directions/index.html
```

Expected: 3 (Astro build minifies spaces in JSON-LD key-value pairs, so the source `"@type": "HowTo"` becomes `"@type":"HowTo"` in the output).

Also verify Phase 14 preservation:
```bash
grep -c 'speakable-city-directions' dist/directions/index.html
```
Expected: 3 (class still present on all three paragraphs in built output).
</verification>

<success_criteria>
- `grep -c '"@type":"HowTo"' dist/directions/index.html` returns 3 after `npm run build`
- `grep -c 'I-40 Exit 146' src/pages/directions.astro` returns 7 (all city sections updated)
- `grep -c 'speakable-city-directions' src/pages/directions.astro` returns 3 (Phase 14 classes intact)
- Source `<script>` block is raw JSON (not is:inline), placed after the Speakable block, before `</Layout>`
</success_criteria>

<output>
After completion, create `.planning/phases/15-voice-directions-content-polish/15-01-SUMMARY.md`
</output>
