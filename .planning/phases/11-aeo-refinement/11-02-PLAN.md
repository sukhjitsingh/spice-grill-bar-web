---
phase: 11-aeo-refinement
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - src/components/schema/RestaurantSchema.astro
autonomous: true
requirements: [AEO-01, AEO-03]
requirements_addressed: [AEO-01, AEO-03]
must_haves:
  truths:
    - "RestaurantSchema.astro openingHoursSpecification includes Monday with opens 08:00 / closes 21:00"
    - "Built dist/index.html contains 'https://schema.org/Monday' in the JSON-LD block"
    - "areaServed entry for Kaibab Estates West has a description distinguishing it from a bare City"
    - "npm run typecheck passes (schema-dts types satisfied, no `as any` casts)"
  artifacts:
    - path: "src/components/schema/RestaurantSchema.astro"
      provides: "Restaurant schema with Monday hours + Kaibab Estates West Place upgrade"
      contains: "https://schema.org/Monday"
  key_links:
    - from: "src/components/schema/RestaurantSchema.astro"
      to: "dist/index.html (after build)"
      via: "<script is:inline type=\"application/ld+json\" set:html={JSON.stringify(schema)} />"
      pattern: "\"https://schema.org/Monday\""
---

<objective>
Fix the Monday hours drift in `RestaurantSchema.astro` (AEO-01) and upgrade the Kaibab Estates West `areaServed` entry from a bare `City` to a `Place` with a description (AEO-03). Both edits live in the same file; doing them together is one atomic commit and avoids a second round-trip through the schema.

Purpose: AEO-01 is P0 — Monday is currently OMITTED from `openingHoursSpecification` (lines 32–53), so AI assistants see the restaurant as closed Mondays despite the source-of-truth `faq.json` saying open. AEO-03 is a low-cost enrichment to give Kaibab Estates West a discoverable description for proximity queries.
Output: Updated `RestaurantSchema.astro` whose JSON-LD output contains `https://schema.org/Monday` and a Kaibab Estates West entry with a `description` field. Drift across `RestaurantSchema.astro` ↔ `llms.txt` ↔ `llms-full.txt` is closed by Plan 11-03 in parallel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-aeo-refinement/11-CONTEXT.md
@.planning/phases/11-aeo-refinement/11-RESEARCH.md
@src/components/schema/RestaurantSchema.astro

<interfaces>
<!-- Schema-dts types the executor must satisfy. Verified in node_modules/schema-dts/dist/schema.d.ts -->

OpeningHoursSpecification:
```typescript
{
  '@type': 'OpeningHoursSpecification';
  dayOfWeek: SchemaValue<DayOfWeek | URL>;  // accepts strings like 'https://schema.org/Monday'
  opens: SchemaValue<Time>;                  // 24-hour 'HH:MM' string
  closes: SchemaValue<Time>;
}
```

areaServed accepts (current file uses both):
- `{ '@type': 'City', name: string, '@id'?: URL }`
- `{ '@type': 'Place', name: string, description?: string, '@id'?: URL }`

The variable is typed `WithContext<Restaurant>` from schema-dts (line 18). No `as any` casts permitted.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Monday to openingHoursSpecification (AEO-01)</name>
  <files>src/components/schema/RestaurantSchema.astro</files>
  <read_first>
    - src/components/schema/RestaurantSchema.astro (full file — lines 32–53 hold openingHoursSpecification)
    - .planning/phases/11-aeo-refinement/11-RESEARCH.md (section "Code Examples > Fixing Monday in `openingHoursSpecification`")
    - src/data/faq.json (entry #14 "What are the operating hours for Spice Grill & Bar?" — source of truth)
  </read_first>
  <action>
Edit `src/components/schema/RestaurantSchema.astro`. Replace the entire `openingHoursSpecification` array (currently lines 32–53, two entries: Tue–Thu and Fri–Sun) with this exact array. Do NOT change indentation style (2-space) or any surrounding code.

```typescript
  openingHoursSpecification: [
    {
      '@type': 'OpeningHoursSpecification',
      dayOfWeek: [
        'https://schema.org/Monday',
        'https://schema.org/Tuesday',
        'https://schema.org/Wednesday',
        'https://schema.org/Thursday',
      ],
      opens: '08:00',
      closes: '21:00',
    },
    {
      '@type': 'OpeningHoursSpecification',
      dayOfWeek: [
        'https://schema.org/Friday',
        'https://schema.org/Saturday',
        'https://schema.org/Sunday',
      ],
      opens: '08:00',
      closes: '22:00',
    },
  ],
```

Why merging Mon–Thu (vs. a separate Monday block): the hours match Tue–Thu exactly, so a single block is correct, more compact, and is the schema-dts-preferred form per research §Code Examples. The new line — `'https://schema.org/Monday',` — is the only addition.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run build &amp;&amp; grep -c '"https://schema.org/Monday"' dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0 (schema-dts WithContext&lt;Restaurant&gt; type still satisfied; no `as any` casts introduced)
    - `npm run build` exits 0
    - `grep -c '"https://schema.org/Monday"' dist/index.html` returns ≥1
    - `grep -c '"https://schema.org/Tuesday"' dist/index.html` returns ≥1 (regression check — Tue still listed)
    - `grep -c "OpeningHoursSpecification" dist/index.html` returns ≥2 (one for Mon–Thu, one for Fri–Sun)
    - File still has exactly 2 OpeningHoursSpecification entries (one merged Mon–Thu, one Fri–Sun) — count via `grep -c "@type.: .OpeningHoursSpecification" src/components/schema/RestaurantSchema.astro` returns 2
  </acceptance_criteria>
  <done>Monday opens 08:00 closes 21:00 in built HTML; typecheck passes; existing Fri–Sun entry unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Upgrade Kaibab Estates West to Place with description (AEO-03)</name>
  <files>src/components/schema/RestaurantSchema.astro</files>
  <read_first>
    - src/components/schema/RestaurantSchema.astro (lines 74–91 hold the areaServed array; Kaibab Estates West is currently at line 77 as bare City)
    - .planning/phases/11-aeo-refinement/11-CONTEXT.md (decisions §"areaServed — verify Kaibab Estates West already exists; upgrade to Place type with description if currently bare City")
    - .planning/phases/11-aeo-refinement/11-RESEARCH.md (§"User Constraints > Schema Fields to Add" — areaServed)
  </read_first>
  <action>
In `src/components/schema/RestaurantSchema.astro`, find the line:

```typescript
    { '@type': 'City', name: 'Kaibab Estates West' },
```

Replace it with this exact line (a `Place` with description; matches the pattern used by the existing `Place` entries at lines 88–90 of the same file):

```typescript
    { '@type': 'Place', name: 'Kaibab Estates West', description: 'Residential community ~5 miles east of Ash Fork on I-40 (~6 minutes).' },
```

Why `Place` and not `City`: per CONTEXT, Kaibab Estates West is a residential community (not an incorporated city). The existing file already uses `Place` for "Grand Canyon South Rim" and "I-40 Corridor, Arizona" — this matches that precedent. The description field gives AI assistants a concrete proximity claim ("5 miles east, 6 minutes") for voice answers like "is there an Indian restaurant near Kaibab Estates West".

Distance value (~5 mi / ~6 min) is locked by CONTEXT.md — do NOT change it. If the owner-confirmation file (Plan 11-01) ends up correcting the distance, a follow-up edit will land in Wave 3.

Leave every OTHER entry in `areaServed` untouched — including the 3 existing `Place` entries at the bottom.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run build &amp;&amp; grep -A1 '"Kaibab Estates West"' dist/index.html | grep -c "Residential community"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0
    - `npm run build` exits 0
    - `grep -c '"Kaibab Estates West"' dist/index.html` returns ≥1
    - `grep -A2 '"Kaibab Estates West"' dist/index.html | grep -c "description"` returns ≥1
    - `grep -A2 '"Kaibab Estates West"' dist/index.html | grep -c "Residential community"` returns ≥1
    - `grep -c '"@type":"Place","name":"Kaibab Estates West"' dist/index.html` returns 1 (upgraded from City to Place)
    - `grep -c '"name":"Williams"' dist/index.html` returns 1 (regression check — Williams still in areaServed)
  </acceptance_criteria>
  <done>Kaibab Estates West entry now `@type: Place` with description "Residential community ~5 miles east of Ash Fork on I-40 (~6 minutes)."; all other areaServed entries unchanged.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` exits 0
- `npm run build` exits 0
- `grep -c '"https://schema.org/Monday"' dist/index.html` returns ≥1
- `grep -c '"@type":"Place","name":"Kaibab Estates West"' dist/index.html` returns 1
- `grep -A2 '"Kaibab Estates West"' dist/index.html | grep -c "Residential community"` returns ≥1
- `npm run test:aeo` exits 0 (no regressions on existing FAQ word-count gate)
</verification>

<success_criteria>
RestaurantSchema.astro emits Monday hours and a Kaibab Estates West Place entry into the home-page JSON-LD. Plans 11-03 (llms files), 11-04 (FAQ), and 11-05 (schema enrichment with payment/reservations/amenities) can build on this without re-touching `openingHoursSpecification` or `areaServed`.
</success_criteria>

<output>
After completion, create `.planning/phases/11-aeo-refinement/11-02-SUMMARY.md` recording the lines changed, the grep results from `dist/index.html`, and a note that hours drift in `llms.txt` / `llms-full.txt` is being addressed in parallel by Plan 11-03.
</output>
