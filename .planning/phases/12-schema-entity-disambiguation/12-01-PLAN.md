---
phase: 12-schema-entity-disambiguation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/schema/RestaurantSchema.astro
  - src/components/schema/OrganizationSchema.astro
autonomous: true
requirements: [AEO-11]

must_haves:
  truths:
    - "RestaurantSchema JSON-LD carries a distinct @id fragment #restaurant"
    - "OrganizationSchema JSON-LD carries a distinct @id fragment #organization"
    - "RestaurantSchema exposes a sameAs array with all 5 profile URLs"
    - "Both schemas reference the same canonical CID Google Maps URL for the entity"
  artifacts:
    - path: "src/components/schema/RestaurantSchema.astro"
      provides: "Restaurant JSON-LD with @id and sameAs"
      contains: "https://spicegrillbar66.com/#restaurant"
    - path: "src/components/schema/OrganizationSchema.astro"
      provides: "Organization JSON-LD with @id"
      contains: "https://spicegrillbar66.com/#organization"
  key_links:
    - from: "src/components/schema/RestaurantSchema.astro"
      to: "src/components/schema/OrganizationSchema.astro"
      via: "shared CID Maps URL in sameAs arrays"
      pattern: "google\\.com/maps\\?cid=5034425112937519671"
---

<objective>
Establish distinct entity identity for the two top-level JSON-LD schema blocks so AI engines and Google's Knowledge Graph resolve the Restaurant and the Organization as separate, unambiguous entities.

Purpose: A shared bare-domain `@id` (or no `@id`) causes an entity graph collision between RestaurantSchema and OrganizationSchema. Distinct `#restaurant` and `#organization` fragments plus a complete `sameAs` profile set let answer engines disambiguate and cross-link the restaurant entity.
Output: Updated `RestaurantSchema.astro` (adds `@id` + `sameAs`) and `OrganizationSchema.astro` (adds `@id`, swaps Maps short link for canonical CID URL).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-schema-entity-disambiguation/12-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- schema-dts types already accept these properties — no casting/workaround needed. -->
<!-- WithContext<T> includes `@id: string`; Thing (base of Restaurant + Organization) includes `sameAs`. -->
<!-- Property-name syntax for @id follows the existing areaServed pattern in RestaurantSchema.astro: -->
<!--   { '@type': 'City', name: 'Ash Fork', '@id': 'https://en.wikipedia.org/wiki/Ash_Fork,_Arizona' } -->
<!-- Both schemas render via: <script is:inline type="application/ld+json" set:html={JSON.stringify(schema)} /> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @id and sameAs to RestaurantSchema</name>
  <files>src/components/schema/RestaurantSchema.astro</files>
  <read_first>
    - src/components/schema/RestaurantSchema.astro (current state — schema object starts at line 18; has @context/@type but no @id and no entity-level sameAs; areaServed entries at lines 76-96 show the existing '@id' property syntax)
    - src/components/schema/OrganizationSchema.astro (the existing sameAs array at lines 17-23 — the 4 non-Maps URLs are mirrored verbatim)
    - .planning/phases/12-schema-entity-disambiguation/12-CONTEXT.md (D-01 locks the canonical CID Maps URL; the 5 sameAs URLs are listed verbatim under "Specific Ideas")
  </read_first>
  <action>
    In the `schema` object literal in `RestaurantSchema.astro`, add the property `'@id': 'https://spicegrillbar66.com/#restaurant'` immediately after `'@type': 'Restaurant'` (conventional JSON-LD ordering — @id near the top after @context/@type per Claude's Discretion in D-CONTEXT). Then add an entity-level `sameAs` array with exactly these 5 string entries in this order (per D-01, the Maps entry is the canonical CID URL, NOT the maps.app.goo.gl short link): `https://www.google.com/maps?cid=5034425112937519671`, `https://www.yelp.com/biz/spice-grill-and-bar-ash-fork`, `https://www.tripadvisor.com/Restaurant_Review-g29037-d33218710-Reviews-Spice_Grill_Bar-Ash_Fork_Arizona.html`, `https://www.facebook.com/profile.php?id=61566349169122`, `https://www.instagram.com/panjabi_dhaba_sgb`. Place `sameAs` at the entity level (sibling of `name`, `url`, `address`) — a reasonable position is right after `url`. Do NOT modify the existing `hasMap` property (it stays as the short link — out of scope per 12-CONTEXT). Do NOT touch any other property.
  </action>
  <verify>
    <automated>grep -c "'@id': 'https://spicegrillbar66.com/#restaurant'" src/components/schema/RestaurantSchema.astro</automated>
  </verify>
  <acceptance_criteria>
    - `RestaurantSchema.astro` contains the literal `'@id': 'https://spicegrillbar66.com/#restaurant'`
    - `RestaurantSchema.astro` contains an entity-level `sameAs` array with all 5 URLs listed in the action
    - `RestaurantSchema.astro` contains `https://www.google.com/maps?cid=5034425112937519671` (canonical CID URL present in sameAs)
    - The existing `hasMap: 'https://maps.app.goo.gl/q2EJFMbMRaysU6vH8'` line is unchanged
    - `npm run typecheck` exits 0 (schema-dts accepts @id and sameAs without error)
  </acceptance_criteria>
  <done>RestaurantSchema JSON-LD has a distinct `#restaurant` @id and a 5-URL sameAs array using the canonical CID Maps URL; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add @id to OrganizationSchema and canonicalize Maps URL</name>
  <files>src/components/schema/OrganizationSchema.astro</files>
  <read_first>
    - src/components/schema/OrganizationSchema.astro (current state — schema object at lines 4-32; has @context/@type and a 5-URL sameAs array at lines 17-23 but no @id; sameAs[0] is currently the maps.app.goo.gl short link)
    - .planning/phases/12-schema-entity-disambiguation/12-CONTEXT.md (D-02 locks the OrganizationSchema Maps URL update to the same canonical CID URL used in RestaurantSchema)
  </read_first>
  <action>
    In the `schema` object literal in `OrganizationSchema.astro`, add the property `'@id': 'https://spicegrillbar66.com/#organization'` immediately after `'@type': 'Organization'` (conventional JSON-LD ordering — matches the @id placement chosen in Task 1). Then, per D-02, update the first entry of the existing `sameAs` array from `'https://maps.app.goo.gl/q2EJFMbMRaysU6vH8'` to `'https://www.google.com/maps?cid=5034425112937519671'` so both schemas reference the same entity with the same canonical URL. Do NOT change the other 4 sameAs entries and do NOT touch any other property.
  </action>
  <verify>
    <automated>grep -c "'@id': 'https://spicegrillbar66.com/#organization'" src/components/schema/OrganizationSchema.astro</automated>
  </verify>
  <acceptance_criteria>
    - `OrganizationSchema.astro` contains the literal `'@id': 'https://spicegrillbar66.com/#organization'`
    - `OrganizationSchema.astro` no longer contains `maps.app.goo.gl/q2EJFMbMRaysU6vH8` (`grep -c` returns 0)
    - `OrganizationSchema.astro` `sameAs[0]` is `https://www.google.com/maps?cid=5034425112937519671`
    - The other 4 sameAs entries (Yelp, TripAdvisor, Facebook, Instagram) are unchanged
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>OrganizationSchema JSON-LD has a distinct `#organization` @id and its Maps sameAs entry matches RestaurantSchema's canonical CID URL; typecheck passes.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` exits 0 — schema-dts accepts both new `@id` properties and the RestaurantSchema `sameAs` array.
- `npm run build` succeeds and `dist/index.html` contains both `"@id":"https://spicegrillbar66.com/#restaurant"` and `"@id":"https://spicegrillbar66.com/#organization"` (JSON.stringify produces no spaces around the colon).
- The two `@id` fragment values are distinct strings (`#restaurant` ≠ `#organization`) — no entity graph collision.
</verification>

<success_criteria>
- RestaurantSchema.astro JSON-LD contains `@id: "https://spicegrillbar66.com/#restaurant"` and a `sameAs` array with all 5 profile URLs.
- OrganizationSchema.astro JSON-LD contains `@id: "https://spicegrillbar66.com/#organization"` — distinct fragment from the Restaurant `@id`.
- Both schemas reference the canonical CID Google Maps URL for the entity.
- Build output (`dist/index.html`) carries both rendered `@id` fragments.
</success_criteria>

<output>
After completion, create `.planning/phases/12-schema-entity-disambiguation/12-01-SUMMARY.md`
</output>
