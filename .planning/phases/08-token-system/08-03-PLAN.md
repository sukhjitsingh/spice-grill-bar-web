---
phase: 08-token-system
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/components/Footer.astro
  - src/components/OurStorySection.astro
  - src/components/LocationSection.astro
  - src/components/OrderSection.astro
  - src/components/GoogleMap.tsx
  - src/components/ui/AstroButton.astro
autonomous: true
requirements:
  - TOKEN-03

must_haves:
  truths:
    - "Footer, OurStorySection, LocationSection, OrderSection, GoogleMap, and AstroButton render correctly using M3 token classes in both modes"
    - "No hardcoded zinc-*, neutral-*, or orange-* Tailwind classes remain in any of the 6 files"
    - "No dark: prefix overrides remain in any of the 6 files (M3 tokens auto-switch via CSS variables)"
    - "OrderSection glassmorphism container uses glass utility class instead of hardcoded backdrop-blur"
  artifacts:
    - path: "src/components/Footer.astro"
      provides: "Footer with M3 token classes"
      contains: "text-on-surface"
    - path: "src/components/OurStorySection.astro"
      provides: "OurStorySection with M3 token classes"
      contains: "text-primary-container"
    - path: "src/components/OrderSection.astro"
      provides: "OrderSection with M3 token classes and glass utility"
      contains: "bg-surface-container-low"
    - path: "src/components/ui/AstroButton.astro"
      provides: "AstroButton with M3 token classes matching button.tsx"
      contains: "bg-primary-container"
  key_links:
    - from: "src/components/Footer.astro"
      to: "src/styles/globals.css"
      via: "Tailwind utility classes referencing @theme inline tokens"
      pattern: "text-on-surface-variant"
    - from: "src/components/ui/AstroButton.astro"
      to: "src/styles/globals.css"
      via: "Tailwind utility classes referencing @theme inline tokens"
      pattern: "bg-primary-container text-on-primary-container"
---

<objective>
Migrate the 6 component files that Plan 02 missed from hardcoded zinc/neutral/orange/dark: classes to M3 token equivalents.

Purpose: Complete TOKEN-03 coverage. Plan 02 covers Radix UI components and brand-* references in pages, but Footer.astro, OurStorySection.astro, LocationSection.astro, OrderSection.astro, GoogleMap.tsx, and AstroButton.astro still contain extensive hardcoded color classes that bypass the M3 token system.

Output: All 6 files updated to use M3 token classes exclusively, with no zinc/neutral/orange hardcoded colors and no dark: prefix overrides.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-token-system/08-CONTEXT.md
@.planning/phases/08-token-system/08-01-SUMMARY.md

<interfaces>
<!-- M3 token classes available after Plan 01 (from @theme inline in globals.css) -->

Surface: bg-surface-dim, bg-surface, bg-surface-container-lowest, bg-surface-container-low, bg-surface-container, bg-surface-container-high, bg-surface-container-highest, bg-surface-bright, bg-surface-variant
Text on surface: text-on-surface, text-on-surface-variant
Primary: bg-primary, bg-primary-container, text-primary, text-primary-container, text-on-primary, text-on-primary-container
Secondary: bg-secondary, bg-secondary-container, text-on-secondary, text-on-secondary-container
Tertiary: bg-tertiary, bg-tertiary-container
Error: bg-error, bg-error-container, text-on-error, text-on-error-container
Outline: border-outline, border-outline-variant, ring-outline
Inverse: bg-inverse-surface, text-inverse-on-surface, text-inverse-primary
Background: bg-background, text-on-background

Utilities: glass, glass-card, hero-gradient, cta-gradient
Shadow token: --shadow-color (auto-tinted per mode, applied globally)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Footer, OurStorySection, and LocationSection to M3 tokens</name>
  <files>src/components/Footer.astro, src/components/OurStorySection.astro, src/components/LocationSection.astro</files>
  <read_first>
    - src/components/Footer.astro (current: ~30 zinc/orange-500 references)
    - src/components/OurStorySection.astro (current: orange-700/dark:orange-400, zinc-700/dark:zinc-300 patterns)
    - src/components/LocationSection.astro (current: zinc-50, zinc-900, orange-700/dark:orange-400)
    - src/styles/globals.css (verify M3 tokens are in place from Plan 01)
  </read_first>
  <action>
Replace all hardcoded color classes in each file. Per D-04 (zero hardcoded hex in components) and D-12 (brand tokens replaced by M3 primary family). Remove ALL `dark:` prefix overrides since M3 tokens auto-switch via CSS variables.

**Footer.astro — complete class replacement map:**

Line 6 (footer element):
- `bg-white dark:bg-black` -> `bg-surface`
- `border-zinc-200 dark:border-white/5` -> `border-outline-variant`

Line 12 (brand link):
- `text-zinc-900 dark:text-white` -> `text-on-surface`
- `focus-visible:ring-orange-500` -> `focus-visible:ring-outline`

Line 16 (description):
- `text-zinc-600 dark:text-zinc-300` -> `text-on-surface-variant`

Line 22 (section heading "Visit Us"):
- `text-zinc-900 dark:text-white` -> `text-on-surface`

Line 29 (address link):
- `text-zinc-600 dark:text-zinc-300` -> `text-on-surface-variant`
- `hover:text-zinc-900 dark:hover:text-white` -> `hover:text-on-surface`
- `focus-visible:ring-orange-500` -> `focus-visible:ring-outline`

Line 41 (phone link):
- `text-zinc-600 dark:text-zinc-300` -> `text-on-surface-variant`
- `hover:text-zinc-900 dark:hover:text-white` -> `hover:text-on-surface`
- `focus-visible:ring-orange-500` -> `focus-visible:ring-outline`

Line 51 (section heading "Hours"):
- `text-zinc-900 dark:text-white` -> `text-on-surface`

Line 56 (hours list):
- `text-zinc-600 dark:text-zinc-300` -> `text-on-surface-variant`

Lines 59, 63, 67 (hour values):
- `text-zinc-700 dark:text-zinc-300` -> `text-on-surface-variant`

Line 73 (section heading "Explore"):
- `text-zinc-900 dark:text-white` -> `text-on-surface`

Line 76 (explore list):
- `text-zinc-600 dark:text-zinc-300` -> `text-on-surface-variant`

Lines 80, 88, 96 (nav links):
- `hover:text-zinc-900 dark:hover:text-white` -> `hover:text-on-surface`
- `focus-visible:ring-orange-500` -> `focus-visible:ring-outline`

Line 101 (social divider):
- `border-zinc-100 dark:border-zinc-800` -> `border-outline-variant`

Lines 106, 128 (social links):
- `hover:text-zinc-900 dark:hover:text-white` -> `hover:text-on-surface`
- `focus-visible:ring-orange-500` -> `focus-visible:ring-outline`

Line 149 (bottom bar):
- `border-zinc-100 dark:border-zinc-900` -> `border-outline-variant`
- `text-zinc-600 dark:text-zinc-400` -> `text-on-surface-variant`

**OurStorySection.astro — complete class replacement map:**

Line 9 (section):
- `bg-white dark:bg-black` -> `bg-surface`

Line 13 (heading):
- `text-zinc-900 dark:text-white` -> `text-on-surface`

Line 16 (body text):
- `text-zinc-700 dark:text-zinc-300` -> `text-on-surface-variant`

Lines 28, 37 (stat numbers):
- `text-orange-700 dark:text-orange-400` -> `text-primary-container`

Lines 32, 41 (stat labels):
- `text-zinc-700 dark:text-zinc-300` -> `text-on-surface-variant`

Line 56 (glass overlay text):
- `text-zinc-900 dark:text-white` -> `text-on-surface`

Line 57 (glass overlay subtext):
- `text-zinc-600 dark:text-zinc-300` -> `text-on-surface-variant`

Lines 66, 75, 84 (mission/vision/values headings):
- `text-orange-700 dark:text-orange-400` -> `text-primary-container`

Lines 69, 78, 87 (mission/vision/values body):
- `text-zinc-700 dark:text-zinc-300` -> `text-on-surface-variant`

**LocationSection.astro — complete class replacement map:**

Line 7 (section):
- `bg-zinc-50 dark:bg-black` -> `bg-surface-container-low`

Line 11 (heading):
- `text-zinc-900 dark:text-white` -> `text-on-surface`

Line 15 (description):
- `text-zinc-700 dark:text-zinc-300` -> `text-on-surface-variant`

Line 19-20 (accent span):
- `text-orange-700 dark:text-orange-400` -> `text-primary-container`

IMPORTANT: After all replacements in each file, verify:
- Zero instances of `zinc-` remain
- Zero instances of `orange-` remain
- Zero instances of `dark:` remain (except inside comments if any)
- Zero instances of `neutral-` remain
  </action>
  <verify>
    <automated>cd /Users/moni/Workspace/SpiceGrillBarWeb/spice-grill-bar-web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `Footer.astro` contains `bg-surface` (not `bg-white dark:bg-black`)
    - `Footer.astro` contains `text-on-surface-variant` (not `text-zinc-600`)
    - `Footer.astro` contains `focus-visible:ring-outline` (not `focus-visible:ring-orange-500`)
    - `Footer.astro` contains `border-outline-variant` (not `border-zinc-200`)
    - `Footer.astro` does NOT contain the string `zinc-`
    - `Footer.astro` does NOT contain the string `orange-`
    - `Footer.astro` does NOT contain the string `dark:`
    - `OurStorySection.astro` contains `bg-surface` (not `bg-white dark:bg-black`)
    - `OurStorySection.astro` contains `text-primary-container` (not `text-orange-700`)
    - `OurStorySection.astro` does NOT contain the string `zinc-`
    - `OurStorySection.astro` does NOT contain the string `orange-`
    - `OurStorySection.astro` does NOT contain the string `dark:`
    - `LocationSection.astro` contains `bg-surface-container-low` (not `bg-zinc-50`)
    - `LocationSection.astro` contains `text-primary-container` (not `text-orange-700`)
    - `LocationSection.astro` does NOT contain the string `zinc-`
    - `LocationSection.astro` does NOT contain the string `orange-`
    - `LocationSection.astro` does NOT contain the string `dark:`
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>Footer.astro, OurStorySection.astro, and LocationSection.astro use M3 token classes exclusively. No zinc/neutral/orange hardcoded colors or dark: prefix overrides remain. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate OrderSection, GoogleMap, and AstroButton to M3 tokens</name>
  <files>src/components/OrderSection.astro, src/components/GoogleMap.tsx, src/components/ui/AstroButton.astro</files>
  <read_first>
    - src/components/OrderSection.astro (current: zinc-100/dark:zinc-950 section bg, hardcoded backdrop-blur-xl container, orange-700/dark:orange-300 accents)
    - src/components/GoogleMap.tsx (current: bg-zinc-100/dark:bg-zinc-800, text-zinc-600/dark:text-zinc-300)
    - src/components/ui/AstroButton.astro (current: neutral-900/neutral-50 variants with dark: overrides, ring-orange-500, shadow-zinc-200)
    - src/styles/globals.css (verify M3 tokens and glass utility are in place from Plan 01)
  </read_first>
  <action>
Replace all hardcoded color classes. Per D-04, D-12, D-14 (glass utility), D-15 (glass-card = no blur).

**OrderSection.astro — complete class replacement map:**

Line 9 (section):
- `bg-zinc-100 dark:bg-zinc-950` -> `bg-surface-container-low`

Line 20 (glassmorphism container — CRITICAL):
- Replace the entire class string `bg-white/30 dark:bg-black/40 backdrop-blur-xl border border-white/20 dark:border-white/10 shadow-2xl rounded-3xl p-8 md:p-12 text-center`
- With: `glass shadow-2xl rounded-3xl p-8 md:p-12 text-center`
- This uses the `glass` utility from Plan 01 which provides backdrop-blur(32px), warm-tinted background, and 0.5px border. The glass utility auto-adapts per mode via --glass-bg and --glass-border variables.

Line 23 (heading):
- `text-zinc-900 dark:text-white` -> `text-on-surface`

Line 27 (body text):
- `text-zinc-700 dark:text-zinc-300` -> `text-on-surface-variant`

Lines 28, 30 (accent spans):
- `text-orange-700 dark:text-orange-300` -> `text-primary-container`

Line 43 (fine print):
- `text-zinc-700 dark:text-zinc-300` -> `text-on-surface-variant`

**GoogleMap.tsx — complete class replacement map:**

Line 41 (placeholder container):
- `bg-zinc-100 dark:bg-zinc-800` -> `bg-surface-container`

Line 58 (loading text):
- `text-zinc-600 dark:text-zinc-300` -> `text-on-surface-variant`

**AstroButton.astro — complete class replacement map:**
This mirrors button.tsx from Plan 02 (D-09/D-10). Replace the entire baseStyles and variants:

Line 14 (baseStyles):
- Replace `focus-visible:ring-1 focus-visible:ring-neutral-950` with `focus-visible:ring-1 focus-visible:ring-outline`
- Replace `focus-visible:ring-2 focus-visible:ring-orange-500 focus-visible:ring-offset-2` with `focus-visible:ring-2 focus-visible:ring-outline focus-visible:ring-offset-2`
- The full baseStyles should be: `'inline-flex items-center justify-center gap-2 whitespace-nowrap text-sm font-medium transition-colors focus-visible:outline-hidden focus-visible:ring-1 focus-visible:ring-outline disabled:pointer-events-none disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:size-4 [&_svg]:shrink-0 rounded-full! focus-visible:ring-2 focus-visible:ring-outline focus-visible:ring-offset-2'`

Line 18 (primary variant):
- Replace `bg-neutral-900 text-neutral-50 shadow hover:bg-neutral-900/90 dark:bg-neutral-50 dark:text-neutral-900 dark:hover:bg-neutral-50/90 shadow-lg shadow-zinc-200/50 dark:shadow-none`
- With: `bg-primary-container text-on-primary-container shadow hover:bg-primary-container/90 shadow-lg`
- Note: No dark: prefixes. No shadow-zinc tint (global --shadow-color handles tinting).

Line 20 (outline variant):
- Replace `border border-neutral-200 bg-white shadow-xs hover:bg-neutral-100 hover:text-neutral-900 dark:border-neutral-800 dark:bg-neutral-950 dark:hover:bg-neutral-800 dark:hover:text-neutral-50`
- With: `border border-outline-variant bg-surface shadow-xs hover:bg-surface-container hover:text-on-surface`

IMPORTANT: After all replacements, verify:
- Zero `zinc-` in all 3 files
- Zero `neutral-` in all 3 files
- Zero `orange-` in all 3 files
- Zero `dark:` in all 3 files
- OrderSection.astro line 20 uses `glass` utility (NOT hardcoded backdrop-blur)
  </action>
  <verify>
    <automated>cd /Users/moni/Workspace/SpiceGrillBarWeb/spice-grill-bar-web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `OrderSection.astro` contains `bg-surface-container-low` (not `bg-zinc-100`)
    - `OrderSection.astro` contains the class `glass` on the glassmorphism container (line ~20)
    - `OrderSection.astro` does NOT contain the string `backdrop-blur`
    - `OrderSection.astro` contains `text-primary-container` (not `text-orange-700`)
    - `OrderSection.astro` does NOT contain `dark:`
    - `GoogleMap.tsx` contains `bg-surface-container` (not `bg-zinc-100`)
    - `GoogleMap.tsx` contains `text-on-surface-variant` (not `text-zinc-600`)
    - `GoogleMap.tsx` does NOT contain `zinc-`
    - `GoogleMap.tsx` does NOT contain `dark:`
    - `AstroButton.astro` contains `bg-primary-container text-on-primary-container` (primary variant)
    - `AstroButton.astro` contains `bg-surface shadow-xs hover:bg-surface-container` (outline variant)
    - `AstroButton.astro` contains `focus-visible:ring-outline` (not `ring-orange-500`)
    - `AstroButton.astro` does NOT contain `neutral-`
    - `AstroButton.astro` does NOT contain `zinc-`
    - `AstroButton.astro` does NOT contain `orange-`
    - `AstroButton.astro` does NOT contain `dark:`
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>OrderSection.astro, GoogleMap.tsx, and AstroButton.astro use M3 token classes exclusively. OrderSection uses glass utility instead of hardcoded backdrop-blur. No zinc/neutral/orange or dark: prefix overrides remain. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `grep -c "zinc-\|neutral-\|orange-" src/components/Footer.astro src/components/OurStorySection.astro src/components/LocationSection.astro src/components/OrderSection.astro src/components/GoogleMap.tsx src/components/ui/AstroButton.astro` returns 0 for each file
- `grep -c "dark:" src/components/Footer.astro src/components/OurStorySection.astro src/components/LocationSection.astro src/components/OrderSection.astro src/components/GoogleMap.tsx src/components/ui/AstroButton.astro` returns 0 for each file
</verification>

<success_criteria>
- All 6 missed files fully migrated to M3 token classes
- OrderSection uses glass utility for its glassmorphism container
- AstroButton mirrors button.tsx M3 token pattern
- Zero hardcoded color classes remain in any of the 6 files
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08-token-system/08-03-SUMMARY.md`
</output>
