---
phase: 08-token-system
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/sheet.tsx
  - src/components/ui/dropdown-menu.tsx
  - src/layouts/Layout.astro
  - src/components/ReviewsSection.astro
  - src/components/Header.tsx
  - src/components/Hero.astro
  - src/components/MenuSection.tsx
  - src/components/MobileActionButtons.astro
  - src/pages/faq.astro
  - src/pages/directions.astro
  - src/pages/near-grand-canyon.astro
autonomous: true
requirements:
  - TOKEN-03

must_haves:
  truths:
    - "Button, Sheet, and DropdownMenu render correctly using M3 token classes in both modes"
    - "No shadcn class names (bg-background, text-foreground, bg-popover, text-popover-foreground, bg-muted, text-muted-foreground, bg-accent, text-accent-foreground, bg-secondary, ring-ring, ring-offset-background) remain in any component"
    - "No brand-orange, brand-green, or brand-gold class references remain in any component"
    - "All orange accent text uses text-primary-container instead of text-brand-orange"
    - "All orange CTA backgrounds use bg-primary-container instead of bg-brand-orange"
  artifacts:
    - path: "src/components/ui/button.tsx"
      provides: "Button with M3 token classes"
      contains: "bg-primary-container"
    - path: "src/components/ui/sheet.tsx"
      provides: "Sheet with M3 token classes"
      contains: "bg-surface"
    - path: "src/components/ui/dropdown-menu.tsx"
      provides: "DropdownMenu with M3 token classes"
      contains: "bg-surface-container-highest"
  key_links:
    - from: "src/components/ui/button.tsx"
      to: "src/styles/globals.css"
      via: "Tailwind utility classes referencing @theme inline tokens"
      pattern: "bg-primary-container text-on-primary-container"
    - from: "src/components/ui/dropdown-menu.tsx"
      to: "src/styles/globals.css"
      via: "Tailwind utility classes referencing @theme inline tokens"
      pattern: "bg-surface-container-highest text-on-surface"
---

<objective>
Replace all shadcn and brand-* class references across the entire codebase with M3 token equivalents.

Purpose: Complete the TOKEN-03 requirement — shadcn semantic tokens remapped to M3 colors. After this plan, zero shadcn or brand-* class names remain anywhere in `src/`.

Output: Updated Radix UI components (Button, Sheet, DropdownMenu) and all Astro/React components that reference brand-orange/brand-green/brand-gold or shadcn token classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-token-system/08-CONTEXT.md
@.planning/phases/08-token-system/08-RESEARCH.md
@.planning/phases/08-token-system/08-01-SUMMARY.md
@src/styles/globals.css

<interfaces>
<!-- M3 token classes available after Plan 01 (from @theme inline in globals.css) -->
<!-- Executor can use any of these as Tailwind utility classes: -->

Surface: bg-surface-dim, bg-surface, bg-surface-container-lowest, bg-surface-container-low, bg-surface-container, bg-surface-container-high, bg-surface-container-highest, bg-surface-bright, bg-surface-variant
Text on surface: text-on-surface, text-on-surface-variant
Primary: bg-primary, bg-primary-container, text-primary, text-primary-container, text-on-primary, text-on-primary-container
Secondary: bg-secondary, bg-secondary-container, text-on-secondary, text-on-secondary-container
Tertiary: bg-tertiary, bg-tertiary-container
Error: bg-error, bg-error-container, text-on-error, text-on-error-container
Outline: border-outline, border-outline-variant, ring-outline
Inverse: bg-inverse-surface, text-inverse-on-surface, text-inverse-primary
Background: bg-background, text-on-background

Utilities: glass, glass-card, hero-gradient, cta-gradient
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Radix UI components from shadcn to M3 token classes</name>
  <files>src/components/ui/button.tsx, src/components/ui/sheet.tsx, src/components/ui/dropdown-menu.tsx</files>
  <read_first>
    - src/components/ui/button.tsx (current hardcoded neutral classes to replace)
    - src/components/ui/sheet.tsx (current shadcn classes: bg-background, ring-offset-background, ring-ring, text-foreground, text-muted-foreground, data-[state=open]:bg-secondary)
    - src/components/ui/dropdown-menu.tsx (current shadcn classes: bg-popover, text-popover-foreground, focus:bg-accent, focus:text-accent-foreground, bg-muted)
    - .planning/phases/08-token-system/08-RESEARCH.md (Pattern 4 mapping table and verified code examples)
  </read_first>
  <action>
**button.tsx** — Replace the entire `buttonVariants` cva definition. Per D-09/D-10 and the research mapping table:

Base classes (line 8): Replace `focus-visible:ring-1 focus-visible:ring-neutral-950 dark:focus-visible:ring-neutral-300` with `focus-visible:ring-1 focus-visible:ring-outline`. Keep all other base classes unchanged.

Variant `default` (line 13): Replace `bg-neutral-900 text-neutral-50 shadow hover:bg-neutral-900/90 dark:bg-neutral-50 dark:text-neutral-900 dark:hover:bg-neutral-50/90` with `bg-primary-container text-on-primary-container shadow hover:bg-primary-container/90`

Variant `destructive` (line 15): Replace `bg-red-500 text-neutral-50 shadow-xs hover:bg-red-500/90 dark:bg-red-900 dark:text-neutral-50 dark:hover:bg-red-900/90` with `bg-error-container text-on-error-container shadow-xs hover:bg-error-container/90`

Variant `outline` (line 17): Replace `border border-neutral-200 bg-white shadow-xs hover:bg-neutral-100 hover:text-neutral-900 dark:border-neutral-800 dark:bg-neutral-950 dark:hover:bg-neutral-800 dark:hover:text-neutral-50` with `border border-outline-variant bg-surface shadow-xs hover:bg-surface-container hover:text-on-surface`

Variant `secondary` (line 19): Replace `bg-neutral-100 text-neutral-900 shadow-xs hover:bg-neutral-100/80 dark:bg-neutral-800 dark:text-neutral-50 dark:hover:bg-neutral-800/80` with `bg-surface-container-high text-on-surface shadow-xs hover:bg-surface-container-high/80`

Variant `ghost` (line 21): Replace `hover:bg-neutral-100 hover:text-neutral-900 dark:hover:bg-neutral-800 dark:hover:text-neutral-50` with `hover:bg-surface-container hover:text-on-surface`

Variant `link` (line 22): Replace `text-neutral-900 underline-offset-4 hover:underline dark:text-neutral-50` with `text-primary-container underline-offset-4 hover:underline`

IMPORTANT: All `dark:` prefixed overrides are REMOVED from button variants. The M3 tokens auto-switch via CSS variables — no dark: prefixes needed.

**sheet.tsx** — Make these specific class replacements:

`sheetVariants` base (line 32): Replace `bg-background` with `bg-surface`

SheetClose (line 63): Replace `ring-offset-background` with `ring-offset-surface`. Replace `focus:ring-ring` with `focus:ring-outline`. Replace `data-[state=open]:bg-secondary` with `data-[state=open]:bg-surface-container-high`.

SheetTitle (line 91): Replace `text-foreground` with `text-on-surface`

SheetDescription (line 103): Replace `text-muted-foreground` with `text-on-surface-variant`

**dropdown-menu.tsx** — Make these specific class replacements:

DropdownMenuSubTrigger (line 30): Replace `focus:bg-accent data-[state=open]:bg-accent` with `focus:bg-surface-container-high data-[state=open]:bg-surface-container-high`

DropdownMenuSubContent (line 49): Replace `bg-popover` with `bg-surface-container-highest`. Replace `text-popover-foreground` with `text-on-surface`.

DropdownMenuContent (line 66): Replace `bg-popover` with `bg-surface-container-highest`. Replace `text-popover-foreground` with `text-on-surface`.

DropdownMenuItem (line 84): Replace `focus:bg-accent focus:text-accent-foreground` with `focus:bg-surface-container-high focus:text-on-surface`

DropdownMenuCheckboxItem (line 100): Replace `focus:bg-accent focus:text-accent-foreground` with `focus:bg-surface-container-high focus:text-on-surface`

DropdownMenuRadioItem (line 123): Replace `focus:bg-accent focus:text-accent-foreground` with `focus:bg-surface-container-high focus:text-on-surface`

DropdownMenuSeparator (line 158): Replace `bg-muted` with `bg-outline-variant`
  </action>
  <verify>
    <automated>cd /Users/moni/Workspace/SpiceGrillBarWeb/spice-grill-bar-web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `button.tsx` contains `bg-primary-container text-on-primary-container` (default variant)
    - `button.tsx` contains `bg-error-container text-on-error-container` (destructive variant)
    - `button.tsx` contains `bg-surface-container-high text-on-surface` (secondary variant)
    - `button.tsx` contains `hover:bg-surface-container hover:text-on-surface` (ghost variant)
    - `button.tsx` contains `text-primary-container` (link variant)
    - `button.tsx` does NOT contain the string `neutral-`
    - `button.tsx` does NOT contain the string `dark:`
    - `sheet.tsx` contains `bg-surface` (not `bg-background`)
    - `sheet.tsx` contains `ring-offset-surface` (not `ring-offset-background`)
    - `sheet.tsx` contains `focus:ring-outline` (not `focus:ring-ring`)
    - `sheet.tsx` contains `text-on-surface` (not `text-foreground`)
    - `sheet.tsx` contains `text-on-surface-variant` (not `text-muted-foreground`)
    - `dropdown-menu.tsx` contains `bg-surface-container-highest` (not `bg-popover`)
    - `dropdown-menu.tsx` contains `text-on-surface` (not `text-popover-foreground`)
    - `dropdown-menu.tsx` contains `focus:bg-surface-container-high` (not `focus:bg-accent`)
    - `dropdown-menu.tsx` contains `bg-outline-variant` (not `bg-muted`)
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>All three Radix UI components use M3 token classes exclusively. No shadcn token names or hardcoded neutral-* colors remain. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Replace brand-* and remaining shadcn class references across all components</name>
  <files>src/layouts/Layout.astro, src/components/ReviewsSection.astro, src/components/Header.tsx, src/components/Hero.astro, src/components/MenuSection.tsx, src/components/MobileActionButtons.astro, src/pages/faq.astro, src/pages/directions.astro, src/pages/near-grand-canyon.astro</files>
  <read_first>
    - src/layouts/Layout.astro (line 123: bg-background)
    - src/components/ReviewsSection.astro (lines 24, 28, 33, 34, 36, 68, 69: brand-orange, brand-gold, text-foreground, text-muted-foreground)
    - src/components/Header.tsx (lines 54, 57, 76, 111: brand-orange, hover:bg-orange-700)
    - src/components/Hero.astro (lines 31, 46: brand-orange)
    - src/components/MenuSection.tsx (lines 153, 160: brand-orange, border-orange-*)
    - src/components/MobileActionButtons.astro (line 29: brand-orange, to-red-600)
    - src/pages/faq.astro (line 15: text-brand-orange)
    - src/pages/directions.astro (lines 23, 53-89, 114-246, 266-278: brand-orange)
    - src/pages/near-grand-canyon.astro (lines 19, 106-156, 184-196: brand-orange)
  </read_first>
  <action>
Replace all brand-* and remaining shadcn token class references. Per D-12, all brand tokens are replaced by M3 primary family. Per D-08, all shadcn names removed.

**Layout.astro (line 123):**
- `bg-background` -> `bg-background` (NOTE: `--background` and `bg-background` still exist in M3 tokens — Plan 01 defines `--color-background` in @theme inline. This class is fine to keep.)

**ReviewsSection.astro:**
- Line 24: `bg-brand-orange/10` -> `bg-primary-container/10`
- Line 28: `bg-brand-gold/10` -> `bg-secondary-container/10`
- Line 33: `text-foreground` -> `text-on-surface`
- Line 34: `text-brand-orange` -> `text-primary-container`
- Line 36: `text-muted-foreground` -> `text-on-surface-variant`
- Line 68: `from-brand-orange to-brand-gold` -> `from-primary-container to-secondary-container`
- Line 69: `from-brand-orange to-brand-gold` -> `from-primary-container to-secondary-container`

**Header.tsx:**
- Line 54: `hover:text-brand-orange` -> `hover:text-primary-container`
- Line 57: `bg-brand-orange` -> `bg-primary-container`
- Line 76: `bg-brand-orange hover:bg-orange-700` -> `bg-primary-container hover:bg-primary-container/90`
- Line 76: `shadow-md shadow-orange-500/20` -> `shadow-md` (tinted shadow handled globally by --shadow-color)
- Line 111: `hover:text-brand-orange` -> `hover:text-primary-container`
- Line 111: `focus-visible:ring-orange-500` -> `focus-visible:ring-outline`
- Also check line 54 for `focus-visible:ring-orange-500` and replace with `focus-visible:ring-outline`

**Hero.astro:**
- Line 31: `from-brand-orange/5` -> `from-primary-container/5`
- Line 46: `text-brand-orange` -> `text-primary-container`

**MenuSection.tsx:**
- Line 153: `text-brand-orange` -> `text-primary-container`. Also `border-b border-orange-300 dark:border-orange-700` -> `border-b border-outline-variant` (ghost border per DESIGN.md, no dark: prefix needed since outline-variant auto-switches)
- Line 160: `group-hover:text-brand-orange` -> `group-hover:text-primary-container`

**MobileActionButtons.astro:**
- Line 29: `from-brand-orange to-red-600` -> `from-primary-container to-inverse-primary`. Also `shadow-md shadow-orange-500/20` -> `shadow-md`
- Also replace `font-serif` with `font-display` if present (Playfair replaced by Manrope per D-25 / font decision)

**faq.astro:**
- Line 15: `text-brand-orange` -> `text-primary-container`

**directions.astro:**
- Line 23: `text-brand-orange` -> `text-primary-container`
- Lines 53, 59, 65, 71, 77, 83, 89 (direction link buttons): Replace `bg-zinc-100 dark:bg-zinc-800 text-zinc-700 dark:text-zinc-300 hover:bg-brand-orange hover:text-white` with `bg-surface-container text-on-surface-variant hover:bg-primary-container hover:text-on-primary-container`
- Lines 114, 136, 158, 180, 202, 224, 246 (phone links): `hover:text-brand-orange` -> `hover:text-primary-container`
- Line 266: `bg-brand-orange text-white hover:bg-orange-600` -> `bg-primary-container text-on-primary-container hover:bg-primary-container/90`
- Lines 272, 278: `hover:border-brand-orange hover:text-brand-orange` -> `hover:border-primary-container hover:text-primary-container`. Also `border-zinc-300 dark:border-white/20 text-zinc-900 dark:text-white` -> `border-outline-variant text-on-surface`

**near-grand-canyon.astro:**
- Line 19: `text-brand-orange` -> `text-primary-container`
- Lines 106, 119, 132, 145, 156 (price spans): `text-brand-orange` -> `text-primary-container`
- Line 184: `bg-brand-orange text-white hover:bg-orange-600` -> `bg-primary-container text-on-primary-container hover:bg-primary-container/90`
- Lines 190, 196: `hover:border-brand-orange hover:text-brand-orange` -> `hover:border-primary-container hover:text-primary-container`. Also `border-zinc-300 dark:border-white/20 text-zinc-900 dark:text-white` -> `border-outline-variant text-on-surface`

IMPORTANT: After all replacements, also check for any remaining hardcoded `dark:bg-zinc-*`, `dark:text-zinc-*`, `dark:border-zinc-*`, `bg-zinc-*`, `text-zinc-*` patterns in the files being modified. Replace with M3 equivalents where the intent is clear:
- `bg-zinc-100`/`dark:bg-zinc-800` (surface-like) -> `bg-surface-container`
- `text-zinc-700`/`dark:text-zinc-300` (muted text) -> `text-on-surface-variant`
- `text-zinc-900`/`dark:text-white` (body text) -> `text-on-surface`
- `text-zinc-800`/`dark:text-zinc-200` (body text) -> `text-on-surface`
- `dark:border-white/20` -> just `border-outline-variant` (auto-switches)

NOTE: Only replace zinc/dark: patterns in the 12 files listed above. Do NOT touch files outside this plan's scope.
  </action>
  <verify>
    <automated>cd /Users/moni/Workspace/SpiceGrillBarWeb/spice-grill-bar-web && npm run build 2>&1 | tail -5 && echo "---" && grep -r "brand-orange\|brand-green\|brand-gold" src/ --include="*.tsx" --include="*.astro" --include="*.ts" | grep -v "globals.css" | head -5 && echo "BRAND CHECK DONE"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "brand-orange" src/ --include="*.tsx" --include="*.astro"` returns zero matches (excluding globals.css which was cleaned in Plan 01)
    - `grep -r "brand-green" src/ --include="*.tsx" --include="*.astro"` returns zero matches
    - `grep -r "brand-gold" src/ --include="*.tsx" --include="*.astro"` returns zero matches
    - `grep -r "text-foreground" src/ --include="*.tsx" --include="*.astro"` returns zero matches
    - `grep -r "text-muted-foreground" src/ --include="*.tsx" --include="*.astro"` returns zero matches
    - `grep -r "bg-popover" src/ --include="*.tsx" --include="*.astro"` returns zero matches
    - `ReviewsSection.astro` contains `text-primary-container` (not `text-brand-orange`)
    - `Header.tsx` contains `hover:text-primary-container` (not `hover:text-brand-orange`)
    - `directions.astro` contains `bg-primary-container text-on-primary-container` (not `bg-brand-orange text-white`)
    - `MenuSection.tsx` contains `border-outline-variant` (not `border-orange-300`)
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>All shadcn and brand-* class references replaced with M3 equivalents across all 12 files. Zero brand-orange/green/gold usage remains in src/. Zero shadcn semantic class names remain. Build passes cleanly.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `grep -r "brand-orange\|brand-green\|brand-gold\|text-foreground\|text-muted-foreground\|bg-popover\|bg-accent\|bg-muted\|ring-ring\|ring-offset-background\|bg-background" src/ --include="*.tsx" --include="*.astro" | grep -v Layout.astro` returns zero matches (Layout.astro keeps bg-background which is a valid M3 token)
- `npm run qa` passes (full quality gate)
</verification>

<success_criteria>
- TOKEN-03 fully satisfied: all shadcn semantic tokens remapped to M3 colors
- Zero brand-* class references in any source file
- Zero shadcn-specific class names in any component
- All Radix components use M3 token names exclusively
- Build and quality checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-token-system/08-02-SUMMARY.md`
</output>
