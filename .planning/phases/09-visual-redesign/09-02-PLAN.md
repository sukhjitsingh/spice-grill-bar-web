---
phase: 09-visual-redesign
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/components/Hero.astro
  - src/pages/index.astro
autonomous: true
requirements:
  - VISUAL-06
  - VISUAL-10

must_haves:
  truths:
    - "Hero H1 is 'Spice Grill & Bar' in text-display-lg with no orange text"
    - "Hero shows cuisine type, location, hours, phone, and star rating above the fold"
    - "All hero text is crawlable HTML mapping to RestaurantSchema fields"
    - "Hero has primary CTA 'ORDER NOW' (orange) and tertiary CTA 'VIEW MENU'"
    - "Home page section order is Hero > OurStory > Reviews > Menu > Order > Location per D-08"
  artifacts:
    - path: "src/components/Hero.astro"
      provides: "Info-rich hero with SEO/AEO content"
      contains: "Spice Grill & Bar"
    - path: "src/pages/index.astro"
      provides: "Correct section ordering"
  key_links:
    - from: "src/components/Hero.astro"
      to: "RestaurantSchema"
      via: "Crawlable H1, hours, phone, address"
      pattern: "Spice Grill & Bar"
    - from: "src/pages/index.astro"
      to: "all section components"
      via: "import + render order"
      pattern: "OurStorySection.*ReviewsSection.*MenuSection.*OrderSection.*LocationSection"
---

<objective>
Redesign the Hero section with the info-rich SEO/AEO layout and reorder home page sections.

Purpose: The Hero is the most critical SEO element — H1 must be the business name, and all key business info (cuisine, location, hours, phone, rating) must be crawlable above the fold. The section reorder establishes the story-driven flow per D-08.
Output: Completely rewritten Hero.astro; reordered index.astro.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-visual-redesign/09-CONTEXT.md
@.planning/phases/09-visual-redesign/09-UI-SPEC.md
@.planning/phases/09-visual-redesign/09-RESEARCH.md
@src/components/Hero.astro
@src/pages/index.astro

<interfaces>
From src/components/ui/AstroButton.astro (used by Hero CTAs):
- Accepts `href`, `variant` ("default" | "outline" | "ghost"), `class` props
- Default variant = orange primary CTA button
- Outline variant = tertiary/outline button

From src/styles/globals.css (typography utilities from Plan 01):
- .text-display-lg: Manrope 3.5rem/500/1.1/-0.02em
- .text-body-lg: Inter 1.125rem/400/1.6/0em
- .text-body-md: Inter 1rem/400/1.5/0em
- .text-label-sm: Inter 0.75rem/600/1.4/0.06em
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Hero.astro with info-rich SEO layout</name>
  <files>src/components/Hero.astro</files>
  <read_first>
    - src/components/Hero.astro (current state — complete rewrite needed)
    - src/components/ui/AstroButton.astro (button API — variant props, href)
    - .planning/phases/09-visual-redesign/09-UI-SPEC.md (Hero section contract — lines 196-226)
    - docs/DESIGN.md (hero-gradient specification)
  </read_first>
  <action>
Complete rewrite of Hero.astro per D-05/D-06/D-07 and UI-SPEC Hero contract.

Keep existing imports: `Image` from `astro:assets`, `ArrowRight` from `lucide-react`, `heroBg` from `../assets/HomePageBackground.webp`, `AstroButton`.
Add import: `Star` from `lucide-react` (for star rating icons).

Structure (per UI-SPEC ASCII layout):

1. **Outer wrapper**: `<section>` (not `<header>`) with `class="relative w-full min-h-screen flex flex-col justify-center items-center text-center overflow-hidden bg-surface-dim"`. Change from `bg-surface` to `bg-surface-dim` per D-10.

2. **Background image layer**: Keep existing `<Image>` with `loading="eager"` and `fetchpriority="high"`. Keep `opacity-40` and responsive `widths`/`sizes`.

3. **Gradient overlay**: Replace current dual gradients with single `.hero-gradient` utility class: `<div class="absolute inset-0 hero-gradient"></div>`. Remove the old `bg-gradient-to-t` and `bg-gradient-to-b` divs.

4. **Content container**: `<div class="relative z-10 max-w-3xl px-6 flex flex-col items-center">`

5. **EST. 2024 badge**: `<span class="inline-block py-1.5 px-4 rounded-full bg-surface-container-high/50 text-label-sm uppercase tracking-wider text-on-surface-variant backdrop-blur-sm">EST. 2024</span>`. Remove `border border-outline-variant/30` (no-line rule D-16). Remove `text-primary-container` — badge is NOT an orange context.

6. **H1**: `<h1 class="text-[2.25rem] md:text-display-lg text-on-surface mt-6 mb-4">Spice Grill & Bar</h1>`. NO orange text. NO `<span class="text-primary-container">`. Responsive: 2.25rem mobile, 3.5rem desktop via text-display-lg.

7. **Cuisine/location line**: `<p class="text-body-lg text-on-surface-variant mb-2">Authentic Punjabi Cuisine &middot; Ash Fork, AZ &middot; Route 66</p>`

8. **Hours lines**:
```html
<div class="text-body-md text-on-surface-variant mb-1">
  <p>Open Tue-Thu 8AM-9PM &middot; Fri-Sun 8AM-10PM</p>
</div>
```

9. **Phone**: `<a href="tel:+19282771292" class="text-body-md text-primary-container hover:underline mb-4">(928) 277-1292</a>` — phone IS orange (accent context 4, section accent detail).

10. **Star rating badge**:
```html
<div class="flex items-center gap-2 mb-8">
  <div class="flex" aria-label="4.8 out of 5 stars">
    {[...Array(5)].map(() => <Star className="w-4 h-4 fill-primary-container text-primary-container" />)}
  </div>
  <span class="text-label-sm text-on-surface-variant">4.8 &middot; 50+ Reviews</span>
</div>
```
Stars use `fill-primary-container text-primary-container` (accent context 2).

11. **CTA buttons**:
```html
<div class="flex flex-col sm:flex-row items-center gap-4">
  <AstroButton href="https://order.toasttab.com/online/spice-grill-bar" target="_blank" rel="noopener noreferrer">
    ORDER NOW
  </AstroButton>
  <AstroButton href="#menu" variant="outline" class="group">
    VIEW MENU <ArrowRight className="w-4 h-4 group-hover:translate-x-0.5 transition-transform" />
  </AstroButton>
</div>
```
"ORDER NOW" = primary CTA (accent context 1). "VIEW MENU" = outline/tertiary. The ORDER NOW link should go to the Toast ordering URL (check current OrderSection.astro for the correct URL).

All text must be crawlable HTML — no images of text. Every piece of info (business name, cuisine, location, hours, phone) maps to RestaurantSchema fields.
  </action>
  <verify>
    <automated>grep -q 'Spice Grill & Bar' src/components/Hero.astro && grep -q 'text-display-lg' src/components/Hero.astro && grep -q 'bg-surface-dim' src/components/Hero.astro && grep -q 'hero-gradient' src/components/Hero.astro && grep -q '928.*277.*1292' src/components/Hero.astro && grep -q 'ORDER NOW' src/components/Hero.astro && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Hero.astro contains `<h1` with text "Spice Grill & Bar" (not "The Soul of Punjab")
    - Hero.astro contains `text-display-lg` on the H1 element
    - Hero.astro contains `bg-surface-dim` on the outer section
    - Hero.astro contains `hero-gradient` (not manual bg-gradient-to-t)
    - Hero.astro contains `(928) 277-1292` as a clickable `<a href="tel:...">`
    - Hero.astro contains `ORDER NOW` text in a primary CTA
    - Hero.astro contains `VIEW MENU` text in an outline CTA
    - Hero.astro does NOT contain `text-primary-container` on the H1 or any span inside H1
    - Hero.astro does NOT contain `border-outline-variant` (no-line rule)
    - Hero.astro contains star rating icons with `fill-primary-container`
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>Hero.astro is completely rewritten with info-rich SEO layout, crawlable business info, and correct token usage</done>
</task>

<task type="auto">
  <name>Task 2: Reorder home page sections in index.astro per D-08</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (current section order — Hero, Reviews, Menu, OurStory, Location, Order)
  </read_first>
  <action>
Reorder the component render order inside `<main>` in `src/pages/index.astro` to match D-08 story-driven flow:

Current order:
```
<Hero />
<ReviewsSection />
<MenuSection client:visible={{ rootMargin: '200px' }} />
<OurStorySection />
<LocationSection />
<OrderSection />
```

Target order per D-08:
```
<Hero />
<OurStorySection />
<ReviewsSection />
<MenuSection client:visible={{ rootMargin: '200px' }} />
<OrderSection />
<LocationSection />
```

Keep all imports and `client:visible` directive on MenuSection exactly as-is. Only change the render order of the components inside `<main>`.
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('src/pages/index.astro','utf8'); const m=f.match(/<(Hero|OurStory|Reviews|Menu|Order|Location)/g); const expected=['Hero','OurStory','Reviews','Menu','Order','Location']; const actual=m.map(s=>s.slice(1).replace('Section','')); console.log(JSON.stringify(actual)); if(JSON.stringify(actual)===JSON.stringify(expected)) process.exit(0); else process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - index.astro renders sections in this exact order: Hero, OurStorySection, ReviewsSection, MenuSection, OrderSection, LocationSection
    - MenuSection retains `client:visible={{ rootMargin: '200px' }}` directive
    - All imports remain unchanged
  </acceptance_criteria>
  <done>Home page section order matches D-08: Hero > OurStory > Reviews > Menu > Order > Location</done>
</task>

</tasks>

<verification>
- Hero H1 contains "Spice Grill & Bar" with text-display-lg class
- No orange text on H1 (VISUAL-10 compliance)
- Phone number, hours, cuisine, location all present as crawlable HTML
- Star rating with fill-primary-container
- index.astro section order: Hero > OurStory > Reviews > Menu > Order > Location
- `npm run build` passes
</verification>

<success_criteria>
- Hero is info-rich with all business details above the fold
- Section order follows D-08 story-driven flow
- Orange appears only in approved contexts (CTA button, stars, phone link)
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-visual-redesign/09-02-SUMMARY.md`
</output>
