---
phase: 14-speakable-coverage
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/directions.astro
autonomous: true
requirements:
  - AEO-13

must_haves:
  truths:
    - "Voice assistants can extract spoken driving directions for Flagstaff, Williams, and Las Vegas from /directions/"
    - "The Flagstaff, Williams, and Las Vegas primary direction paragraphs carry the speakable-city-directions class"
    - "The Speakable cssSelector array on /directions/ includes .speakable-city-directions as the 4th entry"
    - "Seligman, Kingman, Los Angeles, and Phoenix paragraphs are NOT annotated (out of scope per D-05)"
    - "Address blocks are NOT annotated (out of scope per D-05)"
  artifacts:
    - path: "src/pages/directions.astro"
      provides: "speakable-city-directions class on 3 city paragraphs + extended cssSelector with 4th entry"
      contains: "speakable-city-directions"
  key_links:
    - from: "directions.astro SpeakableSpecification cssSelector"
      to: "directions.astro <p class='speakable-city-directions ...'>  (3 occurrences)"
      via: "cssSelector: ['.speakable-heading', '.speakable-lead', '.speakable-exit', '.speakable-city-directions']"
      pattern: "speakable-city-directions"
---

<objective>
Extend Speakable coverage on /directions/ so voice assistants can extract spoken driving directions for the three highest-traffic origin cities: Flagstaff, Williams, and Las Vegas.

Purpose: AEO-13 — the existing Speakable block on directions.astro covers the page header and lead paragraph. Adding per-city direction paragraphs as Speakable targets lets voice assistants answer "how do I get to Spice Grill from Flagstaff/Williams/Las Vegas" with the specific driving text.

Output:
- `src/pages/directions.astro` — `speakable-city-directions` class added to primary `<p>` in sections #flagstaff, #williams, #las-vegas; existing Speakable cssSelector array extended from 3 entries to 4
</objective>

<execution_context>
@/Users/moni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-speakable-coverage/14-CONTEXT.md
@.planning/phases/14-speakable-coverage/14-RESEARCH.md

<interfaces>
<!-- Extracted from src/pages/directions.astro — executor reads these directly. -->

Current state of the three target paragraphs (verified from source):

Flagstaff section (id="flagstaff"), primary <p> — current line 93:
  <p class="text-body-lg text-on-surface-variant mb-4">
    From Flagstaff, take I-40 West toward Kingman. Drive 51 miles (about 46 minutes) and
    take <strong>Exit 146</strong> (Ash Fork / Historic Route 66). Turn right onto Lewis Ave —
    Spice Grill &amp; Bar is at 33 Lewis Ave on your right.
  </p>

Williams section (id="williams"), primary <p> — current line 112:
  <p class="text-body-lg text-on-surface-variant mb-4">
    From Williams, take I-40 West. Drive 18 miles (about 18 minutes) and take
    <strong>Exit 146</strong>. Turn right onto Lewis Ave — Spice Grill &amp; Bar is at 33
    Lewis Ave on your right.
  </p>

Las Vegas section (id="las-vegas"), primary <p> — current line 150:
  <p class="text-body-lg text-on-surface-variant mb-4">
    From Las Vegas, take US-93 South to I-40 East. Drive approximately 200 miles (about 3
    hours) and take <strong>Exit 146</strong> in Ash Fork. Turn left onto Lewis Ave — Spice Grill
    &amp; Bar is at 33 Lewis Ave.
  </p>

Current Speakable schema block (directions.astro lines 288-299):
  <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "WebPage",
      "name": "Directions to Spice Grill & Bar",
      "speakable": {
        "@type": "SpeakableSpecification",
        "cssSelector": [".speakable-heading", ".speakable-lead", ".speakable-exit"]
      }
    }
  </script>

Sections explicitly excluded (do NOT add speakable-city-directions):
  - #seligman primary <p> (line ~131)
  - #los-angeles primary <p>
  - #kingman primary <p>
  - #phoenix primary <p>
  - All <address> blocks in every section
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add speakable-city-directions class to Flagstaff, Williams, Las Vegas paragraphs and extend cssSelector</name>
  <files>src/pages/directions.astro</files>
  <read_first>
    - src/pages/directions.astro — read in full before editing; confirm the three target <p> elements are at approximately lines 93, 112, and 150 with class "text-body-lg text-on-surface-variant mb-4" and no speakable class; confirm Speakable cssSelector at lines 288-299
    - .planning/phases/14-speakable-coverage/14-CONTEXT.md — sections: D-05 (which paragraphs, which excluded), D-06 (cssSelector extension)
    - .planning/phases/14-speakable-coverage/14-RESEARCH.md — sections: "Directions City Paragraph Annotation", "Directions Speakable cssSelector Extension"
  </read_first>
  <action>
    Make four surgical changes to src/pages/directions.astro:

    CHANGE 1 — Flagstaff primary <p> class update.
    The <p> in section id="flagstaff" (approximately line 93) currently has:
      class="text-body-lg text-on-surface-variant mb-4"
    Change to:
      class="speakable-city-directions text-body-lg text-on-surface-variant mb-4"
    (speakable-city-directions prepended as first class per RESEARCH.md A1 assumption — class order has no semantic effect)

    CHANGE 2 — Williams primary <p> class update.
    The <p> in section id="williams" (approximately line 112) currently has:
      class="text-body-lg text-on-surface-variant mb-4"
    Change to:
      class="speakable-city-directions text-body-lg text-on-surface-variant mb-4"

    CHANGE 3 — Las Vegas primary <p> class update.
    The <p> in section id="las-vegas" (approximately line 150) currently has:
      class="text-body-lg text-on-surface-variant mb-4"
    Change to:
      class="speakable-city-directions text-body-lg text-on-surface-variant mb-4"

    CHANGE 4 — Extend Speakable cssSelector array (approximately line 296).
    The current cssSelector line reads:
      "cssSelector": [".speakable-heading", ".speakable-lead", ".speakable-exit"]
    Change to:
      "cssSelector": [".speakable-heading", ".speakable-lead", ".speakable-exit", ".speakable-city-directions"]

    Constraints:
    - The Seligman <p>, Los Angeles <p>, Kingman <p>, and Phoenix <p> sections must NOT receive the speakable-city-directions class (D-05)
    - The <address> blocks in Flagstaff, Williams, and Las Vegas sections must NOT receive the class (D-05)
    - Do NOT modify any other attribute, text content, or tag in the file
    - No new lines, imports, or schema blocks are added — only the 4 surgical class/JSON changes above
  </action>
  <verify>
    <automated>cd /Users/moni/Workspace/SpiceGrillBarWeb/spice-grill-bar-web && npm run build 2>&1 | tail -5 && grep -c "speakable-city-directions" dist/directions/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "speakable-city-directions" dist/directions/index.html` returns exactly 4 (3 on the <p> class attributes + 1 in the cssSelector JSON)
    - `grep -n "speakable-city-directions" src/pages/directions.astro` shows exactly 4 matches: Flagstaff <p>, Williams <p>, Las Vegas <p>, and the cssSelector array
    - Seligman section: `grep -A5 'id="seligman"' src/pages/directions.astro` does NOT contain "speakable-city-directions"
    - Los Angeles section: `grep -A5 'id="los-angeles"' src/pages/directions.astro` does NOT contain "speakable-city-directions"
    - Kingman section: `grep -A5 'id="kingman"' src/pages/directions.astro` does NOT contain "speakable-city-directions"
    - Phoenix section: `grep -A5 'id="phoenix"' src/pages/directions.astro` does NOT contain "speakable-city-directions"
    - `grep -c '"cssSelector"' dist/directions/index.html` returns 1 (one Speakable block, not duplicated)
    - `grep "speakable-city-directions" dist/directions/index.html | grep '"cssSelector"'` returns 1 match (the 4th selector entry in the JSON)
    - `npm run build` exits 0 with no Astro errors
    - `npm run test:aeo` exits 0 (all existing gates still pass; new FAQ gate from Plan 01 also passes if dist/faq/ was built)
  </acceptance_criteria>
  <done>directions.astro built with speakable-city-directions on exactly 3 city paragraphs (Flagstaff, Williams, Las Vegas) and cssSelector extended to 4 entries including .speakable-city-directions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static HTML → Crawlers | Speakable class and JSON-LD are public structured data for Google/voice crawlers; no user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Information Disclosure | speakable-city-directions class + Speakable JSON-LD | accept | Intentionally public structured data; directions content is already visible on the page |
| T-14-04 | Tampering | directions.astro class annotation | accept | Static file in version control; no runtime modification path |
</threat_model>

<verification>
After task completes:

1. `npm run build` exits 0
2. `grep -c "speakable-city-directions" dist/directions/index.html` returns exactly 4
3. `grep -A8 "SpeakableSpecification" dist/directions/index.html` shows all 4 cssSelector entries: .speakable-heading, .speakable-lead, .speakable-exit, .speakable-city-directions
4. Manual: `grep -A5 'id="seligman"' dist/directions/index.html` does NOT contain "speakable-city-directions"
5. `npm run test:aeo` exits 0 (existing gates unaffected)
6. `npm run lint` exits 0
</verification>

<success_criteria>
- Primary direction paragraphs in Flagstaff, Williams, and Las Vegas sections carry speakable-city-directions class
- No other city sections or address blocks are annotated
- Speakable cssSelector array has exactly 4 entries with .speakable-city-directions as the 4th
- Built HTML confirms 4 occurrences of speakable-city-directions (3 on elements + 1 in JSON-LD)
- npm run build and npm run test:aeo both exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/14-speakable-coverage/14-02-SUMMARY.md`
</output>
